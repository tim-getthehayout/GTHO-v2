# Project Changelog

| Date | Change | Files |
|------|--------|-------|
| 2026-04-20 | refactor(survey): delegate Pasture Survey card body to `renderSurveyCard` (OI-0126). Closes the OI-0112 unification gap on the Field Mode → Pasture Survey path. `openSurveySheet` in `src/features/locations/index.js` now mounts one `renderSurveyCard` per paddock instead of its own hand-rolled form — ~100 lines of duplicated rendering deleted (outer rating slider + bar + sync helpers, hand-rolled Height/Cover 2-up row, standalone Bale-Ring Residues block, hand-rolled condition chips, hand-rolled Recovery min–max). Notes textarea is now wired — pre-refactor `readings[].notes` was a dead write with no UI input. Recovery min–max now renders just above Notes (per `renderSurveyCard`'s canonical ordering: `header → state → quality → condition → recovery → notes`). Layout matches the other four observation surfaces byte-for-byte: 3-up Height/Cover/Rings top row at 88/88/72px, 1–100 Relative Forage Quality slider with gradient track + Poor/Fair/Good/Excellent anchors, condition chips, recovery min–max, Notes. **Field-name rename:** local `readings` object dropped its legacy names (`rating`, `heightCm`, `coverPct`, `condition`, `baleRingCount`, `recoveryMin`, `recoveryMax`) in favor of the canonical entity names (`forageQuality`, `forageHeightCm`, `forageCoverPct`, `forageCondition`, `baleRingResidueCount`, `recoveryMinDays`, `recoveryMaxDays`). Both `SurveyDraftEntryEntity.create` and `PaddockObsEntity.create` calls simplified to spread `...r` / `...r2` since the names now match. Hydration paths from `paddockObservations` (bulk-edit) and `surveyDraftEntries` (bulk draft) updated to the canonical names via `Object.assign(readings[locId], {…})`. Completeness badge on the card header + `commitSurvey` rated filter updated to the canonical names. **State-preservation model:** `openSurveySheet` now maintains a `cards` Map keyed by `locId`; `commitReadingsFromCards()` flushes each card's `getValues()` into `readings` before any read path that needs canonical state — called at the top of `renderPaddockList()`, `saveDraft()`, `commitSurvey()`, and the Finish & Save rated-paddock count. `cards.clear()` runs at the top of `renderPaddockList()` so the Map rebuilds alongside the teardown. **OI-0124 Phase 1 parity preserved:** the new `paddockAcres` prop uses `loc2.areaHectares ?? loc2.areaHa` so the BRC auto-fill restored by fa04656 still works on this surface. **Tests:** new `tests/unit/pasture-survey-refactor.test.js` (5 cases) — Single-mode layout (obs-top-row present with 3 fields, Notes textarea present, Recovery above Notes via `compareDocumentPosition`), Bulk-mode expanded-card layout (same assertions after clicking the collapsible header), BRC auto-fill parity (active helper text + ring-count input flips cover > 0), round-trip draft save (all 8 fields populate, Save Draft writes one `survey_draft_entries` row with canonical field names including `notes`), expand/collapse state preservation (type a height, collapse, re-expand, value survives via `commitReadingsFromCards()` → `readings` → re-hydration). Suite 1182 → 1187. `npm run lint` clean (0 errors, 62 pre-existing warnings). `vite build` clean. **Grep contracts (CLAUDE.md §"Scoped Changes"):** (a) `grep -nE "r\.rating\|r\.heightCm\|r\.coverPct\|r\.baleRingCount\|r\.recoveryMin\|r\.recoveryMax" src/features/locations/index.js` → 0 hits; (b) `grep -nE "readings\[.*\]\.(rating\|heightCm\|coverPct\|baleRingCount\|recoveryMin\|recoveryMax)" src/features/locations/index.js` → 0 hits; (c) `grep -cE "import \{ renderSurveyCard \}" src/features/locations/index.js` → 1. **Schema change:** none — all 8 fields already exist in `survey_draft_entries` and `paddock_observations` with the canonical names the refactor now uses locally. **CP-55/CP-56 impact:** none — no new columns, no shape change; export/import round-trips the same rows today and will continue to. **No migration, no BACKUP_MIGRATIONS entry, no `schema_version` bump.** Line 875's `loc2.areaHa` in the card header's area meta display is pre-existing and lives under OI-0124 Phase 2 scope — left untouched per CLAUDE.md §"Scoped Changes Only". **Closes OI-0126.** | src/features/locations/index.js, tests/unit/pasture-survey-refactor.test.js, OPEN_ITEMS.md, PROJECT_CHANGELOG.md |
| 2026-04-20 | feat(settings): add Forage Types access on Settings screen (OI-0125, SP-13, GH-33). **What shipped:** new Forage Types card between Farm Settings and Field Mode with `+ Add` button, seeded-badge list, Edit pill + × delete per row. Edit sheet renders all 9 editable fields (name, DM%, N/P/K per tonne DM, DM-per-in-per-ac, min residual height, utilization %, notes) via the OI-0111 unit-aware descriptor pattern — shared helpers extracted from `settings/index.js` to new `src/features/settings/unit-descriptor.js` so farm + forage sections use the identical `toDisplayValue`/`toStoredValue`/`composeFieldLabel`/`stepForField`/`formatDisplayValue` conversion path (no duplicated conversion logic). **New unit family `dmYieldDensity`** in `src/utils/units.js` — imperial `lbs/in/ac` ↔ metric `kg/cm/ha`, reuses the `DM_LBS_IN_AC_TO_KG_CM_HA = 0.4412` constant from `v1-migration.js` (now exported from `units.js` so there's one source of truth). New descriptor flags to support the forage-yield field: `labelKeyByUnit` (base label differs per unit system: "DM per inch per acre" vs "DM per cm per ha") and `parenUnitType` (paren shows just the weight unit "lbs"/"kg" because the full per-in-per-ac unit is embedded in the base label). Delete-guard: `locations.forageTypeId` referencing rows block delete with a count-named alert; unreferenced rows proceed via confirm. Seeded-badge rendered on list rows AND inside the Edit sheet header; `is_seeded` is never flipped by edit (seeded defaults are overridable per SP-9). Empty state: "No forage types yet. Tap + Add to create one." **Tests:** `tests/unit/forage-types-settings-ui.test.js` (NEW, 10 cases — list renders row with seeded badge, Add opens sheet empty, empty-name save shows inline error, save inserts with `isSeeded=false`, edit preserves `isSeeded=true` on seeded row, delete-guard blocks when location references, delete proceeds when no reference, imperial round-trip `3.0 in → 7.62 cm → 3.0`, imperial round-trip `300 lbs/in/ac → 132.36 kg/cm/ha → 300`, metric round-trip unchanged). `tests/unit/units.test.js` extended with 3 cases (MEASURE_TYPES count 6 → 7, round-trip 300 lbs/in/ac ↔ 132.36 kg/cm/ha, `unitLabel('dmYieldDensity', …)` returns `lbs/in/ac`/`kg/cm/ha`). `tests/e2e/forage-types-settings.spec.js` (NEW) asserts a new forage_types row lands in Supabase after the UI Add flow per CLAUDE.md §"E2E Testing — Verify Supabase, Not Just UI" — cleans up after itself. Full suite 1169 → 1182 (+13 cases; settings-unit-roundtrip 34/34 still green after helper extraction). `vite build` clean. **Grep contracts:** 3 new store calls — `add('forageTypes', …, 5-param)`, `update('forageTypes', …, 6-param)`, `remove('forageTypes', …, 3-param)` — all match CLAUDE.md §"Store call param-count check" requirements. **i18n:** new `forageType` namespace with 27 keys; existing `action.save` + `action.cancel` reused for the sheet buttons. **Schema change:** none — all 14 `forage_types` columns already exist (migration 001 + 003); entity (`src/entities/forage-type.js`) already maps them all. **CP-55/CP-56 impact:** none — `forage_types` is already in `BACKUP_TABLES` and `FK_ORDER`; the new UI writes through the same entity + store path as the onboarding seed step, so the backup round-trip is already proven. **Closes OI-0125 and GH-33.** | src/utils/units.js, src/features/settings/unit-descriptor.js, src/features/settings/forage-type-sheet.js, src/features/settings/forage-types-section.js, src/features/settings/index.js, src/i18n/locales/en.json, tests/unit/forage-types-settings-ui.test.js, tests/unit/units.test.js, tests/e2e/forage-types-settings.spec.js, OPEN_ITEMS.md, PROJECT_CHANGELOG.md, github/issues/GH-33_forage-types-settings-ui.md |
| 2026-04-20 | fix(observations): wire `paddockAcres` on Move All + Event Detail §5 + Edit Paddock Window + Pasture Survey (OI-0124 Phase 1, GH-32). **Root cause:** `Location.fromSupabaseShape` returns objects with `areaHectares` (never `areaHa`), but four of the five pre-graze observation surfaces read `loc.areaHa` directly. Result: `paddockAcres = null` → `isBrcAvailable()` false → bale-ring listener no-op → user types a ring count, Forage Cover % does not auto-populate. Sub-move Open (the fifth surface) was already correct from OI-0114 NC-1. Tim reported the symptom on Move All; 3 sibling surfaces had been silently broken. **Fix:** apply the OI-0075 `areaHectares ?? areaHa` legacy-safe fallback pattern to the four read sites — `move-wizard.js:400-402`, `detail.js:527-529`, `edit-paddock-window.js:84-86`, `surveys/index.js:308-310`. One-line rationale comment added at each site naming OI-0124 Phase 1. **Tests:** new cross-surface file `tests/unit/location-area-field-brc.test.js` (4 cases, one per surface) seeds a Location with `areaHectares: 1.0` (→ ~2.47 ac) + farm_settings with `baleRingResidueDiameterCm: 365.76` (12 ft), drives each surface's render entry point (`openMoveWizard` → Step 3, `openEventDetailSheet`, `openEditPaddockWindowDialog`, `renderSurveysScreen` → draft-entry sheet), and asserts `[data-testid="obs-card-bale-ring-helper"]` shows the active-state text `/12\.0 ft/` + `/2\.47 ac/`. Active state only renders when `paddockAcres && paddockAcres > 0` (see `_shared.js:119,121`), so matching the active text is a direct proof that the prop reached the card. Suite 1165 → 1169. `vite build` clean. **Grep contract:** `grep -n "\.areaHa\b"` across the four fixed files returns only fallback expressions (`?? loc?.areaHa`) plus the Phase-2-out-of-scope `detail.js:452` (area display in §4 — will ride on the Phase 2 sweep). **No schema change. No CP-55/CP-56 impact** — read-path correction, no persisted shape change. Phase 1 closes GH-32 and flips OI-0124's status line. **OI-0124 itself stays open** — Phase 2 (20+ other `loc.areaHa` drift sites across `locations/`, `harvest/`, `field-mode/`, `amendments/`, `feed-forage.js`) and Phase 3 (legacy-fallback cleanup + CLAUDE.md Known Trap entry) are tracked under the same OI and will ship in separate specs/commits. Post-deploy browser smoke on device (4 surfaces × 1 ring-count test each) still pending user verification. | src/features/events/move-wizard.js, src/features/events/detail.js, src/features/events/edit-paddock-window.js, src/features/surveys/index.js, tests/unit/location-area-field-brc.test.js, OPEN_ITEMS.md, PROJECT_CHANGELOG.md, github/issues/GH-32_location-area-field-brc-fix.md |
| 2026-04-20 | fix(move-wizard): same-farm rotations now populate `events.source_event_id` + migration 030 one-time backfill (OI-0122). **Root cause:** `move-wizard.js:680` gated `sourceEventId` assignment behind `isCrossFarm`, leaving every same-farm rotation with `source_event_id = NULL`. The OI-0119 DMI-8 chart date-routing bridge at `dmi-chart-context.js:140-142` relies on this link to run the prior event's cascade for days that pre-date the new event's start; without it, pre-start days render as blank bars. Tim's E-3 event `fa16a58d` (rotated from G-1/G-3 `da54838f` at 2026-04-20 15:00) exhibited exactly this — Monday filled, Sat + Sun blank. **Code diff is literally one line** — `sourceEventId: isCrossFarm ? sourceEvent.id : null` → `sourceEventId: sourceEvent.id`. `src/features/events/index.js:346` already farm-id-gates the "← Moved from {farm}" banner inside the outer `if (evt.sourceEventId)` guard, so same-farm rotations don't trigger a spurious banner. **Migration 030 applied + verified:** walks the `event_group_windows` graph (for every NULL-source event, finds events whose `date_left = this event's date_joined` on the same `group_id`; unambiguous inference when every group came from one source) with a strict-inequality cycle guard (`source_start < target_start`) that excludes same-day or reverse-order candidates. **13 rows backfilled** (brief estimated ~16 — three additional cycle-guard exclusions: Corral pair `7e88a2d4`↔`8fca7c26` same-day cycle + D event `52bca23d` whose inferred source starts 2026-04-13 but target starts 2026-03-24 — cycle guard correctly refused all three). NULL count 22 → 9 (9 = 2 ambiguous + 2 Corral cycle + 4 legitimate firsts + 1 reverse-order). `operations.schema_version` 29 → 30. Motivating case `fa16a58d` now has `source_event_id = da54838f` (G-1/G-3 prior event) — its 3-day chart will fill Sat 4/18 + Sun 4/19 once the deploy lands. **Tests:** `tests/unit/move-wizard-source-event-id.test.js` (NEW, 2 cases — same-farm sets sourceEventId, cross-farm sets sourceEventId + destFarmId), new regression case in `tests/unit/dmi-chart-context.test.js` (OI-0122 — pre-start days are blank when `sourceEventId` is NULL, proving the bridge was doing the work the fix enables), `tests/e2e/move-wizard-source-event-id.spec.js` (NEW) queries Supabase `events.source_event_id` after a same-farm UI rotation per CLAUDE.md §"E2E Testing — Verify Supabase, Not Just UI". `BACKUP_MIGRATIONS[29]` no-op rule added to `src/data/backup-migrations.js` stamping `schema_version = 30`; `CURRENT_SCHEMA_VERSION` in `backup-import.js` bumped 29 → 30. Existing chain-length tests updated: `backup-roundtrip` chain 15 → 16 entries (ends at 29), `backup-import` chain target 14 → 30 and no-op case 29 → 30. Full unit suite 1165 → 1165 (no net change in count; 3 new OI-0122 cases balanced by no new tests deleted — the three cases live in 2 existing files + 1 new file). **No schema change** (column already existed since migration 015-era) — migration 030 is a one-time data backfill + version bump. **CP-55/CP-56 impact:** none on the data side (column already serialized); `BACKUP_MIGRATIONS[29]` covers the version-bump round-trip. **Closes OI-0122** (no separate `github/issues/` file — session brief `SESSION_BRIEF_2026-04-20_oi0122-source-event-id.md` was the spec). | supabase/migrations/030_backfill_source_event_id.sql, src/features/events/move-wizard.js, src/data/backup-migrations.js, src/data/backup-import.js, tests/unit/move-wizard-source-event-id.test.js, tests/unit/dmi-chart-context.test.js, tests/unit/backup-roundtrip.test.js, tests/unit/backup-import.test.js, tests/e2e/move-wizard-source-event-id.spec.js, OPEN_ITEMS.md, PROJECT_CHANGELOG.md |
| 2026-04-20 | docs(reconciliation): OPEN_ITEMS sweep — close OI-0116, OI-0052, OI-0051, OI-0087, OI-0088 with code-citation verification notes (commit 32e5d43). Docs-only reconciliation — each closure cites the live code path or shipped migration that fulfills the OI, so "open" items already delivered in earlier sessions stop accreting. No schema, no code, no CP-55/CP-56 impact. **Also logs IMPROVEMENT #15** in `IMPROVEMENTS.md` (the `git add` atomicity + pre-rename `github/issues/` pathspec strand-sibling-docs failure mode hit during OI-0119, surfaced as a silent partial-commit class that IMPROVEMENT #14's SHA-match push verification doesn't catch — documented so the doc-workflow + deploy-gate plugin skills can absorb the fix). The OPEN_ITEMS Change Log row for the reconciliation sweep already referenced this IMPROVEMENT #15 entry as existing; this row corrects the drift. | OPEN_ITEMS.md (in 32e5d43), IMPROVEMENTS.md, PROJECT_CHANGELOG.md |
| 2026-04-20 | chore(schema): drop `event_observations` table in migration 029 + code/backup/comment sweep (OI-0113, GH-31). Closes the OI-0112 → OI-0119 → OI-0113 observation-unification arc. **Why:** OI-0112 zeroed every writer (2026-04-18) and OI-0119 zeroed the last reader — the DMI-8 chart builder — so the table, entity file, push/sync registries, backup pipeline entry, and RLS policies were pure dead weight. Keeping a dead collection is a silent trap for future contributors who find the entity by grep and try to write to it (same class of bug OI-0119 closed when it found the dashboard + detail chart builders still reading `event_observations` after OI-0112). Option A (clean cut) ships per Tim's 2026-04-20 decision; Option B (REVOKE INSERT freeze) rejected — `paddock_observations` with `source='event'` is semantically identical so preserving the 17-column schema for a hypothetical future re-split adds no value. **Migration 029 applied + verified:** `DROP TABLE IF EXISTS event_observations CASCADE; UPDATE operations SET schema_version = 29;` — `information_schema.tables` count returns `0` post-drop; `SELECT schema_version FROM operations LIMIT 1` returns `29`. CASCADE handled the pre-OI-0112 orphan row documented in the spec (ID `ef5221a6-911f-48f9-8e6f-6ad4f223d123` — its parent event's pre-graze data is intact in `paddock_observations`), the 4 RLS policies from migration 021, and the `bale_ring_residue_count` column added by migration 022. Pre-migration `SELECT COUNT(*)` confirmed 1 row as expected. **Code sweep:** `src/entities/event-observation.js` + `tests/unit/entities/event-observation.test.js` deleted. `eventObservations` removed from `push-all.js` (import + TO_SB_MAP), `sync-registry.js` (import + SYNC_REGISTRY), `store.js` (ENTITY_TYPES list + `captureEventSnapshot` observations key + `restoreEventSnapshot` childTypes entry), `backup-export.js` (BACKUP_TABLES), `backup-import.js` (FK_ORDER). `CURRENT_SCHEMA_VERSION` in `backup-import.js` bumped 28 → 29. New `BACKUP_MIGRATIONS[28]` rule in `src/data/backup-migrations.js` discards `b.tables.event_observations` from older backups (schema v14 through v28) and stamps `b.schema_version = 29` — this is the CP-56 migration chain entry called out in the spec's "Export/Import Spec Sync Rule" section. **Stale pointer comments retargeted:** `src/entities/farm-setting.js:104`, `src/entities/batch.js:85`, and `tests/unit/numeric-coercion-tier1.test.js:14` all pointed at `event-observation.js` as the canonical Tier 1 numeric-coercion pattern reference; updated to `paddock-observation.js` (identical `const n = (v) => v != null ? Number(v) : null;` pattern, direct successor). Optional historical comments in `src/features/events/dmi-chart-context.js` + `src/features/dashboard/index.js` reworded to "dropped in migration 029 (OI-0113)" phrasing. Historical migration-chain comments in `backup-migrations.js` (20→21, 21→22 rules) left as-is per spec — older backups still walk through those rules and the comments document the historical operations. `tests/fixtures/backup-v14.json` left untouched — the v14 fixture's `"event_observations": []` key walks the chain and gets dropped at the new 28→29 step, proving the migration rule round-trips cleanly. Count-assertion tests updated: `store.test.js` ENTITY_TYPES 53 → 52; `backup-import.test.js` `CURRENT_SCHEMA_VERSION` 28 → 29 and FK_ORDER count 50 → 49 and chain target 14 → 29; `backup-roundtrip.test.js` chain length 14 → 15 (with new 28 entry) and the table-count constant split into `EXPECTED_FIXTURE_TABLE_COUNT=50` (historical v14) vs `EXPECTED_EXPORT_TABLE_COUNT=49` (post-drop) so the fixture assertion and the live export assertion don't conflate. Full unit suite 1162/1162 green — backup round-trip still cleanly walks v14 → v29. **Grep acceptance checks clean** per spec §Acceptance: every remaining `event_observations`/`eventObservations` match in `src/` is either the new 28→29 rule itself, a historical migration-chain comment that the spec explicitly keeps (20→21 / 21→22), a reworded history comment in the dashboard or context-builder using the allowed "dropped in migration 029" phrasing, or the new OI-0113 commentary. Every tests/ match is either the v14 fixture (spec says leave as-is) or the negative-assertion docstrings in `dmi-chart-context.test.js` (spec says keep). **OI-0059 verified 2026-04-20 — migration 020 matches live Supabase policies (see SESSION_BRIEF_2026-04-20_oi0113-drop-event-observations.md).** Ran `SELECT policyname, cmd, qual, with_check FROM pg_policies WHERE tablename = 'operation_members'` during the audit for this session brief; all 4 policies match the migration file byte-for-byte (SELECT/DELETE/UPDATE `qual = '(user_id = auth.uid())'`, INSERT `with_check = '(user_id = auth.uid())'`, no recursive subqueries). Migration 020 already shipped 2026-04-14 in commit `5fcd881` with `BACKUP_MIGRATIONS[19]` present — no zero-code action needed here beyond this attestation; OI-0059 flips from open → closed in the same commit. **No user-visible impact** — pure hygiene. **CP-55/CP-56 impact** — yes, handled via `BACKUP_MIGRATIONS[28]` per §"Export/Import Spec Sync Rule". **Schema change:** yes — migration 029, schema_version 28 → 29. Closes OI-0113 + GH-31; verifies and closes OI-0059. | supabase/migrations/029_drop_event_observations.sql, src/data/push-all.js, src/data/sync-registry.js, src/data/store.js, src/data/backup-export.js, src/data/backup-import.js, src/data/backup-migrations.js, src/entities/farm-setting.js, src/entities/batch.js, src/features/dashboard/index.js, src/features/events/dmi-chart-context.js, tests/unit/store.test.js, tests/unit/backup-import.test.js, tests/unit/backup-roundtrip.test.js, tests/unit/numeric-coercion-tier1.test.js, (deleted) src/entities/event-observation.js, (deleted) tests/unit/entities/event-observation.test.js, OPEN_ITEMS.md, github/issues/GH-31_drop-event-observations-table.md |
| 2026-04-20 | feat(members): edit display_name / email / role on pending invites + accepted members (OI-0120, GH-30). Closes the CP-66 post-close-out gap — admin now has an Edit button on every non-owner non-self row in the Member Management sheet. **Pending invite branch** (`Edit | Copy | Regenerate | Cancel`) exposes all three fields (`display_name`, `email`, role segment control) in an inline expand-in-place form; **accepted member branch** (`Role Select | Edit | Remove`) exposes `display_name` + `email` only — role stays on the existing single-tap `renderRoleSelect` per Tim's 2026-04-20 scope decision. One shared `showEditForm(member, operationId, panel, opts)` + `editMember(...)` handle both branches via `opts.isPending`. Toggle behavior — clicking Edit on an already-open form closes it. Form renders below the member-row via `insertBefore(form, row.nextSibling)`, and collapses naturally on list re-render because `renderMemberList` calls `clear(panel)`. **Pre-implementation DB check:** confirmed via MCP `pg_constraint` that `operation_members` has NO `(operation_id, email)` UNIQUE constraint — only `operation_members_invite_token_key` + primary key — so the implementation uses the spec's client-side collision pre-check branch (`select id where operation_id = ? and email = ? and id != member.id`). Belt-and-braces: a `/duplicate key|unique/i` regex in the Supabase error handler maps any future constraint-violation error to the same friendly `t('members.emailInUse')` message, so a later migration that adds the UNIQUE is a no-op for this code. Collision pre-check only fires when email actually changed (no round-trip for display-name-only edits). Six new i18n keys in `src/i18n/locales/en.json` (`members.edit`, `.editTitle`, `.editPending`, `.saveChanges`, `.changesSaved`, `.emailInUse`). Owner row + self row still have no edit buttons (CP-66 protection preserved). Structured logging via `logger.info('members', 'member edited', { operationId, memberId })` on success, `logger.error` on failure — no `console.*`. No `innerHTML`; all strings via `t()`. **Tests**: `tests/unit/member-management-edit.test.js` (NEW, 7 cases — owner no-button, self no-button, pending happy path asserts update payload + role + updated_at, accepted happy path asserts no role key in payload, validation empty-name + bad-email block Save with no update call, email collision inline error with no update call, toggle behavior). Supabase + session mocked via `vi.mock` so the live `openMemberManagementSheet` entry point can exercise the flow without network. `tests/e2e/member-edit.spec.js` (NEW) — admin opens Member Management, edits a pending invite's email, asserts Supabase `operation_members.email` reflects the new value per CLAUDE.md §"E2E Testing — Verify Supabase, Not Just UI"; cleans up by restoring the original email. Seeds a pending invite if none available in the target operation. Full unit suite 1165 → 1172 (116 files). **No schema change** — both columns exist on `operation_members` (CP-66 migration already shipped them). **No CP-55/CP-56 impact** — the existing export already serializes `display_name` + `email`; import already round-trips them. Known gap preserved from CP-66: self-profile editing (a member updating their own display_name/email) is NOT in scope — if/when this becomes painful, a separate OI covers it. Owner display_name/email edit likewise remains on the owner-protection rule. Closes OI-0120 and GH-30. | src/features/settings/member-management.js, src/i18n/locales/en.json, tests/unit/member-management-edit.test.js, tests/e2e/member-edit.spec.js, OPEN_ITEMS.md, github/issues/GH-30_edit-member-info.md |
| 2026-04-20 | feat(dmi): DMI-8 cascade rewrite — fix empty 3-day chart bars (OI-0119, GH-29). **Three compounding root causes closed in one commit**: (1) dead-table observation reads — four chart-builder sites in `dashboard/index.js` + `detail.js` were still calling `getAll('eventObservations')` after OI-0112 migrated writers to `paddock_observations`, returning `[]` on every post-OI-0112 event and silently greying the bars; (2) `loc.areaHa` vs `loc.areaHectares` field-name drift in `detail.js` chart-builder sites (sibling to the OI-0075 Bug 3 fix that covered the capacity line); (3) three logic gaps in DMI-8 itself — `feedEntries: _feedEntries` ignored deliveries, actual path required BOTH bracketing checks (silent fallthrough for single-check-in-the-past, the most common case), no cascade model. Per CLAUDE.md §"Fix Root Causes, Not Symptoms" the three bugs interact and cannot be separately patched without forcing bad design decisions elsewhere — this is one coherent rewrite. **New cascade model (canonical spec: `V2_CALCULATION_SPEC.md §4.2 DMI-8`, pre-rewritten by Cowork 2026-04-20):** per-day pasture-first → stored-second → deficit-third allocation via two buckets (`remainingPastureDm` seeded from `sum(FOR-1)` across all open paddock windows, pooled for parallel sub-paddocks; `remainingStoredDm` seeded from deliveries and re-anchored by feed checks). **Five statuses (was three):** `actual` · `estimated` · `needs_check` · `no_animals` (zero demand — distinct from missing data) · `no_pasture_data` with `reason='missing_observation'|'missing_forage_type'` (user-fixable with inline CTA). **Retroactive actual-conversion:** feed-check insertion flips the prior interval's `storedDmiKg` bars from `estimated`→`actual` (pure-functional — natural consequence of re-running DMI-8 after the insert); pasture bars in the same interval stay `estimated` (pre-graze is too subjective to true up retroactively). **Sub-move interactions:** open adds new FOR-1 to the pool on open date; close drops the closing window's attributable remainder (per-window pool tracking with proportional decrement on pool-wide demand). **Source-event bridge simplified to date-routing only:** for each chart date, the owning event's self-contained cascade runs — no state handoff between events. **New flow rule:** sub-move Close requires an inline feed-check card when `event.hasStoredFeed` is true (writes `event_feed_checks` + per-combo `event_feed_check_items` with `isCloseReading=false`, striking a clean actual/estimated boundary). **Pre-graze partial cover default:** null `forageCoverPct` with present `forageHeightCm` defaults cover to 100% and emits `hint: 'assumed_full_cover'`; renderer surfaces a "(Fix cover)" chip wired to the same `onNoPastureData` callback that opens OI-0118's Edit Paddock Window dialog. **DMI-8 input shape changes:** `feedEntries` underscore dropped (param now active), new `batches` input (map keyed by `batchId` of `{ weightPerUnitKg, dmPct }` for DM conversion of feed entries + check items), entity-native field names throughout (`feedChecks[].date` not `checkDate`; `feedCheckItems[].remainingQuantity` not `remainingQty`; observations shape matches `paddock_observations` with `type/source/sourceId/locationId`). **Shared chart-context helper** `src/features/events/dmi-chart-context.js` (NEW, ~130 lines) exports `buildDmi8ChartContext(eventId)` + `computeDmi8Days(event, dmi8)` — centralizes dead-table fix, `areaHectares ?? areaHa` fallback, OI-0117 `getEventStartDate` decoration, and the date-routing bridge. Both `dashboard/index.js` (lines 1336–1425 replaced with shared-helper call) and `detail.js` (lines 389–538 replaced) now import the helper, collapsing ~170 lines of duplicated context-gathering + bridge logic into a single call per caller. **Chart renderer `src/ui/dmi-chart.js` additions:** blank bar at barHeight for `no_animals`; grey short bar with "Add pre-graze" / "Set forage type" inline CTA for `no_pasture_data` (reason-specific, fires `opts.onNoPastureData(reason, { pwId, locationId })`); red segment atop the stored stack for estimated + `deficitKg > 0` with a `+X deficit` sub-label in red; legend adds red deficit swatch only when `days.some(d => d.result.deficitKg > 0)`; "(Fix cover)" chip for `hint: 'assumed_full_cover'`. Replaced stale `opts.onForageTypeMissing` with the unified `opts.onNoPastureData` callback. Fixed pre-existing unit-conversion bug in `toDisplay` (metric mode was dividing kg by 2.20462 via `convert(kg, 'weight', 'toMetric')` — now identity for metric, convert only for imperial). **Callback wiring:** dashboard + detail both fire `openEditPaddockWindowDialog(pw, ownerEvent, operationId)` for `missing_observation` + `assumed_full_cover`, and `navigate('#/locations')` for `missing_forage_type`. **Sub-move close** (`src/features/events/submove.js`) imports `FeedCheckEntity` + `FeedCheckItemEntity`, groups event_feed_entries by `batchId|locationId` with a card per combo (`data-testid="submove-close-feed-${batchId}-${locationId}"`), blocks Save until every input is filled (status error surfaces), then writes the check + items on Save — all `add()` calls with full 5-param count per CLAUDE.md §"Store call param-count check". **Tests (new + rewrites, +30 cases):** `tests/unit/dmi-8-cascade.test.js` (NEW, 11 cases) — cascade allocation table rows 1–5 each as a dedicated case, parallel sub-paddocks pool, sub-move open adds FOR-1 on open date, sub-move close drops attributable remainder, delivery during walk adds DM on delivery date, retroactive actual-conversion after check insertion. `tests/unit/dmi-8.test.js` (REWRITTEN, 16 cases) — 5-status model, entity-native shapes, actual/estimated/no_animals/no_pasture_data/hint coverage, single-check projection, bracketed interpolation, floor-at-0 edge cases. `tests/unit/dmi-chart-context.test.js` (NEW, 5 cases) — observations sourced from paddock_observations not event_observations, areaHectares fallback, event.dateIn derived decoration, date-routing source-event bridge. `tests/unit/dmi-chart.test.js` (NEW, 6 cases) — all 5 status branches + deficit segment + legend conditional + hint chip + callback wiring. `tests/unit/submove-close-feed-check.test.js` (NEW, 4 cases) — no-card when no stored feed, card renders one input per batch×location, blank-input Save blocked, filled-input Save creates check + items with `isCloseReading=false`. `tests/e2e/dmi-chart.spec.js` (NEW) — asserts chart renders non-empty and sub-move close feed-check round-trips to Supabase `event_feed_checks` per CLAUDE.md §"E2E Testing — Verify Supabase, Not Just UI". Full unit suite 1135 → 1165 (112 → 115 files). Grep contracts (per plan §Verification): `getAll('eventObservations')` returns 0 matches in `src/features/dashboard` + `src/features/events`; `feedEntries: _feedEntries` returns 0 in `src/calcs/feed-forage.js`; `update('events'` returns 0 in `submove.js` (OI-0115 pure-insert invariant preserved). V2_CALCULATION_SPEC.md §4.2 DMI-8 was pre-updated by Cowork (2026-04-20 changelog entry in that doc) with the authoritative cascade model; implementation matches the pre-updated spec. Remaining `loc.areaHa` sites in `detail.js` §4 (paddocks list) and §5 (pre-graze card BRC) are outside the chart-builder scope called out in the OI-0119 spec — pre-existing class-related bugs deferred to a future OI per CLAUDE.md §"Scoped Changes Only". No schema change. No CP-55/CP-56 impact (compute-on-read; forced feed-check uses existing `event_feed_checks` + `event_feed_check_items` already in CP-55). Closes OI-0119 and GH-29; supersedes OI-0069 (original DMI-8 spec) and OI-0076 (deferral). | src/calcs/feed-forage.js, src/features/events/dmi-chart-context.js, src/features/dashboard/index.js, src/features/events/detail.js, src/features/events/submove.js, src/ui/dmi-chart.js, tests/unit/dmi-8.test.js, tests/unit/dmi-8-cascade.test.js, tests/unit/dmi-chart-context.test.js, tests/unit/dmi-chart.test.js, tests/unit/submove-close-feed-check.test.js, tests/e2e/dmi-chart.spec.js, OPEN_ITEMS.md, github/issues/GH-29_dmi-8-cascade-rewrite.md |
| 2026-04-20 | feat(edit-paddock-window): render pre-graze + post-graze cards in the dialog (OI-0118, GH-28). Adds the shared `renderPreGrazeCard` + `renderPostGrazeCard` components to `openEditPaddockWindowDialog` so the edit dialog now matches the seven-surface observation-card footprint from OI-0112. **Pre-graze renders on both open and closed windows** — the root-cause angle: Event Detail §5 filters to `!pw.dateClosed`, so once a window closes, its pre-graze observation was unreachable from any UI; the edit dialog is the only surface a closed window exposes, so it becomes the single home for historical pre-graze corrections. **Post-graze renders only when `pw.dateClosed != null`** (mirrors Event Detail §6). Each card has its own inline Save button with the transient "Saved" indicator — `[data-testid="edit-pw-pregraze-save-${pw.id}"]` and `[data-testid="edit-pw-postgraze-save-${pw.id}"]` — keeping the observation write separable from the window-metadata Save. Save path follows the canonical `detail.js` §5/§6 lookup: prefer `sourceId === pw.id`, fall back to most-recent by `createdAt`, then `if (obs) update('paddockObservations', obs.id, values, PaddockObsEntity.validate, PaddockObsEntity.toSupabaseShape, 'paddock_observations')` else `add('paddockObservations', PaddockObsEntity.create({ operationId, locationId: pw.locationId, observedAt: new Date().toISOString(), type, source: 'event', sourceId: pw.id, ...values }), ...)` — all five/six store-call params per CLAUDE.md §"Store call param-count check". `paddockAcres = loc?.areaHa != null ? convert(loc.areaHa, 'area', 'toImperial') : null` is computed at dialog open (BRC late-bind not needed — `pw.locationId` is known up front). Cards land above `statusEl` / the Save/Cancel row so Reopen and Delete stay below. **Tests**: `tests/unit/edit-paddock-window-observations.test.js` — 7 cases: (1) pre-graze renders on open window, (2) pre-graze renders on closed window, (3) post-graze renders only on closed, (4) Save with no prior obs → new `paddock_observations` row with `source='event'`, `type='open'`, `sourceId=pw.id`, (5) Save with prior obs → updates existing row (no duplicate), (6) round-trip: save → reopen dialog → initialValues rehydrates, (7) post-graze Save writes `type='close'` with recovery fields. `tests/e2e/edit-paddock-window-observations.spec.js` asserts Supabase `paddock_observations` row lands with matching `source_id` after a real UI edit, per CLAUDE.md §"E2E Testing — Verify Supabase, Not Just UI". Full unit suite 1128 → 1135 (111 files). **No schema change** — `paddock_observations` columns already exist (OI-0112 shipped). **No CP-55/CP-56 impact** — this is a seventh writer on the same existing columns. Base-doc reconciliation: V2_UX_FLOWS.md §12 + §17.15 will name pre-graze + post-graze as rendered components of the paddock-window edit dialog. **Closes OI-0118 and GH-28.** | src/features/events/edit-paddock-window.js, tests/unit/edit-paddock-window-observations.test.js, tests/e2e/edit-paddock-window-observations.spec.js, github/issues/GH-28_edit-paddock-window-observation-cards.md |
| 2026-04-18 | refactor(events): derive event start datetime from earliest child window — drop `events.date_in` / `events.time_in` columns (OI-0117, GH-27). **Root cause closed:** two stored columns for one derivable fact = silent drift. `events.date_in` / `events.time_in` duplicated the earliest `event_paddock_windows.date_opened` / `event_group_windows.date_joined`; any code path that updated one but not both produced drift. OI-0115 was exactly that bite (phantom `change` on a teardown-replaced input wrote `events.date_in` while the child windows stayed correct). This OI eliminates the duplicate state. **Migration 028** applied + verified against Supabase: `ALTER TABLE events DROP COLUMN date_in, DROP COLUMN time_in` with pre-check that logs any drift victims to `app_logs` (source='migration.028') and a fail-safe `DO` block warning on events with no child windows. One live drift victim captured from Tim's operation: event `da54838f-c79d-4749-a74f-601c7139599f`. `schema_version` 27 → 28. **New module** `src/features/events/event-start.js` (162 lines) exports: `getEventStart(eventId)` (returns `{ date, time, sourceWindowId, sourceWindowType } | null`), `getEventStartDate(eventId)` (convenience date-only wrapper), `getEventStartFloorExcluding(eventId, excludeWindowId, excludeType)` (edit-dialog guard that excludes the window being edited so the farmer can legitimately move that window's opening earlier), `setEventStart(eventId, newDate, newTime, opts)` (write-through path with three rules: sort key `(date, time, createdAt)` with null time sorting BEFORE explicit time; reject-on-narrow blocks a move-later that would orphan a sibling window; tied-earliest on move-later updates ALL tied windows together via an async `opts.confirm(names)` callback — cancel → no-op). **Entity** (`src/entities/event.js`): `dateIn` / `timeIn` removed from FIELDS / validate / create / toSupabaseShape / fromSupabaseShape. **Detail.js hero line:** the date + time inputs now write through `setEventStart()` instead of `update('events', ...)`. All three OI-0115 teardown guards preserved on the new handlers (isConnected, render-time snapshot identity, current store identity) with the store identity check now against the derived value. A blockedBy response from setEventStart triggers `window.alert` naming the blocking window; cancelled returns reset the input. **Consumer sweep** (~70+ sites rewired across `src/`): dashboard (14 sites — group-window day spans, pasture-percent event filter, DMI-8 chart source-event decoration, hero "In [date]" label), reports (NPK by location + AUDS by paddock), events log + mobile events banner, rotation calendar grid (event block positions + days), retro-place (`computeCandidateEvents` signature gains `startByEventId` Map — kept pure for testing), field-mode events list, feed delivery sheet day count, events index (sort + day count + header display + saveEvent creator), move-wizard (new event creator + Step 2 event picker), edit-paddock-window + edit-group-window (guard uses `getEventStartFloorExcluding`), edit-feed-check + feed-entry-inline-form (guard uses `getEventStartDate`), v1-migration (drop date_in/time_in from events row; keep on paddock window), DMI-8 calc (caller decorates event object with derived dateIn). Store.js toast label no longer references `event.dateIn`. **BACKUP_MIGRATIONS[27]** added: for pre-v28 backups, logs drift between stored `date_in` and MIN(child opening), then `delete evt.date_in` / `delete evt.time_in` on events rows and bumps `schema_version` to 28. `CURRENT_SCHEMA_VERSION` 27 → 28. **CLAUDE.md:** new Architecture Audit rule #7 with two grep contracts (no `events\.date_in` / `events\.time_in` references in `src/`; no `.dateIn` / `.timeIn` reads outside a short allow-list). New Known Traps entry: "Two stored columns for one derivable fact = silent drift — if a value is derivable from child rows at read time, derive it on read." **Tests:** new `tests/unit/event-start.test.js` (16 cases: derivation across paddock + group, null-time sort, floor excluding, move-earlier, move-later no-conflict, move-later rejected with blockedBy, tied-earliest confirm accept/cancel). New `tests/e2e/event-start-write-through.spec.js` asserts hero-date edit updates the earliest child paddock window's `date_opened` in Supabase + asserts the dropped columns no longer exist. Updated: entity test (FIELDS length 11 → 9), backup-roundtrip (chain 13 → 14 entries, ends at 27), backup-import (CURRENT 27 → 28), retro-place (new `startByEventId` Map param), submove-does-not-mutate-event-date-in (asserts derived start stable + anchor paddock window's `dateOpened` stable + writes flow through to child window on legitimate edit; `window.confirm` stubbed true), detail-time-in-editor (asserts child-window `timeOpened` not `events.time_in`; seed uses `timeOpened` + matching `timeJoined`). Full suite 1128/1128 pass. **CP-55 export** naturally omits the dropped columns since `select('*')` reads live Supabase schema. **V2_SCHEMA_DESIGN.md, V2_APP_ARCHITECTURE.md, V2_UX_FLOWS.md** updates deferred to sprint reconciliation per base-doc policy. **Closes OI-0117 and GH-27.** | supabase/migrations/028_drop_event_datetime_columns.sql, src/features/events/event-start.js, src/features/events/detail.js, src/features/events/index.js, src/features/events/move-wizard.js, src/features/events/edit-paddock-window.js, src/features/events/edit-group-window.js, src/features/events/edit-feed-check.js, src/features/events/feed-entry-inline-form.js, src/features/events/retro-place.js, src/features/events/list-view/events-log.js, src/features/events/mobile-events-screen.js, src/features/events/rotation-calendar/calendar-grid.js, src/features/dashboard/index.js, src/features/reports/index.js, src/features/field-mode/index.js, src/features/feed/delivery.js, src/entities/event.js, src/calcs/feed-forage.js, src/data/store.js, src/data/v1-migration.js, src/data/backup-migrations.js, src/data/backup-import.js, CLAUDE.md, OPEN_ITEMS.md, tests/unit/event-start.test.js, tests/unit/entities/event.test.js, tests/unit/backup-roundtrip.test.js, tests/unit/backup-import.test.js, tests/unit/retro-place.test.js, tests/unit/submove-does-not-mutate-event-date-in.test.js, tests/unit/detail-time-in-editor.test.js, tests/e2e/event-start-write-through.spec.js, github/issues/GH-27_derived-event-start-datetime.md |
| 2026-04-18 | feat(event-detail): editable time_in input in hero line (OI-0116, GH-26). Adds a `<input type="time">` immediately after the existing `dateInInput` on the Event Detail hero line (`src/features/events/detail.js:300-349`), closing a usability gap where editing an event's start time required drilling into the Paddock Windows list and editing `timeOpened` on the earliest window — a two-hop detour farmers didn't discover without being told. Layout is `In [date-input] [time-input] · Out [date] · $[cost]`. **Same three OI-0115 teardown guards** applied verbatim to the new change handler: `(1) if (!timeInInput.isConnected) return;` (phantom-change-after-teardown), `(2) if ((newTime ?? '') === renderedTimeIn) return;` (no-op phantom with render-time snapshot), `(3) if ((newTime ?? null) === (evt?.timeIn ?? null)) return;` (no-op against current store). Empty-string input normalizes to `null` on store (per spec acceptance #5 — don't store `""`). Write is direct to `events.time_in` via `update('events', ctx.eventId, { timeIn: newTime }, EventEntity.validate, EventEntity.toSupabaseShape, 'events')` — all six store-call params verified. This is the interim implementation before OI-0117 makes both date and time inputs on this line write-through to the earliest child window. New testid `detail-time-in`. **Tests**: `tests/unit/detail-time-in-editor.test.js` (7 cases): initial render reads `event.timeIn`; null → empty input; real change persists; empty-string clear → null normalization; Guard 1 (detached input phantom change is no-op); Guard 2 (render-time snapshot identity phantom is no-op); legitimate edit + clear sequence still writes through the subscription-driven re-render (re-queries the fresh input). `tests/e2e/detail-time-in-editor.spec.js` — Supabase row assertion per CLAUDE.md §"E2E Testing — Verify Supabase, Not Just UI": after UI edit, `events.time_in` column matches byte-for-byte. Suite 1105 → 1112. No schema change (`events.time_in` is `text NULL`, already on the row). No CP-55/CP-56 impact. Base-doc reconciliation: `V2_UX_FLOWS.md §17.15` gets the "In [date] [time]" hero-line update at end-of-sprint. **Closes OI-0116 and GH-26.** | src/features/events/detail.js, tests/unit/detail-time-in-editor.test.js, tests/e2e/detail-time-in-editor.spec.js, github/issues/GH-26_event-detail-time-in-editor.md |
| 2026-04-18 | fix(detail): stop phantom change on dateInInput from resetting event.date_in (OI-0115, GH-25). **Root cause:** `src/features/events/detail.js:307-325` `dateInInput` change handler had no guards. When a sub-move Save fires `add('eventPaddockWindows', ...)`, `notify('eventPaddockWindows')` triggers Event Detail's subscription at `detail.js:135`, which calls `renderSummary(ctx)` — that clears and rebuilds the dateInInput. In real browsers (iOS Safari especially, with native date-picker implicit-focus semantics during layout shifts), the teardown of the old input can fire a phantom `change` event with whatever value the input holds at that moment — which matches today's date if the browser's picker had defaulted to it. Pre-fix the handler would write whatever value it got to `event.date_in`. Confirmed via grep that `detail.js:323` is the ONLY `update('events', {dateIn: ...})` caller in `src/`; an instrumented jsdom `Store.update` spy showed 0 events-entity writes during the submove save click (meaning the mutation doesn't flow through any other code path). Tim's live-browser repro had to route through this handler. jsdom doesn't model iOS Safari's native-picker teardown cascade, so the regression test dispatches a synthetic `change` event on the live input to exercise the same contract the browser was triggering. **Fix:** three guards on the dateInInput change handler — (1) `if (!dateInInput.isConnected) return;` skips writes after the element was torn down; (2) `if (newDate === renderedDateIn) return;` rejects no-op phantom events that fire with the render-time snapshot value; (3) `if (newDate === evt?.dateIn) return;` rejects writes when the store already matches. Same `isConnected` guard applied as belt-and-braces to the adjacent `renderNotes` onBlur handler (`detail.js:1189`). Grep contract verified: `grep -rn "update('events'" src/features/events/submove.js` returns zero matches (submove is pure INSERT). **CLAUDE.md** gains a new Architecture Audit rule #6 naming the submove grep contract + the change-handler guard pattern, and a Known Traps entry "Phantom change/blur events on teardown-replaced inputs" with the iOS Safari native-picker scenario + the two required guards. **Tests**: new `tests/unit/submove-does-not-mutate-event-date-in.test.js` (6 cases): (a) Tim's exact repro without Event Detail open, (b) same with Detail open (suspect-1 scenario), (c) synthetic phantom-change on the live dateInInput — the one that fails pre-fix, models iOS Safari's teardown cascade, (d) disconnected-input change → Guard 1, (e) genuine user edit still works (fix does not break the legitimate path), (f) sub-move Close Save adjacent-flow smoke. New `tests/e2e/submove-preserves-event-date-in.spec.js` queries Supabase `events.date_in` byte-for-byte after a real sub-move Save per CLAUDE.md §"E2E Testing — Verify Supabase, Not Just UI". Suite 1099 → 1105. **Recovery instruction for Tim** (already documented in OI-0115): any event whose `date_in` was corrupted by this bug can be corrected by opening Event Detail → tapping the "In" date input → picking the correct original start date — the new guards will allow the legitimate edit through. No schema change; no CP-55/CP-56 impact. **Closes OI-0115 and GH-25.** | src/features/events/detail.js, CLAUDE.md, tests/unit/submove-does-not-mutate-event-date-in.test.js, tests/e2e/submove-preserves-event-date-in.spec.js, github/issues/GH-25_submove-resets-event-date-in.md |
| 2026-04-18 | fix(observations): shared CSS polish + inline unit suffix + amber Required pill (OI-0114 NC-2–NC-7, GH-24, commit `a9ffbd4`). Six visual-conformance fixes in `_shared.js` + `pre-graze-card.js` + `main.css`, propagating to all non-survey observation surfaces via the shared renderers; survey card inherits NC-2–NC-5 automatically. **NC-2** top row switches from equal-width grid to flex-end flex (new `.obs-top-row` / `.obs-field` / `.obs-field-rings` CSS) — Height · Cover · Residual Bale Rings inputs share a baseline regardless of label line count. Inputs at 88px (72px rings). Helper note now sits as a normal-flow sibling below the row. **NC-3** `.obs-compact-label` CSS added (13px / 500 / `var(--text2)`, flex-column with `justify-content: flex-end` so the label's bottom edge aligns with the input). Bale Rings sub-label converted from inline styles to `.label-aux` (10px / 400 / `var(--text3)`). **NC-4** `.obs-compact-input` CSS with cross-browser spinner reset: `-webkit-appearance: none`, `-moz-appearance: textfield`, explicit `::-webkit-outer/inner-spin-button` selectors. Focus state uses teal accent with 3px soft box-shadow. No native spinner arrows on Height, Cover, Bale Rings, Recovery Min/Max, or Residual Height. **NC-5** new `withSuffix(input, text)` helper in `_shared.js` wraps an input in `.input-suffix` with an absolutely-positioned `.input-suffix-label` span. Applied to Forage Height, Forage Cover, Residual Height. Labels strip `(in)` / `(%)` — become pure field names. Input padding-right bumps to 28px so typed values don't collide with the floating suffix. **NC-6** `.obs-required` flips red → amber via `var(--color-amber-light)` / `var(--color-amber-dark)` (both tokens already exist; no token additions). Required is a soft CTA, not a validation error. Optional stays green. **NC-7** dead `.paddock-card` classname dropped from `pre-graze-card.js` — no CSS rule referenced it anywhere. `post-graze-card.js` and `survey-card.js` already had clean classnames. Grep for `paddock-card` in `src/` returns 0 hits. **Tests** add 6 cases to `tests/unit/observation-cards.test.js` (top-row structure, input-suffix wrapping, label purity asserting no `(` or `%`, `.label-aux` sub-label, amber Required pill class, no `paddock-card` classname). Suite 1093 → 1099. `vite build` clean. CP-55/CP-56 impact: none. **Closes OI-0114 NC-2 through NC-7.** | src/features/observations/_shared.js, src/features/observations/pre-graze-card.js, src/styles/main.css, tests/unit/observation-cards.test.js, github/issues/GH-24_observation-boxes-polish.md |
| 2026-04-18 | fix(submove-open): reactive BRC auto-fill via setPaddockAcres late-bind (OI-0114 NC-1, GH-24, commit `095d76e`). Sub-move Open renders the pre-graze card before the farmer picks a location. Pre-fix, `paddockAcres: null` made `brcAvailable` compute to `false` once at render time, the bale-ring input listener was never attached, and typing a ring count on this surface did nothing. Move-wizard and event-detail pre-graze worked because they already knew the destination paddock; sub-move Open was the outlier. **Fix**: `renderForageStateRow` in `_shared.js` now carries mutable state — the ring-count listener is always attached and no-ops when BRC isn't available. `setPaddockAcres(newAcres)` on the returned object updates acres, refreshes helper-note text + preview-chip styling, and if a ring count is already in the input, runs BRC-1 immediately so the cover field auto-fills without another keystroke. `renderPreGrazeCard` and `renderSurveyCard` re-export `setPaddockAcres` on their returned objects (post-graze and the three non-BRC surfaces don't need it). `renderLocationPicker` in `src/features/events/index.js` gains optional `opts.onSelect(loc)` callback, fired from the existing item click handler right after `selection.locationId` is set — purely additive; seven other call sites (move wizard Step 2 + new-event dialog) stay unchanged. `submove.js` renders the pre-graze card first, then passes `onSelect: (loc) => preGraze.setPaddockAcres(convert(loc.areaHectares, 'area', 'toImperial'))` to the picker. **Tests** new `tests/unit/submove-brc-reactive.test.js` (6 cases): inactive helper before pick → silent cover before pick → active-text helper with "12.0 ft · 2.47 ac" after pick → cover populates on subsequent ring-count input → late-bind case where ring count is typed first and cover fills on the pick → preview chip activates with ≈XX% cover text. Suite 1087 → 1093. CP-55/CP-56 impact: none. **Closes OI-0114 NC-1.** | src/features/observations/_shared.js, src/features/observations/pre-graze-card.js, src/features/observations/survey-card.js, src/features/events/index.js, src/features/events/submove.js, tests/unit/submove-brc-reactive.test.js |
| 2026-04-18 | fix(dashboard): locations tab final pass — close OI-0075 bugs 3/5/7 (GH-22). Three fixes in a single dashboard render-path commit; pre-handoff audit confirmed every required calc (FOR-1/2/3, DMI-1/2/4, NPK-1/2) was already registered, so no new calcs — pure wiring + investigation. **Bug 3 root cause:** silent field-name drift — dashboard read `loc.areaHa` but the location entity stores the column as `areaHectares`, so `totalAreaHa` resolved to 0 on every real record and the old gate `availableDmKg > 0 && dailyDmiKg > 0` always failed (FOR-1 skips when area ≤ 0). Fixed at 8 read sites in `src/features/dashboard/index.js` via `loc.areaHectares ?? loc.areaHa` (legacy fallback preserved for any in-flight localStorage state). The capacity line also now reads pre-graze observations from `paddock_observations` (OI-0112 moved writes there) with a fallback match on the paddock window id. When any input is legitimately missing (no forage type, no observation, no animals, no area), an italic hint `Capacity: add <missing> to estimate` renders under a new `dashboard-capacity-hint-{event.id}` testid plus any partial info we can show (forage height, ADA) — no more silent disappearance. Partial ADA is still surfaced when acres + AU are known even if forage observation isn't. **Bug 5 (stored feed DM matches v1 `calcConsumedDMI`):** DMI-1 was being called with `qtyKg: fe.quantity` (the batch's UNITS — bales, lbs, tonnes — not kg) and `dmPct: 100` (hardcoded). Off by a factor of `weightPerUnitKg × (dmPct/100)` — two 500 kg bales at 85% DM rendered as 2 kg DM instead of 850. Now converts per-unit quantity × `batch.weightPerUnitKg`, uses `batch.dmPct` from the batch record, and subtracts the latest close-reading feed check's residual across items so residual semantics match v1 `calcConsumedDMI(allFeedEntries, getEffectiveFeedResidual, _lastFeedCheck)`. When no close-reading exists yet, residual = 0 and the two formulas agree. **Bug 7 (top stat cards — Pasture %, NPK/Acre, NPK Value):** extracted exported helper `computePasturePercent(events)` that does mass balance — total DMI-2 kg/day × days across all group windows on all supplied events, minus total DMI-1 stored deliveries — and routes through DMI-4 to get `pasturePct`. Returns `{ pasturePercent, color, subLabel }` with green ≥ 75 / amber 50–74 / red < 50 color grading (v1 parity). Both `computeDesktopMetrics` (line 303 path — was `pasturePercent = null` placeholder) and `computeMobileMetrics` (line 414 path — was hardcoded em-dash) now call the helper. NPK/Acre and NPK Value were already wired on both paths; the areaHectares fix above is what lets them render real values instead of em-dashes. **Tests:** new `tests/unit/locations-tab-bugs-357.test.js` (8 cases): (1) capacity line renders with full inputs (observation + area + animals + forage type), (2) capacity hint renders with "forage height" missing when obs absent, (3) capacity hint renders with "forage type" missing when paddock lacks forage type, (4) stored-feed DM matches expected ~1874 lbs for 2 bales × 500 kg × 85% DM, (5) `computePasturePercent` returns null on empty events, (6) returns null on events with no animals, (7) 100% pasture → green band, (8) 60% pasture → amber band. Full suite 1079 → 1087; `vite build` clean (200ms). CP-55/CP-56 impact: none — visual/wiring only. **Closes OI-0075 fully (bugs 3/5/7) and GH-22.** | src/features/dashboard/index.js, tests/unit/locations-tab-bugs-357.test.js |
| 2026-04-18 | feat(move-wizard): per-group scoped move (OI-0066, GH-23, commit `cf39516`). `openMoveWizard(sourceEvent, operationId, farmId, { scopedGroupWindowId })` — the 4th opts arg scopes the wizard to one `event_group_window` instead of the whole event. When scoped: close-out closes only the one GW; source event's paddock windows stay open; `event.dateOut` stays unset — so long as any other open GW on the event remains. When the scoped move is on the LAST open group, the wizard closes every open PW and stamps `event.dateOut` exactly as the non-scoped path does today. Feed entries on the source event stay put in scoped mode (no close-reading check, no transfer toggles, no residual capture — the remaining groups still need their feed). Card-level "Move all" call sites (6 of 8 total) are unchanged — `openMoveWizard(event, operationId, farmId)` with no opts param continues to run the full-event close. Dashboard wiring at the two per-group Move sites: `buildGroupCard` line 842 uses `activeGW?.id` (existing `dashboard-move-btn-{group.id}` testid unchanged); `buildLocationCard` group row line 1187 uses `gw.id` (new `dashboard-group-move-btn-{gw.id}` testid). Six new invariant tests in `tests/unit/move-wizard-scoped.test.js` drive the public `openMoveWizard` → Step 1 → Step 2 → Save flow with a seeded two-group event (Group A 10 head, Group B 12 head; 22 memberships) and assert: (1) scoped move gives dateLeft only to the scoped GW, (2) source event stays open when another GW remains, (3) source PW stays open likewise, (4) destination event has exactly one GW (the scoped one), (5) non-scoped "Move all" closes every GW + event + PW as before (unchanged regression), (6) scoped move on the last open group closes the event + PW. Existing `move-wizard-mirror` + `move-wizard-feed-transfer` tests pass — no regression. Pre-build audit 2026-04-18 confirmed this was UNTOUCHED (no prior scoping work); full build delivered in this commit. CP-55/CP-56 impact: none (no schema change; `event_group_windows` was already the unit of scoping). Base doc impact: `V2_UX_FLOWS.md` §13 (Move wizard) + §17.7 (Dashboard card per-group row) will gain a scoped-variant note at end-of-sprint reconciliation per UI_SPRINT_SPEC SP-12. **Closes OI-0066 and GH-23.** | src/features/events/move-wizard.js, src/features/dashboard/index.js, tests/unit/move-wizard-scoped.test.js, github/issues/GH-23_per-group-move-scoping.md |
| 2026-04-18 | fix(event-detail): rename primary action button to Save & recalculate (OI-0074, GH-21, commit `202069e`). Pre-build audit found the three-tier v1 layout (green primary + outline cancel row; amber full-width "Close this event & move groups" warning; red-small "Delete event" destructive) was already in place; only the primary button identity had drifted from the spec. Fix: label `"Save & close"` → `"Save & recalculate"`, testid `detail-save-close` → `detail-save-recalc`, handler now invokes `renderAll(ctx)` to re-run every derived view in the sheet (DMI, NPK, summary, DMI chart, pre-graze, post-graze) then `closeEventDetailSheet()`. V2 auto-saves inline edits so there's no unsaved-form state to persist — the button exists to give the farmer explicit confidence pending values are fresh. Cancel button gains a `detail-cancel` testid for symmetric selectors. New i18n key `action.saveAndRecalculate`. **Tests:** `tests/unit/detail-action-buttons.test.js` (4 cases): active event primary row (green flex:2 + outline flex:1), active event amber full-width warning present, active event red-small destructive present, closed event renders only Cancel + Delete (no save-recalc, no amber warning). Suite 1069 → 1073. Scope boundary: undefined CSS classes (`btn-olive`, `btn-danger`, `btn-ghost`) exist elsewhere in `detail.js` but NOT in `renderActions`; respected CLAUDE.md "Scoped Changes Only" — can clean up in a follow-up if field-testing flags them. CP-55/CP-56 impact: none. **Closes OI-0074 and GH-21.** | src/features/events/detail.js, src/i18n/locales/en.json, tests/unit/detail-action-buttons.test.js, github/issues/GH-21_event-detail-action-buttons.md |
| 2026-04-18 | docs(oi): retroactive close OI-0072 + partial-close OI-0075 after pre-testing cleanup audit. **OI-0072** (GH-20, Feed Dialogs v1 parity) — audited against spec and found DONE in earlier commits `1d9c0e4` (feat: feed check sheet v1 parity) and `74c53c9` (feat: deliver feed sheet v1 parity); 11/11 acceptance items present and matching v1 reference HTML (triple-sync stepper/slider/%, consumed banner, tap-to-select batch cards, inline steppers, multi-batch, feed-type grouping of unselected batches, live DMI + cost summary footer, global `.sheet-panel` sizing). No new code shipped; OPEN_ITEMS status flipped `open` → `closed` retroactively with the earlier commit hashes. **OI-0075** (GH-22, Locations Tab 7 display bugs) — audited and found partial: bugs 1/2/4/6 shipped in earlier commit `e124952` (never closed out in OPEN_ITEMS); bugs 3 (green capacity line), 5 (stored-feed DMI value), 7 (top stat cards — Pasture %, NPK/Acre, NPK Value) are wired structurally but render em-dashes pending DMI-8 / OI-0069 data flow. Per the session brief's explicit carve-out, those three stay deferred until DMI-8 ships. Spec files renamed `feed-check-ui-v1-parity.md` → `GH-20_feed-check-ui-v1-parity.md`, `BUG_locations-tab-display-fixes.md` → `GH-22_locations-tab-display-fixes.md`. GH issues #20 + #22 filed and closed with audit notes. | OPEN_ITEMS.md, github/issues/GH-20_feed-check-ui-v1-parity.md, github/issues/GH-22_locations-tab-display-fixes.md |
| 2026-04-18 | feat(observations): big-bang observation-box redesign across 7 surfaces (OI-0112, GH-19, commit `13a3327`). Three new card components — `renderPreGrazeCard`, `renderPostGrazeCard`, `renderSurveyCard` — replace `renderPaddockCard` + `renderPreGrazeFields` + `renderPostGrazeFields` + the hand-rolled survey form in a single commit. Shared `_shared.js` sub-renderers for internal DRY (forage-state compact top row · anchored-label color-graded quality slider 1–100 · single-select chip picker with deselect-on-reclick · recovery-window row · BRC preview chip · notes textarea). Each card exposes the unified `{ container, getValues, validate }` contract so callers don't need bespoke integration. **All 7 surfaces migrated:** (1) move wizard destination pre-graze, (2) move wizard source post-graze, (3) close-event post-graze, (4) sub-move Open pre-graze (fixes OI-0110 — old helper collected only height+cover), (5) sub-move Close post-graze (gains Notes), (6) survey draft entry (individual + bulk — inline form removed), (7) event detail §5 pre-graze + §6 post-graze panels become editable cards, one per open/closed paddock window respectively (absorbs OI-0107; post-graze editability is a new capability). **Write path:** every caller writes to `paddock_observations` with `source: 'event' \| 'survey'` and `type: 'open' \| 'close'`. No writes to `event_observations` — OI-0113 retires that orphan table later. **BRC-1 helper** targets `farmSettings.baleRingResidueDiameterCm` (OI-0111 shipped first — metric-internal); `_shared.js` converts cm → ft inline before invoking the imperial-native `src/calcs/survey-bale-ring.js`. **Files created:** `src/features/observations/_shared.js`, `pre-graze-card.js`, `post-graze-card.js`, `survey-card.js`. **Files deleted:** `src/features/events/observation-fields.js`, `src/features/observations/paddock-card.js`, `tests/unit/paddock-card.test.js`. Grep for `renderPreGrazeFields` / `renderPostGrazeFields` / `renderPaddockCard` returns zero matches in `src/` and `tests/` post-migration (verified pre-commit). **Event-detail scope carve-out:** the DMI-8 chart's observation-feed reads (`detail.js` lines 378, 421) still pull from `event_observations`; migrating the calc pipeline is a separate follow-up tracked on OI-0113. **i18n** keys added under `event.`: `surveyObs`, `residualBaleRings`, `forageCoverCalculator`, `recoveryWindow`, `recoveryWindowDays`, `baleRingCoverPreview`, `baleRingHelperDetail`, `relativeForageQuality`. No hardcoded English in new card files. **Tests:** new `tests/unit/observation-cards.test.js` (26 cases — contract, BRC auto-fill active + inactive, chip toggle + single-select, required vs optional validation, Required pill surfacing, `initialValues` hydration with cm→in conversion, post-graze-only fields + always-optional validate, survey-specific shape with recovery days). New `tests/e2e/observation-cards.spec.js` (2 Supabase-verification scenarios per CLAUDE.md §"E2E Testing — Verify Supabase, Not Just UI" — sub-move Open pre-graze card → `paddock_observations` row with source='event' + all collected fields; individual-mode survey commit → `paddock_observations` row with source='survey', type='open', recovery days populated; both skip cleanly when E2E env vars missing). Suite 1049 → 1067 pass. **Closes OI-0107, OI-0110, OI-0112 and GH-19.** Supersedes OI-0100 visual treatment; lineage preserved via git history. | src/features/events/move-wizard.js, src/features/events/close.js, src/features/events/submove.js, src/features/events/detail.js, src/features/surveys/index.js, src/features/observations/_shared.js, src/features/observations/pre-graze-card.js, src/features/observations/post-graze-card.js, src/features/observations/survey-card.js, src/features/events/observation-fields.js (deleted), src/features/observations/paddock-card.js (deleted), src/i18n/locales/en.json, tests/unit/observation-cards.test.js, tests/unit/paddock-card.test.js (deleted), tests/e2e/observation-cards.spec.js, github/issues/GH-19_observation-boxes-redesign.md |
| 2026-04-18 | feat(dashboard): 3-up quick-action row (Feed Check · Feed · Sub-Move) (OI-0109, GH-18, commit `2db2efc`). Reverses SP-3 / GH-11's two-large-stacked-buttons bottom row in `buildLocationCard()`. New 3-up flex row with equal `flex: 1` width, gap 6px, padding 10px/8px, 13px/600 weight, border-radius 8px — mirrors v1's `.grp-actions` compact small-button style already extracted in GH-4. Sub-Move promoted from the buried teal `+ Add sub-move` text link above SUB-PADDOCKS to a primary quick-access button alongside Feed Check and Feed. Standalone link above SUB-PADDOCKS removed (the new button supersedes it); in-section `+ Add sub-move` link **inside** SUB-PADDOCKS preserved — Tim explicitly asked to keep that one because it's the mid-flow "add another from here" affordance, not a quick-access entry point. Event Detail's Sub-move History `+ Add sub-move` button untouched (different surface). New testid `dashboard-submove-btn-{event.id}`; existing Feed / Feed Check testids preserved. Uses existing `event.subMove` i18n key + `event.feedCheck` / `event.deliverFeed`. `openSubmoveOpenSheet` import already present from earlier sub-move work. **Tests:** `tests/unit/dashboard-3-button-row.test.js` seeds a minimal op/farm/location/event/paddock-window through the store, invokes the exported `buildLocationCard`, and asserts (a) all three testids exist, (b) the three buttons share a single flex row with equal `flex-grow`, (c) Sub-Move click dispatches `openSubmoveOpenSheet` with the event + operationId, (d) the standalone `+ Add sub-move` link is absent when no sub-moves exist. Reverses SP-3 / GH-11 §13–16 bottom-button design; reconciliation into V2_UX_FLOWS.md §17.7 at sprint end per UI_SPRINT_SPEC SP-12 Part A. Suite 1045 → 1049. **Closes OI-0109 and GH-18.** | src/features/dashboard/index.js, tests/unit/dashboard-3-button-row.test.js, github/issues/GH-18_dashboard-card-3-button-bottom-row.md |
| 2026-04-18 | fix(events): rename feed-entry DMI → DM label + guard silent-zero + metric (OI-0108, GH-17, commit `35e6764`). Fixes two bugs in the Event Detail §8 feed-entry row: (1) the label `{N} lbs DMI` was wrong — the formula `quantity × weightPerUnitKg × dmPct/100` produces dry matter DELIVERED (DM), not DMI (per-head-per-day intake); labelling the row "DMI" conflated the two concepts. (2) When a batch was missing `weightPerUnitKg` or `dmPct` (null or zero), the row rendered `0 lbs DMI` — a silent zero indistinguishable from a legitimate zero-quantity entry. Tim saw this during field testing on a real bale delivery that should have shown ~850 lbs DM. **Fix:** label renamed `lbs DMI` → `lbs DM` via new i18n key `event.feedEntryDm: "{n} {unit} DM"`. Em-dash guard: when `weightPerUnitKg > 0` and `dmPct > 0` are not both satisfied, the row renders `— lbs DM` with tooltip from new i18n key `event.feedEntryDmMissing` directing the farmer to edit the batch in Feed. Metric unit support via `operation.unitSystem` + `unitLabel('weight', unitSys)` — toggles `lbs DM` / `kg DM` based on user preference. Extracted `computeFeedEntryDm(quantity, batch, unitSys)` as a pure helper exported for tests (minimal testable seam; the inline block was too entangled with the render loop to exercise directly). **Audit of `openDeliverFeedSheet`** per CLAUDE.md "Fix Root Causes, Not Symptoms" scope rule: the Deliver Feed sheet only READS `batch.weightPerUnitKg` / `batch.dmPct` — it doesn't create batches. Batch creation lives in `src/features/feed/index.js` Add Batch sheet (lines ~609–693) where both fields ARE captured via explicit inputs. So the silent-zero was a display-guard gap, not a capture gap. No scope extension. **Tests:** `tests/unit/detail-feed-entry-dm.test.js` (8 cases covering imperial + metric labels, missing-weight guard, missing-DM guard, zero-weight / zero-DM treated as missing, legitimate zero-quantity, undefined-batch lookup miss). Suite 1037 → 1045. **Closes OI-0108 and GH-17.** | src/features/events/detail.js, src/i18n/locales/en.json, tests/unit/detail-feed-entry-dm.test.js, github/issues/GH-17_event-detail-feed-entry-dm-label.md |
| 2026-04-18 | fix(settings): unit-aware Settings UI + bale-ring column rename to metric storage (OI-0111, GH-16). Closes the silent-corruption bug where `renderFarmSection` rendered stored metric values raw with no unit suffix — imperial users saw `454` / `10` in fields labeled only "AU Reference Weight" / "Default Residual Height", assumed the numbers were wrong for their mental model, and "corrected" them — writing imperial numbers into metric columns. Every downstream DMI / stocking-rate / residual-badge / threshold / NPK-pricing calc reading those fields then ran on nonsense. Enforces the CLAUDE.md Known Trap "Unit confusion: always store metric, display converted" *in the UI*; the trap was documented but the code bypassed it. **renderFarmSection rewrite** uses a 13-field descriptor with `measureType` / `inverted` / `currency` / `perDay` / `displayUnit` / `precision` flags. Render path converts stored metric → display unit (e.g. 454 kg → 1001 lbs for imperial users) and composes unit-suffixed labels dynamically ("AU Reference Weight (lbs)", "N Price ($/lb)", "Manure Rate (lbs/AU/day)"). Save path runs the inverse conversion and stores the full JS float — no rounding or truncation on save, per spec §Precision, so sub-decimal drift from `convert()` sits below the display precision and the farmer never sees it. Round-trip rule enforced: entering X in display unit → save → reopen shows exactly X again. Price fields use the `inverted: true` flag (÷ factor on display, × factor on save) so $1.21/kg stored renders as ~$0.5489/lb. Bale-ring field uses `displayUnit: 'ft'` (imperial shows ft, not inches; metric shows cm). `__settingsUnitInternals` export added so the round-trip test can exercise the conversion helpers without pulling in the full DOM. **Migration 027** (`supabase/migrations/027_bale_ring_diameter_to_cm.sql`) renames `farm_settings.bale_ring_residue_diameter_ft` → `_cm`, multiplies existing values × 30.48, sets default 365.76 (= 12 ft), drops the old column; applied and verified via MCP (`information_schema.columns` shows only `_cm`; `schema_version` 26 → 27). Per CLAUDE.md Migration Execution Rule — write + run + verify in the same session. **Entity rename** propagated through `src/entities/farm-setting.js` (FIELDS key, create default 365.76, to/fromSupabaseShape). **BRC-1 calc stays imperial-native** (`src/calcs/survey-bale-ring.js` untouched): paddock-card.js and the bulk survey in locations/index.js convert `cm → ft` inline before invoking, via `convert(cm, 'length', 'toImperial') / 12`. Rationale: the BRC-1 formula is imperial-native (sq ft of ring footprint / sq ft of paddock); touching the calc is out of scope. **BACKUP_MIGRATIONS[26]** renames imported backup rows `_ft` × 30.48 → `_cm`, deletes the old key, stamps `schema_version = 27`. **CURRENT_SCHEMA_VERSION** bumped 26 → 27 in backup-import.js. **i18n** keys added: `unit.ft`, `unit.pct`, `unit.days`, `unit.score`, `settings.baleRingDiameter`; the unit-bearing labels in en.json stripped of their hardcoded "(per kg)" / "(kg/AU/day)" suffixes — the suffix is now composed dynamically. **Tests:** new `tests/unit/settings-unit-roundtrip.test.js` (34 cases covering every unit-bearing field — imperial + metric round-trip, full-float storage, null handling, imperial display of metric defaults). Updated `paddock-card.test.js` fixture (`baleRingResidueDiameterFt: 12` → `baleRingResidueDiameterCm: 365.76`). Updated `numeric-coercion-tier1.test.js` fixture for the renamed column. Updated `backup-roundtrip.test.js` (new migration-26 rename case + chain-length assertion 12 → 13). Updated `backup-import.test.js` (chain target 26 → 27). Full suite 1037/1037 pass. **Docs:** V2_SCHEMA_DESIGN.md §1.3 gains `bale_ring_residue_diameter_cm` column row + CREATE TABLE entry + Change Log row. V2_MIGRATION_PLAN.md §5.3a `farm_settings` entry annotated with the column rename. UI_SPRINT_SPEC.md SP-9 schema row + reconciliation checklist updated. OPEN_ITEMS OI-0107 + OI-0110 acceptance lines renamed (`baleRingResidueDiameterFt` → `Cm`). GH-12 historical spec updated. observation-boxes-redesign.md (live spec for OI-0112) updated — dependency note now reflects that OI-0111 shipped and the new field name is in place. Spec file renamed `BUG_` → `GH-16_`. GH issue #16 closed with commit hash. Param-count grep verified on the `update('farmSettings', ...)` call (6-param — correct per CLAUDE.md rule 7). **Closes OI-0111 and GH-16.** | src/features/settings/index.js, src/entities/farm-setting.js, src/features/observations/paddock-card.js, src/features/locations/index.js, src/calcs/survey-bale-ring.js, src/data/backup-migrations.js, src/data/backup-import.js, src/i18n/locales/en.json, supabase/migrations/027_bale_ring_diameter_to_cm.sql, tests/unit/settings-unit-roundtrip.test.js, tests/unit/paddock-card.test.js, tests/unit/numeric-coercion-tier1.test.js, tests/unit/backup-roundtrip.test.js, tests/unit/backup-import.test.js, V2_SCHEMA_DESIGN.md, V2_MIGRATION_PLAN.md, UI_SPRINT_SPEC.md, OPEN_ITEMS.md, github/issues/GH-16_settings-ui-unit-conversion.md, github/issues/GH-12_survey-sheet-v1-parity.md, github/issues/observation-boxes-redesign.md |
| 2026-04-18 | fix(entities): Tier 3 numeric-coercion sweep — 10 entities; close OI-0106 structural sweep (OI-0106, GH-15, commit `caada42`). Closes the PostgREST stringified-numeric sweep opened by d55ba9b. Coerces 75 numeric/integer fields across 10 entities on the nutrient/cost math path: `soil-test` (18 chemistry cols), `manure-batch` (15 nutrient cols), `manure-batch-transaction` (volumeKg), `input-product` (13 *Pct + costPerUnit), `amendment` (totalQty, costOverride), `amendment-location` (14 cols), `npk-price-history` (3 prices), `spreader` (capacityKg), `animal-treatment` (doseAmount), `farm` (lat/lng/areaHectares). **CP-56 audit**: `backup-import.js:402` already calls `pullAllRemote()` which routes every reinserted row through `fromSupabaseShape` — every entity coerces, so post-import in-memory state is guaranteed numeric. No import-side defensive wraps needed; entity-level fix is the structural solution. **CLAUDE.md Known Traps** updated: new entry documents the trap, four harm classes (math concat, .toFixed crash, strict typeof validate rejection, lex threshold comparison), reference pattern (`event-observation.js`), and round-trip-test requirement per new numeric column. **Tests**: 10 new Tier 3 round-trip tests + 3 new pre-OI-0106 backup-roundtrip cases (stringified-numeric row → fromSupabaseShape → numbers in memory, including farm-setting threshold lex-vs-numeric regression guard). Suite 989 → 1002. **Closes OI-0106 and GH-15.** | src/entities/soil-test.js, src/entities/manure-batch.js, src/entities/manure-batch-transaction.js, src/entities/input-product.js, src/entities/amendment.js, src/entities/amendment-location.js, src/entities/npk-price-history.js, src/entities/spreader.js, src/entities/animal-treatment.js, src/entities/farm.js, tests/unit/numeric-coercion-tier3.test.js, tests/unit/backup-roundtrip.test.js, CLAUDE.md |
| 2026-04-18 | fix(entities): Tier 2 numeric-coercion sweep — 9 entities (OI-0106, GH-15, commit `33c6add`). Tier 2 entities manifest the stringified-numeric bug in specific reports/flows rather than the dashboard hot path. Coerces ~45 numeric/integer fields across: `event-paddock-window` (areaPct — strip-graze proportional band), `paddock-observation` (7 cols), `survey-draft-entry` (6 cols), `forage-type` (7 cols — DMI yields + NPK rates), `feed-type` (6 cols), `batch-nutritional-profile` (12 lab-result cols), `harvest-event-field` (4 cols — strict typeof validate), `animal-bcs-score` (score), `batch-adjustment` (3 qty cols — strict typeof validate). **Tests**: 9 new Tier 2 round-trip tests. `harvest-event-field` + `batch-adjustment` tests also assert `validate()` now passes (pre-coercion stringified values silently rejected). Suite 980 → 989. | src/entities/event-paddock-window.js, src/entities/paddock-observation.js, src/entities/survey-draft-entry.js, src/entities/forage-type.js, src/entities/feed-type.js, src/entities/batch-nutritional-profile.js, src/entities/harvest-event-field.js, src/entities/animal-bcs-score.js, src/entities/batch-adjustment.js, tests/unit/numeric-coercion-tier2.test.js |
| 2026-04-18 | fix(entities): Tier 1 numeric-coercion sweep — batch/group-window/weight/farm-setting/location/animal-class (OI-0106, GH-15, commit `43d46b2`). Structural follow-up to d55ba9b (OI-0103 hotfix): PostgREST returns PostgreSQL numeric/decimal columns as JavaScript strings (arbitrary-precision safety). The hotfix coerced three surfaces; this sweep extends the `Number(v)` (null-safe) coercion pattern in `fromSupabaseShape` to the dashboard/feed/DMI hot-path entities before Tim's next field-testing pass. 33 fields across 6 entities: `batch` (quantity, remaining, weightPerUnitKg, dmPct, costPerUnit), `event-group-window` (headCount, avgWeightKg), `animal-weight-record` (weightKg — rolls up into group-window avgWeightKg), `farm-setting` (16 threshold/default/price cols — threshold lex-comparisons are the worst quiet bug here), `location` (areaHectares, capturePercent — divide-by-string NaN cascade), `animal-class` (defaultWeightKg + 5 DMI/NPK rates + weaningAgeDays). **Harm classes removed:** (1) silent math concat ("0"+"1"+"2"="012"), (2) `.toFixed()` TypeError, (3) strict typeof-number validator silent-reject in `add()`/`update()`, (4) lex threshold comparisons ("100" < "60" lex-true, numeric-false), (5) divide-by-string NaN cascade. **Tests**: `tests/unit/numeric-coercion-tier1.test.js` (8 round-trip tests, plus a batch.validate regression guard and a farm-setting threshold lex-comparison regression guard), `tests/unit/merge-remote-numeric-coercion.test.js` (3 integration tests driving the full pull pipeline — stubbed sync adapter → pullAllRemote → fromSupabaseShape → mergeRemote → getAll — asserting downstream math sum/toFixed/divide all work on pulled batch rows; this is the exact code path Tim got burned by). Also renames `github/issues/BUG_numeric-coercion-sweep.md` → `github/issues/GH-15_numeric-coercion-sweep.md` per workflow rule. Suite 969 → 980. | src/entities/batch.js, src/entities/event-group-window.js, src/entities/animal-weight-record.js, src/entities/farm-setting.js, src/entities/location.js, src/entities/animal-class.js, tests/unit/numeric-coercion-tier1.test.js, tests/unit/merge-remote-numeric-coercion.test.js, github/issues/GH-15_numeric-coercion-sweep.md |
| 2026-04-18 | fix(animals): Edit Animal silent-drop inputs — complete fix (OI-0099, GH-14). All four previously-silent-drop inputs on the Edit Animal dialog — `damId`, `sireTag`, `weaned`, `confirmedBred` — now round-trip through `saveAnimal` → `update()` → Supabase. Shipped as one bundle per Tim's "no gaps" direction (no split Class A / Class B ship). **Migration 026** (`animals.confirmed_bred BOOLEAN NOT NULL DEFAULT false`) applied and verified via MCP: `information_schema` shows the column exists with `false` default; `UPDATE operations SET schema_version = 26` guarded with `WHERE schema_version < 26`. `BACKUP_MIGRATIONS[25]` no-op chain entry added; `CURRENT_SCHEMA_VERSION` bumped 25→26. **Entity update** (`src/entities/animal.js`): `confirmedBred` added to FIELDS / create (defaults false) / toSupabaseShape / fromSupabaseShape (missing-column fallback → false, so pre-migration-026 backup rows resolve correctly on import). **Class A wiring** — `saveAnimal` now reads `damId: inputs.damId?.value || null` and `weaned: inputs.weaned?.checked ?? null` into the `data` object. `weanedDate` behavior (locked by Tim): checkbox-off → `weanedDate = null` + date field disabled; off→on flip → `weanedDate` defaults to today + date field enabled and editable (farmer can back-date); on→off flip → clears. New editable date input adjacent to the Weaned checkbox. **Class B sire picker** (replaces the freeform `sireTag <input type="text">` which had no matching entity field — classic "UI field without Supabase column" v1 trap). Three modes — **Animal in herd** (select from `operationId` males; row format `{tagNum} — {name}`; writes `sireAnimalId`), **AI bull from list** (select from `ai_bulls` where `archived = false`; row format `{name} · {tag}  [breed]`; writes `sireAiBullId`), **None** (clears both FKs). Mutual exclusivity enforced: switching modes clears the other FK; clicking "None" clears both. **Inline "+ Add AI bull"** sub-dialog (modal overlay) captures name (required), tag (optional), breed (optional); creates an `ai_bulls` record via standard 5-param `add()`; immediately sets `sireAiBullId` on the current animal and returns the farmer to Edit Animal with the new bull selected — the escape hatch for pre-app / historical / external / neighbor bulls. Semantic note added in code comments: `ai_bulls` retains its v1-era name but will hold non-AI bulls in practice; rename/split is a future OI. **Class B confirmedBred** — `saveAnimal` reads `inputs.confirmedBred?.checked ?? false`. Reverses the original A29 "derive from breeding records" design (never implemented, blocked the UI). **Docs:** V2_SCHEMA_DESIGN.md §3.2 updated — new `confirmed_bred` column row, A29 rewritten to document the reversal, A28 annotated with picker + ai_bulls v1-era-name note, CREATE TABLE DDL amended, Change Log row added. **CP-55/CP-56:** Export picks `confirmed_bred` automatically (`select('*')`); import relies on column default for missing-column old backups (chain entry is a no-op). No animals shape whitelist to update. v1-migration.js preserves v1 `confirmedBred` on seed. **Tests** (969 total pass): 4 new unit cases in `tests/unit/entities/animal.test.js` (confirmedBred default, round-trip, missing-column fallback, sire FK mutual-exclusivity round-trip); new `tests/unit/edit-animal-save.test.js` (5 Edit Animal dialog integration cases — four-input persistence, sire mode switch mutual exclusivity, None mode clears, weaned-off clears date, inline Add AI bull creates row + selects it). **E2E** `tests/e2e/edit-animal-silent-drop-inputs.spec.js` asserts Supabase rows directly (not just localStorage) per CLAUDE.md rule — the rule OI-0050 and OI-0053 exist specifically because it used to be missed. Param-count grep verified on every touched `add()` / `update()` call. **Closes OI-0099 and GH-14.** | supabase/migrations/026_add_confirmed_bred.sql, src/entities/animal.js, src/data/backup-migrations.js, src/data/backup-import.js, src/data/v1-migration.js, src/features/animals/index.js, V2_SCHEMA_DESIGN.md, OPEN_ITEMS.md, github/issues/GH-14_edit-animal-silent-drop-inputs.md, tests/unit/entities/animal.test.js, tests/unit/edit-animal-save.test.js, tests/unit/backup-roundtrip.test.js, tests/unit/backup-import.test.js, tests/e2e/edit-animal-silent-drop-inputs.spec.js |
| 2026-04-18 | feat(move-wizard): 2-way feed transfer radio + residual-capture placeholder for OI-0092 (OI-0104, commit `1a22923`). Step 3 feed-transfer UI replaces the per-line checkbox with a per-line 2-way radio ("Move to new paddock" default / "Leave as residual" captures remaining to fertility ledger). Feed Transfer section relocated inside Close section (between post-graze observation and the Open destination) per the brief reorder. `executeMoveWizard` per-line branch: **Move** stamps `remainingQuantity = 0` + writes destination `event_feed_entries`; **Residual** stamps real `remainingQuantity = group.total` + NO destination entry + emits `logger.info('residual-capture', ...)` for the future OI-0092 ledger pipeline to consume. Mixed per-line handled via `toggleByKey` map. **Sequencing with OI-0092:** OI-0092 is still stub-only (no schema). This OI delivers the UI + close-reading capture; a follow-up PR flips the residual arm from the logger-only placeholder to the real ledger write once OI-0092 locks its schema (column on `event_feed_entries.residual_qty` OR a new `event_feed_residual_deposits` table — TBD). The OI-0091 placeholder comment at `move-wizard.js:480-481` ("residual feed NPK deposit is a v1-parity gap tracked separately; remainingQuantity:0 is a deliberate placeholder here") is replaced by the real per-line branch. New i18n keys `event.feedTransferMoveLabel`, `event.feedTransferResidualLabel`, `event.feedTransferResidualCaption`. New `tests/unit/move-wizard-feed-transfer.test.js` (5 cases pinning the per-line branch contract including the fall-back for undefined toggles) + `tests/e2e/move-wizard-feed-residual.spec.js` (Supabase round-trip: assert `event_feed_check_items.remaining_quantity > 0` after selecting Residual, per CLAUDE.md E2E rule). 959 tests pass. **Closes OI-0104.** | src/features/events/move-wizard.js, src/i18n/locales/en.json, tests/unit/move-wizard-feed-transfer.test.js, tests/e2e/move-wizard-feed-residual.spec.js |
| 2026-04-18 | feat(observations): shared paddock-card component; embed in move wizard pre-graze (OI-0100, commit `8ff3572`). GH-12 (Survey sheet v1 parity) shipped 2026-04-17 but its paddock card was inline in `src/features/surveys/index.js` rather than a standalone module — the OI-0100 brief's "build-shared" branch applied. New `src/features/observations/paddock-card.js` exports `renderPaddockCard({ saveTo, farmSettings, paddockAcres, initialValues })` returning the same `{ container, getValues, validate }` contract as `renderPreGrazeFields`, so the existing move-wizard `createObservation` call consumes it unchanged. Fields per GH-12 spec: forage height (unit-converted on display), forage quality slider (1-100), forage cover % with the BRC-1 bale-ring auto-fill helper (active when `farmSettings.baleRingResidueDiameterFt` AND a caller-supplied `paddockAcres` are present), forage condition enum (poor/fair/good/excellent — matching `paddock_observations` + `survey_draft_entries`; `event_observations` uses a divergent set which is preexisting doc-drift tracked separately), bale-ring residue count, recovery min/max days, notes. Move wizard Step 3 pre-graze (lines 386-400) now renders the shared card instead of the minimal `renderPreGrazeFields`; `paddockAcres` is derived from destination `location.areaHa`. Survey sheet NOT migrated this commit — a follow-up PR can adopt the shared module. New i18n keys: `event.conditionPoor`/Fair/Good/Excellent, `event.baleRingResidueCount`, `event.baleRingHelperActive`/Inactive, `event.notes`. New `tests/unit/paddock-card.test.js` (8 cases covering contract, `getValues()` shape, null-on-empty, BRC-1 auto-fill active + inactive, validate required-mode, initialValues pre-fill). 954 tests pass. **No schema change, no CP-55/CP-56 impact.** **Closes OI-0100.** | src/features/observations/paddock-card.js, src/features/events/move-wizard.js, src/i18n/locales/en.json, tests/unit/paddock-card.test.js |
| 2026-04-18 | feat(location-picker): anchored search bar (OI-0105, commit `8c71cb8`). Sticky search input (`position: sticky; top: 0`) at the top of `renderLocationPicker` in `src/features/events/index.js`. Filters the three sections (Ready / In Use / Confinement) by case-insensitive substring on `loc.name`. Sections with zero matches collapse (header skipped). × clear button restores the full list. Empty-state message renders when no matches. Query persists across internal re-renders (selection click re-renders the picker) via `container.dataset.locationSearchQuery`; focus + caret position preserved across re-render so typing is uninterrupted. Applies to both callers (move-wizard Step 2, new-event dialog) since they share the picker. New i18n keys: `event.locationPicker.search` ("Search paddocks"), `event.locationPicker.noMatches`. New `tests/unit/location-picker-search.test.js` (5 cases). 946 tests pass. **Closes OI-0105.** | src/features/events/index.js, src/i18n/locales/en.json, tests/unit/location-picker-search.test.js |
| 2026-04-18 | feat(move-wizard): one-way mirror dateIn/timeIn from dateOut/timeOut (OI-0101, commit `373f276`). Step 3 now auto-populates open-side date/time from close-side date/time until the farmer types into the open field. First keystroke on dateIn/timeIn flips a `dateInTouched` / `timeInTouched` state flag and further close-side edits no longer propagate. Rule is strictly one-way (close → open); editing open-side never writes back to close-side. Cuts double-entry in the common case (move today → destination opens today) without locking in the less-common case where open-side date differs (e.g., one-day gap between paddocks). New `tests/unit/move-wizard-mirror.test.js` (5 cases pinning the state-machine contract: cascade works, cascade stops after touch, dateIn+timeIn are independent, no reverse propagation). 941 tests pass. **Closes OI-0101.** | src/features/events/move-wizard.js, tests/unit/move-wizard-mirror.test.js |
| 2026-04-18 | fix(feed-check): Save button now persists (OI-0103, commit `38925be`). `src/features/feed/check.js:262` passed `checkDate:` to `FeedCheckEntity.create` but the entity's FIELDS key is `date:` (see `src/entities/event-feed-check.js:7`). Silent drop on `create()` — the row was persisted with `date=null` → `validate()` rejected the write → Save button appeared to no-op with no error surfaced to the user. Classic v1-trap class: UI captured data, silent drop on entity validate, invisible to user. One-character rename at the write site; two defensive read sites in the same file (`allChecks.sort` at line 63 and the "Last check" info line at 179) were also falling through to `createdAt` because the column never populated — fixed to read `date` first. Siblings at `move-wizard.js:466` and `close.js:185` already used the correct key, so `check.js` was the outlier. New `tests/unit/feed-check.test.js` (3 cases: round-trip preserves date, validate rejects null-date row, validate accepts populated row). New `tests/e2e/feed-check-save.spec.js` verifies Supabase round-trip per CLAUDE.md E2E rule. 936 tests pass. **Closes OI-0103.** | src/features/feed/check.js, tests/unit/feed-check.test.js, tests/e2e/feed-check-save.spec.js |
| 2026-04-18 | fix(§7-remove-group): remove incorrect `maybeShowEmptyGroupPrompt` call (OI-0097). Correction to OI-0090's session brief Phase 3 — §7 Remove group closes the `event_group_window` without touching `animal_group_memberships`, so the empty-group check does not apply at this entry point. Removed the `maybeShowEmptyGroupPrompt(groupWindow.groupId)` call from `src/features/events/group-windows.js`; the `closeGroupWindow` call (OI-0094 entry #7) stays. Added scope-boundary comment above the remaining call. Removed the now-unused `maybeShowEmptyGroupPrompt` import. Annotated OI-0090's session brief with a correction banner at the top so re-reads don't follow the broken guidance. No test assertion rewrites — grep confirmed no `maybeShowEmptyGroupPrompt` coverage in tests. 933 tests pass unchanged. **Closes OI-0097.** | src/features/events/group-windows.js, github/issues/SESSION_BRIEF_2026-04-17_empty-group-archive.md, OPEN_ITEMS.md |
| 2026-04-18 | fix(weight): close group-window split gaps on Quick Weight + Edit Animal (OI-0096). Two weight-specific entry points from the OI-0094 audit now route through `maybeSplitForGroup` (promoted to a shared export in the Phase 0 prereq commit `959a7f3`). **Quick Weight sheet** (`src/features/health/weight.js`) — after the existing `add('animalWeightRecords', ...)`, looks up every active membership for the animal and calls `maybeSplitForGroup(m.groupId, dateInput.value)` per membership. The helper no-ops when the group isn't on an open event, so no additional guards needed at the call site. Loop handles the edge case where an animal is in multiple active groups. **Edit Animal dialog** (`src/features/animals/index.js`) — editable `currentWeight` input and `inputs.currentWeight` reference both **deleted** (saveAnimal never read them; removing permanently kills the silent-save regression). Replaced with a read-only "Current weight" row that renders the latest stored weight (or `—` when no record exists) plus a ⚖ Weight button (visible only when editing an existing animal) that closes Edit Animal and opens Quick Weight for the current animal — so weight changes flow through the one `maybeSplitForGroup`-aware path. Existing read-only Weight history section preserved; inline edit/delete of historical weight records deferred to OI-0098 (DESIGN REQUIRED, do not build). New `tests/unit/weight-sheet.test.js` covers the split wiring (3 cases — split triggered, no-op when no memberships, no-op when group not on open event). 933 tests pass (3 new). **No schema change, no migration, no CP-55/CP-56 impact.** **Closes OI-0096.** | src/features/health/weight.js, src/features/animals/index.js, OPEN_ITEMS.md, tests/unit/weight-sheet.test.js |
| 2026-04-18 | feat(event-paddock-window): add split-on-state-change architecture (OI-0095). Paddock-side analog of OI-0091's group-window split. Two new store helpers — `splitPaddockWindow(locationId, eventId, changeDate, changeTime, newState)` closes the current open PW with its prior state preserved as historical truth and opens a new PW carrying `newState` (accepts `areaPct`, `isStripGraze`, `stripGroupId`, `noPasture`; fields not overridden fall back to the closing row's values); `closePaddockWindow(locationId, eventId, closeDate, closeTime)` is the terminal-close variant. Both log warn and no-op when no open PW exists for the pair (Part A orphan prevention). New calc helper `getOpenPwForLocation(locationId, eventId, paddockWindows)` in `src/calcs/window-helpers.js` returns the single currently-open PW for the pair or null. Entry points wired: Advance Strip (`submove.js`) routes the close half through `closePaddockWindow` and keeps the existing `add()` for the open half because the UI exposes distinct close/open dates (strip grazing can have a gap between strips). Edit paddock window (`edit-paddock-window.js`): OPEN windows with `areaPct` or `isStripGraze` changed route through `splitPaddockWindow` with "Saving creates a new window from today forward. The prior area is preserved in the grazing history." caption; toggling strip-graze on mints a fresh `stripGroupId`, toggling off clears it; metadata-only edits (`dateOpened` / `timeOpened`) stay as direct `update()`; CLOSED windows keep direct `update()` as the historical-correction escape hatch; Reopen action adds a same-paddock overlap guard that blocks if any other PW on the same location is currently open on any event. Move-wizard close loop and `close.js` close-event loop converted to `closePaddockWindow`. New paddock-side classifier `classifyPwsForReopen(event)` in `reopen-event.js` — partitions PWs closed on `event.dateOut` into `toReopen` vs `toKeepClosed` with reasons `reused` (same paddock opened later in the event — strip-grazing reuse pattern) or `swap` (another event has an open window on the same paddock). Reopen summary dialog now renders combined group + paddock counts with separate "stays closed because..." lists. Hardcoded `areaPct: 100` literals replaced with `getOpenPwForLocation(...)?.areaPct ?? 100` (falls back to `primaryPw?.areaPct ?? 100` in the single-paddock dashboard case; multi-paddock case still uses 100 because a single areaPct can't represent per-paddock variation across a combined areaHa sum). Part B orphan cleanup shipped as app-side `src/data/one-time-fixes.js::closePaddockWindowOrphans()` guarded by a per-device `localStorage` flag — runs once, routes through `closePaddockWindow` so sync queues normally, logs every row touched; wired into `src/main.js` after store init + initial pull. V2_APP_ARCHITECTURE.md §4.4b added with paddock helper contract + 10-entry-point table + paddock grep contract; §4.4 change-log row added. 930 tests pass (20 new: `store-paddock-window-split.test.js` ×6, `orphan-cleanup.test.js` ×5, `classifyPwsForReopen` cases in `reopen-event.test.js` ×5, `getOpenPwForLocation` cases in `window-helpers.test.js` ×4). **No schema change, no migration, no CP-55/CP-56 impact, no `schema_version` bump.** **Closes OI-0095.** | src/data/store.js, src/data/one-time-fixes.js, src/calcs/window-helpers.js, src/features/events/submove.js, src/features/events/edit-paddock-window.js, src/features/events/move-wizard.js, src/features/events/close.js, src/features/events/reopen-event.js, src/features/dashboard/index.js, src/features/locations/index.js, src/main.js, V2_APP_ARCHITECTURE.md, OPEN_ITEMS.md, tests/unit/store-paddock-window-split.test.js, tests/unit/orphan-cleanup.test.js, tests/unit/reopen-event.test.js, tests/unit/window-helpers.test.js |
| 2026-04-18 | feat: empty group archive flow — migration 024 `archived_at` + empty-group prompt + management UI (OI-0090 / SP-11 Parts 2–4). Migration 024 (`024_groups_archived_at.sql`) adds `groups.archived_at TIMESTAMPTZ`, backfills from `archived = true` → `updated_at`, drops the old boolean, adds a partial `idx_groups_active` on `farm_id WHERE archived_at IS NULL`. Schema-version bump is guarded (`UPDATE operations SET schema_version = 24 WHERE schema_version < 24`) so Tim's live DB at 25 (from OI-0091's migration 025 running before 024 existed) doesn't downgrade; fresh DBs get the normal sequential 23→24→25 chain. Applied + verified via MCP — `information_schema` shows exactly one row (`archived_at`, `timestamp with time zone`). Entity update: `src/entities/group.js` `archived` field replaced with `archivedAt`; `create`/`validate`/`toSupabaseShape`/`fromSupabaseShape` handle null-or-ISO round-trip; new validate rule rejects non-ISO strings. Two new store actions — `archiveGroup(id)` / `reactivateGroup(id)` — stamp / clear `archivedAt` via the standard 6-param `update` flow (Rule 7). Backup-migrations v23→v24 chain entry now contains the real boolean→timestamp transform (falls back to `exported_at` then `now()` when `updated_at` is missing); v24→v25 stays no-op. New `src/features/animals/empty-group-prompt.js` exports `maybeShowEmptyGroupPrompt(groupId)` (triggers when group has zero open memberships and isn't already archived), plus pure helpers `groupIsEmpty` / `groupEventHistoryCount`. Prompt sheet: Archive (primary green), Keep active (secondary), Delete (danger, disabled + tooltip when `event_group_windows` count > 0). Wired after existing OI-0091 window commits in cull-sheet, animals/index.js Edit Group checkboxes / Edit Animal group change / Split Group, move-wizard (only when live headCount < 1 after close), and group-windows §7 Remove. Management UI: `renderGroupsList` extended with Show archived toggle (shows only when archived groups exist), archived section below active groups with Reactivate + Delete buttons (same gating as prompt). Picker filter sweep: every `!g.archived` read in src/features/ swapped to `!g.archivedAt` (animals/index.js ×5, dashboard/index.js ×4, health/calving.js, events/group-windows.js, events/index.js). v1-migration updated to emit `archived_at` instead of `archived` on seed. `src/features/animals/index.js:30 maybeSplitForGroup` + Phase 3 trigger sites left untouched — still OI-0094 territory. 910 tests pass (16 new: `group-archive.test.js` ×10 covering entity round-trip, store actions, chain entry; `empty-group-prompt.test.js` ×6 covering the two pure helpers). New e2e `empty-group-archive.spec.js` cull→prompt→archive→reactivate with Supabase verification. **Closes OI-0090.** SP-11 reconciliation (merge into V2_UX_FLOWS.md §3.4 + §15.2, V2_SCHEMA_DESIGN.md §3.3) deferred to sprint-end reconciliation session. | src/entities/group.js, src/data/store.js, src/data/backup-migrations.js, src/data/v1-migration.js, src/features/animals/empty-group-prompt.js, src/features/animals/index.js, src/features/animals/cull-sheet.js, src/features/events/group-windows.js, src/features/events/move-wizard.js, src/features/events/index.js, src/features/health/calving.js, src/features/dashboard/index.js, supabase/migrations/024_groups_archived_at.sql, OPEN_ITEMS.md, tests/unit/group-archive.test.js, tests/unit/empty-group-prompt.test.js, tests/e2e/empty-group-archive.spec.js |
| 2026-04-18 | feat: complete window-split coverage across all state-change entry points (OI-0094) + remove Animals bulk action bar (OI-0093). **OI-0094 (package 2 after OI-0091):** ten additional flows now route group state changes through OI-0091's `splitGroupWindow` / `closeGroupWindow` helpers, completing the architectural rule ("split on every state change"). (#1) Edit Group checkboxes aggregate affected groupIds into one `affectedGroupIds` pass, then split each. (#2) Split Group sheet splits source on commit; guards target for rare placed-existing case. (#3) Edit Animal group dropdown splits both source (previousGroupId) and target (newGroupId) groups. (#4) Group Weights bulk update splits the group's open window when any weight was written (head count unchanged, avg weight shifts). (#5) Calving new-calf-to-group flow splits dam's group window via new `maybeSplitForGroup` helper; no-op when group not placed. (#6) §7 Add group (group-windows.js openGroupAddSheet) now renders `headCount` + `avgWeightKg` as system-generated view-only labels computed from `getLiveWindowHeadCount` / `getLiveWindowAvgWeight` at save-time; caption: "System generated from live memberships. Use Cull, Move, or Reweigh to change." (#7) §7 Remove group routes through `closeGroupWindow` so live values are stamped on close (was bare `update()`). (#8) §7 per-row Edit dialog (edit-group-window.js) renders `headCount` / `avgWeightKg` as disabled view-only labels when `dateLeft === null` (open window — system-generated, v1 parity per locked sub-decision 2026-04-17); closed windows keep editable inputs as historical-correction escape hatch; date/time join/left fields remain editable in both states. (#9) Delete window button shows the spec'd confirm: "Delete this window? This removes the group's historical presence on this event. Use only to clean up mistakes." (#10) Event reopen flow rewritten with `classifyGwsForReopen` pure classifier — reopen iff group hasn't since moved (no other open window) AND has live memberships (>0); summary dialog shows "N group windows will be reopened. M stay closed because the group has since left." with explicit confirm. New i18n keys `event.systemGeneratedCaption` + `event.deleteWindowConfirm`. V2_APP_ARCHITECTURE.md §4.4 expanded with the authoritative 13-entry-point table + updated grep-contract exception list documenting the narrow allowed direct-mutation paths (new-window creation, explicit user correction in per-row Edit, selective reopen). **OI-0093:** removed the green `#animals-action-bar` DOM, `renderActionBar()`, `selectedAnimals` Set, per-row checkbox column, and the `openAnimalMoveSheet` function entirely (grep confirmed no callers outside the action bar). Edit Animal → Group field rewritten from raw `<select>` to v2 `loc-picker` tap-to-select rows (matches Move wizard + group-add patterns); `inputs.groupId.value` shim preserves saveAnimal() interface. OI-0094 entry #11 (Animals bulk Move) done-by-default — flow removed. New unit test file `tests/unit/reopen-event.test.js` (4 cases covering classifier partitioning). New e2e `tests/e2e/group-state-change-completeness.spec.js` — Edit Animal group change + §7 Add/Remove with Supabase round-trip verification. 894 tests pass. **Closes OI-0094 and OI-0093.** | src/features/animals/index.js, src/features/animals/cull-sheet.js (unchanged, cross-ref), src/features/health/calving.js, src/features/events/group-windows.js, src/features/events/edit-group-window.js, src/features/events/reopen-event.js, src/i18n/locales/en.json, V2_APP_ARCHITECTURE.md, OPEN_ITEMS.md, tests/unit/reopen-event.test.js, tests/e2e/group-state-change-completeness.spec.js |
| 2026-04-18 | fix: event window split on state change (OI-0091 + OI-0073 coordinated P0 package). **OI-0091 (architectural fix, 13 acceptance criteria):** treat `event_group_window` as a period of stable group state. Any mid-event state change (cull, move, event close) closes the current open window with live `head_count` + `avg_weight_kg` stamped at the change date and opens a new window with the new live state. New store helpers `splitGroupWindow` and `closeGroupWindow` in `src/data/store.js` (6-param `update` + 5-param `add` per CLAUDE.md Rule 7, close toast + logger.warn on no-op). New pure calc helpers `getLiveWindowHeadCount` / `getLiveWindowAvgWeight` in `src/calcs/window-helpers.js` — open windows recompute from memberships, closed windows return the stored snapshot; inclusive lower bound on `dateJoined`, exclusive upper on `dateLeft`. All calc and render read sites rerouted through helpers across `src/features/dashboard/index.js` (×16 sites), `src/features/events/detail.js` (×6), `src/features/events/retro-place.js`, `src/features/events/rotation-calendar/calendar-grid.js`, `src/features/reports/index.js` (×3), and FOR-5 (`src/calcs/feed-forage.js` — input schema extended with memberships/animals/animalWeightRecords, DMI-8 callers threaded through). State-change flows wired at the mutation site: `cull-sheet.confirmCull` calls `splitGroupWindow` per affected group after the membership close loop; `move-wizard` replaces the close loop with `closeGroupWindow` and the destination creation (both new-event and existing-event paths) now stamps live values at `dateIn` with a duplicate-open guard; `events/close` uses `closeGroupWindow`. OI-0092 placeholder comment added above `move-wizard.js:482` — `remainingQuantity:0` unchanged per explicit non-scope. `event-group-window.js` validator relaxed to allow `headCount = 0` on closed windows (terminal state after full cull). New V2_APP_ARCHITECTURE.md §4.4 "Window-Split on State Change". **OI-0073 (Parts A/B/C coordinated cleanup):** Part A — dashboard `renderGroupCard` selection prefers open-event windows with most-recent `dateJoined` tie-break. Part B — migration `025_close_orphan_group_windows.sql` closes orphan open windows keeping only the most-recent per group; applied and verified via MCP (0 groups with >1 open window post-apply); `CURRENT_SCHEMA_VERSION` bumped 23→25; `backup-migrations.js` adds 23→24 (reserved slot) and 24→25 no-op entries so the chain stays contiguous. Part C — NPK NaN fix at the property-name mismatch root cause: dashboard was reading `result.n/.p/.k` but NPK-1 returns `{nKg, pKg, kKg}`; rewrote to defensive `result.nKg ?? result.n ?? 0` pattern at 4 sites (331, 422, 679-681). New unit tests: `window-helpers.test.js` (7 cases) + `store-window-split.test.js` (5 cases). New e2e: `cull-dashboard-event-detail.spec.js` (verifies Supabase has exactly 2 rows per (group, event) after cull: closed snapshot + new open). 890 tests pass. **Closes OI-0091 and OI-0073.** | src/data/store.js, src/calcs/window-helpers.js, src/calcs/feed-forage.js, src/features/dashboard/index.js, src/features/events/detail.js, src/features/events/move-wizard.js, src/features/events/close.js, src/features/events/retro-place.js, src/features/events/rotation-calendar/calendar-grid.js, src/features/reports/index.js, src/features/animals/cull-sheet.js, src/entities/event-group-window.js, src/data/backup-migrations.js, src/data/backup-import.js, supabase/migrations/025_close_orphan_group_windows.sql, V2_APP_ARCHITECTURE.md, tests/unit/window-helpers.test.js, tests/unit/store-window-split.test.js, tests/unit/backup-import.test.js, tests/unit/backup-roundtrip.test.js, tests/e2e/cull-dashboard-event-detail.spec.js |
| 2026-04-17 | fix | backup-export.js, backup-import.js, tests/unit/backup-sync.test.js | Closed OI-0087 and OI-0088 from Cowork's local-only fields audit. Added event_observations to BACKUP_TABLES/FK_ORDER; bumped CURRENT_SCHEMA_VERSION 20→23; added 3 mechanical invariant tests so the Export/Import Spec Sync Rule is enforced by CI. |
| 2026-04-17 | docs | SCHEMA_DUMP_2026-04-17.md | Live Supabase schema captured for local-only fields audit. Ground-truth input for Cowork matrix. |
| 2026-04-17 | fix: Cull Sheet v1 parity (OI-0086 / GH-13). Removed broken `window.prompt()` cull stub at `src/features/animals/index.js:1241–1261` that wrote a non-existent `culled` field (silently dropped by `toSupabaseShape()`). New `src/features/animals/cull-sheet.js` exports `openCullSheet`, `buildCulledBanner`, and pure helpers (`confirmCull`, `reactivateAnimal`, `buildAnimalLabel`, `CULL_REASONS`). Cull dialog: date input (defaults to today, can be backdated), 9-option reason `<select>`, optional notes input, amber info banner, Confirm/Cancel buttons. On confirm: writes `active=false` + `cullDate` + `cullReason` + `cullNotes` to Supabase via the entity's existing snake_case mapping AND closes every open `animal_group_memberships` row with `dateLeft=cullDate, reason='cull'`. Toast on success. Red culled-state banner now shows reason + date + notes + Reactivate button (replacing the previous broken red placeholder). Reactivate clears all four cull fields after a confirm. Fixed three stale `a.culled` references (filter, isCulled badge, group picker) to use `a.active === false` so the "Show culled" checkbox works correctly. All store calls verified at 6 params per CLAUDE.md Rule 7. `logger.info('cull', ...)` on confirm + reactivate. New i18n keys (`animal.cull*`, `animal.reactivate*`, `animal.optional`). 12 new unit tests; new `tests/e2e/cull-sheet.spec.js` for sync verification (skips without `E2E_EMAIL`/`E2E_PASSWORD`/`VITE_SUPABASE_*`). 872 tests. | src/features/animals/cull-sheet.js, src/features/animals/index.js, src/i18n/locales/en.json, tests/unit/cull-sheet.test.js, tests/e2e/cull-sheet.spec.js, github/issues/GH-13_cull-sheet-v1-parity.md, OPEN_ITEMS.md |
| 2026-04-17 | fix: §8 Feed Entries v1 parity + bug fixes (OI-0085). (1) Fixed two field-name typos in `renderFeedEntries`: `batch?.feedName` → `batch?.name` and `fe.deliveryDate` → `fe.date`. (2) Rebuilt §8 to v1 inline pattern: `+ Add feed` button in section header, v1-style row layout (date + description on left; DMI/cost stacked + Edit/× buttons on right), inline form mounts below the list (no sheet). New `src/features/events/feed-entry-inline-form.js` holds module-level form state (survives subscriber-triggered re-renders), pure helpers (`validateFeedEntryForm`, `commitAdd`, `commitEdit`, `clampDateToEvent`), and renders into a container. Multi-batch add creates N rows sharing a date; edit updates one row + adjusts `batches.remaining` by the delta; batch is locked in edit mode. All 5 SP-10 §8 validation guards enforced. Delete button restores inventory. `Deliver feed` big footer button removed (replaced by header `+ Add feed`); `Move feed out` footer button kept. 19 new unit tests. 860 tests. | src/features/events/detail.js, src/features/events/feed-entry-inline-form.js, tests/unit/feed-entry-inline-form.test.js, OPEN_ITEMS.md |
| 2026-04-17 | feat: SP-10 §9 feed check edit + re-snap invariant (OI-0084). New `src/features/events/edit-feed-check.js` — per-item edit dialog (date/time/remaining/notes; batch + location read-only chips), range guards (date < event.dateIn, > event.dateOut on closed, > today, remaining < 0), invariant check via pure `buildLineSequence` / `computeIntervals` / `classifyInvariant` helpers covering Cases A (benign save), B (later-interval break → re-snap dialog deletes impossible later items + saves atomically + non-modal toast with "Feed check" shortcut), C (earlier-interval break → Cancel-only conflict dialog), D (back-fill insert routes to A/B/C). Restructured `renderFeedChecks` in `detail.js` to render one row per (check × item) feed line — Edit button now wires to the new dialog. Delete button in the dialog removes the item and orphan parent check. 20 unit tests. 841 tests. | src/features/events/edit-feed-check.js, src/features/events/detail.js, tests/unit/edit-feed-check.test.js, OPEN_ITEMS.md |
| 2026-04-17 | feat: SP-10 retro-place flow (OI-0083). New `src/features/events/retro-place.js` — sheet picker with full-containment filter, optional paddock sub-picker, conflict-check abort, confirm dialog, atomic two-write commit (pre-validate + sequential update/add + manual revert on add failure). Pure helpers (`computeCandidateEvents`, `computeCandidatePaddocks`, `findConflictingWindow`, `commitRetroPlace`) exported for testing. 14 unit tests. Note: gap detection in `edit-group-window.js` is still stubbed (line 95 comment), so retro-place is reachable only via direct invocation/tests until gap detection is wired separately. 835 tests. | src/features/events/retro-place.js, tests/unit/retro-place.test.js, OPEN_ITEMS.md |
| 2026-04-17 | fix: Field mode 3 fixes — (1) feed check returns to #/field when in field mode, (2) event cards expand with detail (date, groups, feed status, cost) + Move All relocated inside expanded section, (3) heat tile 2-step picker: animal selection with event/group filter pills + search, then recording form with toast, stays open for multi-record. Closes OI-0078. | src/features/feed/check.js, src/features/field-mode/index.js, OPEN_ITEMS.md |
| 2026-04-17 | fix: SP-10 deferred — entry_type filtering in DMI-1/CST-1 + inline date_in edit + re-close overlap warning + recovery date preview. Dashboard negates removal quantities. Event summary date_in is editable with reject-on-narrow. Close flow warns on group overlap with other open events. §6 post-graze shows live recovery dates. OI-0083/OI-0084 added for retro-place and feed check edit (design required). | src/features/dashboard/index.js, src/features/events/detail.js, src/features/events/close.js, OPEN_ITEMS.md |
| 2026-04-17 | feat: SP-10 Phase 5 — Event Reopen. Clears date_out + re-opens child windows + group conflict dialog. Reopen button on closed event detail sheet. | src/features/events/reopen-event.js, src/features/events/detail.js |
| 2026-04-17 | feat: SP-10 Phase 4 — §12 paddock window edit + OI-0064 reopen. Date/area/strip edit, range guards, same-paddock overlap reject, delete guards, reopen closed windows. | src/features/events/edit-paddock-window.js, src/features/events/detail.js |
| 2026-04-17 | feat: SP-10 Phase 3 — §7 group window edit. Date/headcount/weight edit, auto-save on blur, delete window, Edit button per group row. Move Feed Out button on §8. | src/features/events/edit-group-window.js, src/features/events/detail.js |
| 2026-04-17 | feat: SP-10 Phase 2 — §8a Move Feed Out. Migration 023 (entry_type, destination_type, destination_event_id on event_feed_entries + check constraints). Entity updated. Move Feed Out 4-step sheet (state → check → amount+dest → confirm). BACKUP_MIGRATIONS[22] entry. | supabase/migrations/023_feed_removal_columns.sql, src/entities/event-feed-entry.js, src/features/events/move-feed-out.js, src/data/backup-migrations.js |
| 2026-04-17 | feat: SP-10 Phase 1 — snapshot/rollback helper + gap/overlap resolver. | src/data/store.js, src/features/events/resolve-window-change.js |
| 2026-04-17 | fix: OI-0080 SP-9 bulk header + surveys tab + bulk-edit replace. Added missing Row 1 action buttons (Cancel, DRAFT pill, Expand/Collapse all, Save Draft, Finish & Save, ✕). Surveys sub-tab: draft resume banner + committed survey list with Edit. Bulk-edit now replaces prior observations. | src/features/locations/index.js, OPEN_ITEMS.md |
| 2026-04-17 | feat: SP-9 survey sheet v1 parity (GH-12). Migration 022 (bale_ring_residue_count + bale_ring_residue_diameter_ft). BRC-1 calc with 6 unit tests. Survey sheet: bulk/single modes, filter pills, collapsible paddock cards, rating sync, bale ring helper, recovery window, draft lifecycle with auto-save, Finish & Save confirm bar. Field mode pasture picker. Settings diameter field. | src/features/locations/index.js, src/calcs/survey-bale-ring.js, src/entities/event-observation.js, src/entities/farm-setting.js, src/data/backup-migrations.js, supabase/migrations/022_survey_bale_ring_columns.sql |
| 2026-04-14 | fix: OI-0056 P1 — REFERENCE_TABLES blocking import delete. Removed 5 per-operation seed tables (forage_types, animal_classes, treatment_categories, treatment_types, input_product_categories) from REFERENCE_TABLES. Only truly global tables (dose_units, input_product_units) remain. Per-operation seed data now follows normal delete-then-insert during import. Cleaned stale Supabase data. 779 tests. | src/data/backup-import.js, tests/unit/backup-import.test.js, OPEN_ITEMS.md |
| 2026-04-14 | fix: OI-0055 P1 — add operation_id to 4 child tables. Migration 019: added operation_id (uuid NOT NULL FK) to event_feed_check_items, harvest_event_fields, survey_draft_entries, todo_assignments with parent-backfill. RLS policies simplified from join-based to direct operation_id. Entity files updated (FIELDS/create/validate/toSupabaseShape/fromSupabaseShape). All add() call sites updated (8 total: close.js, move-wizard.js, check.js, harvest, surveys ×2, todos ×2). v1-migration.js updated for all 4 tables. Backup migration 18→19 backfills from parent tables. Executed against Supabase via MCP + verified. Schema version 19. 779 tests. | supabase/migrations/019_add_operation_id_to_child_tables.sql, src/entities/event-feed-check-item.js, src/entities/harvest-event-field.js, src/entities/survey-draft-entry.js, src/entities/todo-assignment.js, src/data/backup-import.js, src/data/backup-migrations.js, src/data/v1-migration.js, src/features/events/close.js, src/features/events/move-wizard.js, src/features/feed/check.js, src/features/harvest/index.js, src/features/surveys/index.js, src/features/todos/todo-sheet.js, OPEN_ITEMS.md |
| 2026-04-14 | fix: OI-0054 P0 — sync insert/update split + granular RLS. Part 1: sync adapter now uses .insert() for new records, .update() for existing, .upsert() only for recovery/retry. Operation type stored in queue. store.add() passes 'insert', store.update() passes 'update'. Part 2: migration 018 splits all 45 FOR ALL policies into INSERT (WITH CHECK true) / SELECT / UPDATE / DELETE. INSERT allows all authenticated users; membership checks on read/modify only. Part 3: all 12 source migration files (001–012) updated to match. Part 4: executed against Supabase via MCP, verified zero FOR ALL policies remain. Schema version 18. 779 tests. | supabase/migrations/018_granular_rls_policies.sql, src/data/custom-sync.js, src/data/sync-adapter.js, src/data/store.js, src/data/backup-import.js, src/data/backup-migrations.js, supabase/migrations/001–012 (all updated), tests/unit/store.test.js, tests/unit/backup-import.test.js, tests/unit/backup-roundtrip.test.js, OPEN_ITEMS.md |
| 2026-04-14 | fix: OI-0053 P0 — Supabase migrations + RLS bootstrap. Applied missing migrations 014–016 to Supabase (active_farm_id, schema_version, invite_token + RPCs). Fixed operation_members RLS infinite recursion: dropped FOR ALL policy, replaced with granular SELECT/INSERT/UPDATE/DELETE. INSERT uses `user_id = auth.uid()` for onboarding bootstrap (no self-referential subquery). Disabled RLS on dose_units/input_product_units (standalone lookups, no operation_id). Migration 017 written. Migration 001 updated for fresh DB. Schema version 17. 779 tests. | supabase/migrations/017_fix_operation_members_rls.sql, supabase/migrations/001_d1_operations_farms.sql, src/data/backup-import.js, src/data/backup-migrations.js, tests/unit/backup-import.test.js, tests/unit/backup-roundtrip.test.js, OPEN_ITEMS.md |
| 2026-04-14 | fix: OI-0048/0049/0051 — pre-migration data fixes. OI-0048: observation type inferred from raw v1 source string (event_close→close, else open). OI-0049: feed transfer source linking via transferPairId index — both sides get source_event_id, orphans logged to audit. OI-0051: "Copy error log" button on all 3 migration result screens (success, parity, error) — collects logger buffer, audit warnings, result summary, copies to clipboard. 7 new tests. 779 tests passing. | src/data/v1-migration.js, src/features/settings/v1-import.js, src/i18n/locales/en.json, tests/unit/v1-migration.test.js, OPEN_ITEMS.md |
| 2026-04-14 | fix: OI-0050 — onboarding & settings records never synced to Supabase. All 10 add() calls in onboarding and 5 update() calls in settings now include toSupabaseFn + table params. Full codebase audit confirmed 0 other missing sync params. Added pushAllToSupabase() recovery function (src/data/push-all.js) + "Resync to server" button in Settings → Sync & Data for existing users who onboarded before this fix. 772 tests. | src/features/onboarding/index.js, src/features/settings/index.js, src/data/push-all.js, src/i18n/locales/en.json, OPEN_ITEMS.md |
| 2026-04-14 | CP-66: Member management & invite link. Migration 016 (invite_token uuid UNIQUE on operation_members + claim_invite_by_token + claim_pending_invite RPCs). Entity updated (inviteToken field). Backup migration chain 14→15→16. Member management sheet (list with role badges, invite creation form, copy/regenerate/cancel link, role change, remove member). Invite acceptance flow (router #invite= detection, auth context banner, sessionStorage token persistence, claim RPC, email fallback). Settings members section (admin: manage button, team: read-only count). 37 i18n keys. 772 tests passing. | supabase/migrations/016_invite_token.sql, src/entities/operation-member.js, src/data/backup-import.js, src/data/backup-migrations.js, src/features/settings/member-management.js, src/features/settings/index.js, src/features/auth/invite-claim.js, src/main.js, src/styles/main.css, src/i18n/locales/en.json, tests/unit/entities/operation-member.test.js, tests/unit/invite.test.js |
| 2026-04-14 | CP-66 spec written — member management & invite link (shareable URL, no email service). Schema: invite_token uuid on operation_members. UX: §20.7 in V2_UX_FLOWS.md. Phase 3.5. | github/issues/GH-10_cp-66-member-management.md, V2_UX_FLOWS.md, V2_BUILD_INDEX.md, V2_SCHEMA_DESIGN.md, OPEN_ITEMS.md |
| 2026-04-14 | Audit findings fixes (OI-0040/41/43/45/46). Fix 1 (OI-0040/41): Pre-graze and post-graze observation fields on all close/open surfaces — new observation-fields.js helper, createObservation() extended with fields param, post-graze on close.js/move-wizard.js/submove.js, pre-graze on move-wizard.js/submove.js, farm_settings.recovery_required toggle in Settings, unit-aware height display, i18n keys. Fix 2 (OI-0043): Harvest tile → #/harvest. Fix 3 (OI-0045): Removed dead daysBetweenExact(). Fix 4 (OI-0046): "Get The Hay Out" app name in header. OI-0042 deferred to Phase 3.5. 762 tests passing. | src/features/events/observation-fields.js, src/features/events/close.js, src/features/events/move-wizard.js, src/features/events/submove.js, src/features/events/index.js, src/features/settings/index.js, src/features/field-mode/index.js, src/ui/header.js, src/utils/date-utils.js, src/styles/main.css, src/i18n/locales/en.json, tests/unit/date-utils.test.js, OPEN_ITEMS.md |
| 2026-04-14 | Full app audit (Tier 2, 7 audits). Audit 7 (i18n): 28 violations found, 28 fixed (validation messages, filter labels, dashboard display labels, form defaults, reference console title, calendar toolbar, mobile event summary, parity reports, currency display). Audit 8 (innerHTML): 0 violations. Audit 9 (Logger): 0 violations. Audit 10 (Sync): all 8 priority files pass — zero missing sync calls. Audit 11 (UX Flows): §7 Survey PASS; §1 Move wizard 3 deviations (OI-0040, OI-0041); §9 Event close 2 deviations (OI-0040); §14 Health 2 deviations (OI-0042); §16 Field mode 1 deviation (OI-0043); §17 Dashboard 1 partial. Audit 12 (Dead Code): 1 dead export (OI-0045), 0 orphan routes, 0 commented blocks. Audit 13 (Design Tokens): 1 hardcoded hex fixed, 92 inline px values noted as pattern. 765 tests passing. Pre-migration full app audit: 33 violations found, 29 fixed, 5 flagged in OPEN_ITEMS.md. | src/features/dashboard/index.js, src/features/events/index.js, src/features/events/close.js, src/features/events/move-wizard.js, src/features/events/group-windows.js, src/features/events/list-view/events-log.js, src/features/events/mobile-events-screen.js, src/features/events/rotation-calendar/toolbar.js, src/features/reports/reference-console.js, src/features/settings/index.js, src/features/settings/v1-import.js, src/features/amendments/entry.js, src/features/health/weight.js, src/features/health/bcs.js, src/features/feed/delivery.js, src/features/feed/index.js, src/features/todos/todo-sheet.js, src/calcs/feed-forage.js, src/i18n/locales/en.json, OPEN_ITEMS.md |
| 2026-04-14 | Pre-migration audit (Tier 1, 6 audits). Audit 1 (Entity↔Schema): 52 entities verified, 0 mismatches. Audit 2 (Round-Trip): all 52 entities have substantive round-trip tests. Audit 3 (Store↔Entity): 52 types aligned, all add/update actions validate. Audit 4 (Transform Completeness): all 25 sections correct, flagged §2.25 spec text vs schema mismatch (OI-0039). Audit 5 (OI-0012): 29 new calc tests — DMI-1 residual regression, DMI-2 lactation branching, DMI-5 interpolation, FED-1 residual pct, CST-1/2/3 cost math, REC-1 strip graze recovery (13→42 calc tests). Audit 6 (OI-0013): 37 registerCalc() descriptions audited, 1 fix (CST-2 "bidirectional"). 765 tests passing. | tests/unit/calcs.test.js, src/calcs/feed-forage.js, OPEN_ITEMS.md |
| 2026-04-14 | CP-58: v1 migration integration tests + CP-57 spec drift fixes. Audit: OI-0037 (schema_version was hardcoded — now imported from backup-import.js), OI-0038 (auto-backup not skipped for empty operations — added skipAutoBackup option to importOperationBackup). 17 integration tests (full v1 fixture → transform → validate envelope → verify counts, conversions, splits, no data loss). 7 Playwright e2e tests (v1 import button, format detection, preview with counts, two-step confirm, cancel, unparseable dose warning). v1 fixture at tests/fixtures/v1-export-sample.json. 747 tests passing. | src/data/v1-migration.js, src/data/backup-import.js, src/features/settings/v1-import.js, tests/unit/v1-migration.test.js, tests/e2e/v1-migration.spec.js, tests/fixtures/v1-export-sample.json, OPEN_ITEMS.md |
| 2026-04-14 | CP-57: v1 → v2 migration tool — v1-migration.js (25 transform sections per §2.1–§2.25: ID remapping, imperial→metric conversions, location type mapping, event/paddock-window/group-window restructuring, 5-way health event split, dose parsing with audit CSV, NPK parity data collection, settings decomposition into operations+farms+farm_settings+user_preferences, NPK price history seeding). v1-import.js (Settings → Import from v1 button, file detection, transform preview with counts/warnings, two-step confirm, CP-56 pipeline handoff, dose audit CSV auto-download, success/parity/error reporting). 50 new unit tests. 730 tests passing. GH-9 filed. | src/data/v1-migration.js, src/features/settings/v1-import.js, src/features/settings/index.js, src/i18n/locales/en.json, tests/unit/v1-migration.test.js, github/issues/GH-9_cp-57-v1-migration-tool.md |
| 2026-04-13 | CP-56: JSON backup import — backup-import.js (§5.7 steps 1-10: envelope validation, preview extraction, migration chain, FK-ordered wholesale replace per §5.3a, two-pass for animals/events, reference table upsert, auto-backup before destructive replace, parity check). Settings import button with file picker, preview sheet, two-step destructive confirm, progress phases, parity report. 27 unit tests (validation, FK ordering, two-pass, reference tables) + 5 E2E tests. 680 tests passing. | src/data/backup-import.js, src/features/settings/index.js, src/styles/main.css, src/i18n/locales/en.json, tests/unit/backup-import.test.js, tests/e2e/backup-import.spec.js |
| 2026-04-13 | CP-55: JSON backup export — migration 015 (operations.schema_version), operation entity update, backup-export.js (Supabase-read with pagination, §5.2 envelope, §5.5 serialization, offline/sync guard, progress UI), backup-migrations.js (empty registry at v14), Settings export button with confirm/progress sheets, download trigger. 49 tables included, 3 excluded. Round-trip test (26 cases) + fixture + E2E test. 653 tests passing. | supabase/migrations/015_schema_version_stamp.sql, src/entities/operation.js, src/data/backup-export.js, src/data/backup-migrations.js, src/features/settings/index.js, src/styles/main.css, src/i18n/locales/en.json, tests/unit/entities/operation.test.js, tests/unit/backup-roundtrip.test.js, tests/fixtures/backup-v14.json, tests/e2e/backup-export.spec.js |
| 2026-04-13 | CP-54: Rotation calendar — FOR-6 (Forecast Standing DM) + CAP-1 (Period Capacity Coverage) formulas registered with full metadata. 11 new unit tests for confidence bands, strip-grazing sums, coverage fractions, never-grazed, multi-group, surplus/shortfall. Formula count 35 → 37 across 11 domains. | src/calcs/feed-forage.js, src/calcs/capacity.js, src/main.js, tests/unit/calcs.test.js |
| 2026-04-13 | CP-54: Calendar state module — URL query schema (zoom, anchor, groups, period, showConfinement, view), defaults per §19.9, reducers, history.replaceState sync, readStateFromUrl for deep-link support. Router updated to strip query params before route matching. 24 unit tests. | src/features/events/calendar-state.js, src/ui/router.js, tests/unit/calendar-state.test.js |
| 2026-04-13 | CP-54: Rotation calendar UI — 10-file component subtree (header-strip, toolbar, legend, calendar-grid, paddock-column, timeline, sidebar, past-block, future-block, today-line). 3-column grid layout, past event blocks with multi-group labels/strip bands/linked outlines/NOW ring, future forecast blocks (estimated gradient + DM capacity split), toolbar lightboxes, confinement toggle, keyboard shortcuts. 15 component unit tests. | src/features/events/rotation-calendar/*.js, tests/unit/rotation-calendar.test.js |
| 2026-04-13 | CP-54: List view + mobile fallback — GRZ-10 events log (parent + sub-move threads, filter, summary metrics) for desktop List toggle and mobile. GRZ-11 active-rotation banner (paddock chips, date, feedings, groups) for mobile. Mobile events screen rendered at <900px, no calendar. | src/features/events/list-view/events-log.js, src/features/events/mobile-events-screen.js |
| 2026-04-13 | CP-54: Events screen orchestrator — width check (≥900px: calendar + view toggle, <900px: mobile fallback). Calendar state read from URL on mount. Sheet wrappers preserved for event management actions. | src/features/events/index.js |
| 2026-04-13 | CP-54: Reports tabs updated — added Pasture Surveys + Weaning placeholder tabs. Reference tab kept. Tab order: Feed & DMI, NPK Fertility, Animal Performance, Season Summary, Pasture Surveys, Weaning, Reference (7 total). No stale rotation-calendar links found. | src/features/reports/index.js, src/i18n/locales/en.json |
| 2026-04-13 | CP-54: Calendar CSS — header strip, toolbar (lightboxes, pills, chips, confinement toggle), legend (swatches), calendar grid (3-col layout), paddock column, timeline (axes, today line), past/future blocks, sidebar, list view, mobile banner. | src/styles/main.css |
| 2026-04-13 | CP-54: E2E test — 7 Playwright tests: default first-load, view toggle, confinement toggle, deep-link, mobile fallback, resize behavior, Reports tab count. | tests/e2e/rotation-calendar.spec.js |
| 2026-04-13 | All-farms mode filtering (GH-5) — dashboard, events, locations, animals screens use getVisibleEvents/Locations/Groups(). Farm chips on event cards in all-farms mode. 575 tests. | src/features/events/index.js, src/features/locations/index.js, src/features/animals/index.js, src/features/dashboard/index.js |
| 2026-04-13 | Move wizard farm chip + cross-farm event markers (GH-5) — farm chip on Step 2 location picker scopes to dest farm without mutating active_farm_id. Cross-farm moves set sourceEventId. Event cards show directional markers (← from / → to) with farm names. CSS for cross-farm-marker, wizard-farm-chip, farm-chip. 575 tests. | src/features/events/move-wizard.js, src/features/events/index.js, src/styles/main.css |
| 2026-04-13 | Header redesign + farm picker + user menu (GH-5) — left cluster: operation name (18px/700) + farm picker button (multi-farm: chevron, single: plain text). Right cluster: sync dot + build stamp + Field Mode + user menu (28px circle with initials). Farm picker dropdown with All Farms + alphabetical farm list. User menu popover with email + Log Out (confirm only if unsynced writes). 575 tests. | src/ui/header.js, src/styles/main.css, src/i18n/locales/en.json |
| 2026-04-13 | Store API for multi-farm context (GH-5) — getActiveFarmId(), setActiveFarm(farmId), getVisibleLocations/Groups/Events(). Deleted-farm fallback. 5 new store tests. 575 tests. | src/data/store.js, tests/unit/store.test.js |
| 2026-04-13 | Entity updates for multi-farm context (GH-5) — user-preference.js +activeFarmId, event.js +sourceEventId. FIELDS, create, validate, toSupabaseShape, fromSupabaseShape all updated. 2 new round-trip tests. 570 tests. | src/entities/user-preference.js, src/entities/event.js, tests/unit/entities/user-preference.test.js, tests/unit/entities/event.test.js |
| 2026-04-13 | Schema migration 014: multi-farm context (GH-5) — user_preferences.active_farm_id (uuid NULL FK farms ON DELETE SET NULL) + events.source_event_id (uuid NULL FK events ON DELETE SET NULL) | supabase/migrations/014_multi_farm_context.sql |
| 2026-04-13 | Product add dialogs expose unit selection (closes OI-0017) — added unitId selector from inputProductUnits to amendment input product create/edit sheet. Feed type and treatment recording sheets already had unit fields. 568 tests. | src/features/amendments/reference-tables.js, OPEN_ITEMS.md |
| 2026-04-13 | Sync status indicator in app header (closes OI-0018) — compact dot-based indicator (idle/syncing/offline/error) in header-right. Tap navigates to settings sync panel. Reuses existing getSyncAdapter().getStatus(). 568 tests. | src/ui/header.js, src/styles/main.css, OPEN_ITEMS.md |
| 2026-04-13 | Dose units CRUD (closes OI-0016) — add/edit sheet, archive action in settings health reference tables. Follows existing category/type pattern. 568 tests. | src/features/health/reference-tables.js, src/i18n/locales/en.json, OPEN_ITEMS.md |
| 2026-04-13 | Verify manure volumeKg placeholder (closes OI-0014) — confirmed placeholder is architecturally correct (reports compute via NPK-1 at display time, not stored column). Updated code comment in close.js. | src/features/events/close.js, OPEN_ITEMS.md |
| 2026-04-13 | Wire feed screen metrics to live data (closes OI-0011) — DM on hand (batch.remaining × dm_pct), daily run rate (30-day avg DM delivered), days on hand (DM ÷ rate). Three stat cells in goal banner. Progress bar threshold coloring active. 568 tests. | src/features/feed/index.js, OPEN_ITEMS.md |
| 2026-04-13 | docs: add OI-0017 (product unit selection), OI-0018 (header sync status), OI-0019 (header logout) to OPEN_ITEMS.md | OPEN_ITEMS.md |
| 2026-04-13 | Strip grazing UI (A45) — dual area/pct input in move wizard with auto-derivation and unit awareness, strip progress bar visualization per §3.15 on event cards and dashboard (active/completed/upcoming states, proportional widths), per-window area_pct labels. Standalone commit before CP-54. 568 tests. | src/features/events/move-wizard.js, src/features/events/index.js, src/features/dashboard/index.js, src/styles/main.css, src/i18n/locales/en.json |
| 2026-04-13 | Unit system migration (A44) — move unit_system from localStorage to operations table. SQL migration 013, entity update (FIELDS/validate/shape), store.setUnitSystem() action, preferences.js reads from store, settings toggle rewired, onboarding adds unit selector, boot-time localStorage migration. 568 tests. | supabase/migrations/013, src/entities/operation.js, src/data/store.js, src/utils/preferences.js, src/features/settings/index.js, src/features/onboarding/index.js, src/main.js, tests/unit/entities/operation.test.js |
| 2026-04-13 | docs: integrate A44 unit system + A45 strip grazing across specs; close OI-0001, OI-0002 — schema design, infrastructure, migration plan, build index, open items updated; new implementation spec for unit system migration | GTHO_V2_SCHEMA_DESIGN.md, V2_INFRASTRUCTURE.md, V2_MIGRATION_PLAN.md, V2_BUILD_INDEX.md, OPEN_ITEMS.md, github/issues/unit-system-operations-migration.md |
| 2026-04-13 | OI-0009: Fix desktop layout overlap — add grid-column: 2 to .app-content in desktop media query | src/styles/main.css |
| 2026-04-13 | OI-0010: Complete dashboard rebuild per §17 — stats row (5 desktop / 3 mobile), period pills, view toggle, group cards (composition, location bar, DMI, NPK, actions), location cards, open tasks section, survey draft card, weaning nudge, mobile bottom nav, header with farm name | src/features/dashboard/index.js, src/ui/header.js, src/styles/main.css, src/i18n/locales/en.json |
| 2026-04-13 | Todos feature UI — list screen with 3-axis filters, create/edit sheet, card component, #/todos route, nav entries with badge | src/features/todos/index.js, src/features/todos/todo-card.js, src/features/todos/todo-sheet.js, src/main.js, src/ui/header.js |
| 2026-04-11 | Initial scaffold — repo setup with doc structure and issue tracking | CLAUDE.md, PROJECT_CHANGELOG.md, OPEN_ITEMS.md, IMPROVEMENTS.md, github/issues/ |
| 2026-04-12 | CP-1: Vite project init — package.json, vite.config.js, index.html, src/main.js, .gitignore, .env.example, eslint.config.js, vitest harness | package.json, vite.config.js, index.html, src/main.js, .gitignore, .env.example, eslint.config.js, tests/unit/setup.test.js |
| 2026-04-12 | CP-2: Core utilities — logger, date-utils, validators, units (6 types), calc-registry with 89 passing tests | src/utils/logger.js, date-utils.js, validators.js, units.js, calc-registry.js + 4 test files |
| 2026-04-12 | CP-3: D1 entities — operation, farm, farm-setting, operation-member, user-preference + migration SQL + 5 test files (134 total tests) | src/entities/5 files, supabase/migrations/001, tests/unit/entities/5 files |
| 2026-04-12 | CP-4: D2-D4 entities — location, forage-type, animal-class, animal, group, animal-group-membership, feed-type, batch, batch-adjustment + 3 migrations + 9 test files (179 total tests) | src/entities/9 files, supabase/migrations/002-004, tests/unit/entities/9 files |
| 2026-04-12 | CP-5: D5-D7 entities — 11 entities (events, surveys, harvest) + 3 migrations + 11 test files (226 total tests) | src/entities/11 files, supabase/migrations/005-007, tests/unit/entities/11 files |
| 2026-04-12 | CP-6: D8-D11 entities — 26 entities (nutrients, health, feed quality, infrastructure) + 4 migrations + 26 test files (473 total tests). All 51 entities complete. | src/entities/26 files, supabase/migrations/008-011, tests/unit/entities/26 files |
| 2026-04-12 | CP-7: i18n setup — t() with nested keys, interpolation, missing-key fallback + en.json skeleton + tests | src/i18n/i18n.js, src/i18n/locales/en.json, tests/unit/i18n.test.js |
| 2026-04-12 | CP-8: Data layer — store (51 entity types, getters return copies, validate→mutate→persist→notify), sync adapter interface, custom sync (offline queue, exponential backoff, dead letters), localStorage wrapper, Supabase client + 3 test files (516 total tests) | src/data/store.js, local-storage.js, sync-adapter.js, custom-sync.js, supabase-client.js + 3 test files |
| 2026-04-12 | CP-9: UI framework — DOM builder (el/text/clear, no innerHTML), Sheet class (.open toggle), hash router (7 routes), header, main.css (design tokens, responsive layout) + 2 test files | src/ui/dom.js, sheet.js, router.js, header.js, src/styles/main.css + 2 test files |
| 2026-04-12 | CP-10: App shell — boot sequence wired (store→locale→router→header), 7 placeholder screens, integration test (create→read→shape round-trip→subscriber). Phase 3.1 Scaffold COMPLETE. 538 tests passing. | src/main.js, index.html, 7 feature/index.js placeholders, tests/unit/integration.test.js |
| 2026-04-12 | GitHub Actions CI/deploy — lint→test→build→deploy to Pages on push to main. Vite base path set to /GTHO-v2/. Pages switched to Actions workflow. | .github/workflows/deploy.yml, vite.config.js, CLAUDE.md |
| 2026-04-12 | CP-11: Auth flow — login/signup/logout via Supabase Auth, session persistence, auth overlay (z-500), boot sequence gates app behind auth | src/features/auth/session.js, auth/index.js, main.js, en.json, main.css |
| 2026-04-12 | CP-12: Onboarding wizard — 4-step wizard (operation→farm→species→review), seeds animal classes with NRCS defaults, treatment/input categories, forage types, dose/product units | src/features/onboarding/index.js, seed-data.js, main.js, en.json, main.css |
| 2026-04-12 | CP-13: Settings screen — farm settings (12 fields), unit system toggle, user prefs (view/home mode), account/logout, sync status indicator. OI-0002 logged for missing unit_system column. | src/features/settings/index.js, src/utils/preferences.js, en.json, main.css, OPEN_ITEMS.md |
| 2026-04-12 | All 11 Supabase migrations applied — 51 tables created with RLS enabled via MCP | supabase/migrations/001–011 (applied to remote) |
| 2026-04-12 | CP-14: Locations screen — location CRUD with cards, filter pills (All/Pasture/Mixed-Use/Crop/Confinement), type-conditional create/edit sheet, area in user units, forage type CRUD section, data-testid on all interactive elements. 538 tests passing. | src/features/locations/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-15: Animals screen — Groups & Classes tabs. Group CRUD with cards (color dot, head count from memberships). Class CRUD with species grouping, filter pills, edit sheet for all rates (DMI, excretion, weight in user units, weaning age). 538 tests passing. | src/features/animals/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-16: Individual animals — Animals tab with sortable table (tag/class/group columns), search by tag/name/EID, animal CRUD sheet with sex toggle, class picker, group assignment (creates/closes memberships via ledger), birth date, notes. 538 tests passing. | src/features/animals/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-17: Events — Create & List. Event list (active first, day count, status badges). Event card shows paddock windows (primary label, open/closed) and group windows (head count, avg weight snapshots). Create event sheet with location picker (Ready/In Use/Confinement sections) and group picker (auto-fills head count + avg weight from memberships). Creates event + paddock_window + group_window records. 538 tests passing. | src/features/events/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-18: Paddock & Group windows. Sub-move open/close sheets (z-index 210). Primary paddock Close button disabled with tooltip. Group add sheet (group picker, head count, avg weight). Group remove sheet (date/time left). Event cards update live via subscribers. Advance Strip button wired (strip observation fields deferred to Phase 3.3). 538 tests passing. | src/features/events/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-19: Move wizard — 3-step wizard (destination type → location/event picker → close & move). Closes source event (all windows + date_out), creates destination event with paddock + group windows. Join existing event support. Strip graze flags (is_strip_graze, strip_group_id, area_pct). Feed transfer placeholder. 9-action save sequence. 538 tests passing. | src/features/events/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-20: Event close (no move) — Close Event button on active event cards. Sheet with date/time out. Closes all open paddock + group windows, sets event date_out. Feed check placeholder. Observation creation deferred to Phase 3.3. 538 tests passing. | src/features/events/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-21: Dashboard — Group cards with color bar, head count, location status (from active events), days on pasture. Mobile collapse/expand. Desktop 2-col grid. Move button navigates to events and triggers move wizard. FAB with creation shortcuts (new event, location, group). Reactive updates via store subscribers. Metrics placeholder. 538 tests passing. | src/features/dashboard/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-22: Supabase sync wiring — CustomSync adapter wired in boot sequence. Online writes → Supabase upsert. Offline → localStorage queue. Flush on reconnect via window 'online' event. Dead letters after 5 retries. Sync status indicator on settings screen reads from adapter. 538 tests passing. | src/main.js |
| 2026-04-12 | CP-23: Integration smoke test — Playwright e2e covering signup → onboard → locations → animals → events → sub-move → move wizard → close event → verify persistence. Playwright config added. 538 unit tests passing. **Phase 3.2 Core Loop COMPLETE.** | playwright.config.js, tests/e2e/smoke.spec.js |
| 2026-04-12 | OI-0004 fix: Pull/merge from Supabase — sync registry (50 entity types → table + fromSupabaseShape), mergeRemote() with remote-wins-on-updated_at logic (5 tests), pullAllRemote() orchestrator, wired into boot + reconnect. 543 tests. | src/data/sync-registry.js, src/data/pull-remote.js, src/data/store.js, src/main.js, tests/unit/store.test.js |
| 2026-04-12 | OI-0005 fix: E2E test selectors — fixed 3 onboarding mismatches, changed from signup to login flow, added env var guard. All 35 selectors verified against source. | tests/e2e/smoke.spec.js |
| 2026-04-12 | OI-0007 fix: Paddock observations — createObservation() helper wired into all 5 event action points (event create, sub-move open, sub-move close, move wizard, event close). Records shell with type/source/sourceId/observedAt. Forage fields Phase 3.3. 543 tests. | src/features/events/index.js, OPEN_ITEMS.md |
| 2026-04-12 | OI-0006 fix: Advance Strip button — renders on event cards with open strip graze windows. Two-phase sheet (close current + open next). End strip early option. Strip progress label. Creates close/open observations. 543 tests. | src/features/events/index.js, OPEN_ITEMS.md |
| 2026-04-12 | Schema amendment: animal_notes table (OI-0003). Migration 012, entity file, store (52 types), sync registry, 7 round-trip tests. 550 tests. | supabase/migrations/012, src/entities/animal-note.js, src/data/store.js, src/data/sync-registry.js, tests/unit/entities/animal-note.test.js |
| 2026-04-12 | CP-24: Feed types CRUD — Feed screen with feed type list (category, unit, DM%, weight, harvest badge). Create/edit sheet with all fields: name, category, unit, DM%, NPK%, default weight, cutting #, forage type, harvest_active toggle. 550 tests. | src/features/feed/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-25: Batch inventory — Batch CRUD with cards (remaining/total progress bar, color-coded). Batch adjustment sheet (reconcile/waste/sold/other) creates batch_adjustment record + updates batch remaining. Feed metrics placeholder for daily run rate/DM on hand. 550 tests. | src/features/feed/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-26: Feed day goal — Banner at top of feed screen showing goal from farm_settings.feed_day_goal. Progress bar with threshold coloring (green ≥ goal, amber 33–99%, red < 33%). Days on hand placeholder until CP-27 provides consumption data. 550 tests. | src/features/feed/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-27: Feed delivery — Deliver Feed button on active event cards. Delivery sheet with batch selector, paddock picker (from active windows), quantity with ±0.5 adjusters, date/time. Creates event_feed_entry + decrements batch.remaining. 550 tests. | src/features/events/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-28: Feed check — Feed Check button on active event cards. Sheet shows one row per batch×paddock with total delivered and remaining input. Creates event_feed_check + event_feed_check_items. 550 tests. | src/features/events/index.js, src/i18n/locales/en.json |
| 2026-04-12 | Refactor: Split events/index.js into 7 domain-scoped files per V2_APP_ARCHITECTURE.md §3 (events/index 725, move-wizard 419, submove 303, group-windows 208, close 106, feed/delivery 197, feed/check 156). | src/features/events/*, src/features/feed/* |
| 2026-04-12 | CP-29: Feed transfer in move wizard — Step 3 shows remaining feed per batch×paddock with "Move?" toggles. Creates close-reading feed_check on source (is_close_reading=true) + feed_entry on destination with source_event_id. Replaces placeholder. 550 tests. | src/features/events/move-wizard.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-30: Event close full version — optional feed check (shows batch×paddock remaining inputs when feed exists, creates close-reading check). Confinement NPK routing creates manure_batch_transaction (type='input') for locations with capture_percent > 0. Volume populated by calc engine. 550 tests. | src/features/events/close.js |
| 2026-04-12 | CP-31: Survey workflow — create survey (bulk/single), draft entry form with all fields (forage height, cover, quality, condition, bale ring residue, recovery days, notes). Bulk auto-creates entries for all land locations. "Finish Survey" commits drafts to paddock_observations. Route #/surveys registered. 550 tests. | src/features/surveys/index.js, src/main.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-32: Health reference tables — AI sires CRUD, treatment categories CRUD, treatment types CRUD (with category FK), dose units list. All on settings screen. Sheets in health/reference-tables.js. 550 tests. | src/features/health/reference-tables.js, src/features/settings/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-33: Weight & BCS recording — health/weight.js and health/bcs.js reusable sheets per §14. Weight + BCS buttons on animal table rows. Species-dependent BCS scale (cattle 1–9, sheep/goat 1–5). Latest weight + BCS displayed in animal table. 550 tests. | src/features/health/weight.js, src/features/health/bcs.js, src/features/animals/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-34: Field mode — field-mode/index.js home screen with 4 action tiles, active events (feed status dots), tasks. Toggle in header. URL param (?field=*) + localStorage persistence. Full-screen sheets on mobile via CSS. Field mode header with exit/home nav. #/field route registered. 550 tests. | src/features/field-mode/index.js, src/ui/header.js, src/main.js, src/utils/preferences.js, src/styles/main.css, src/i18n/locales/en.json |
| 2026-04-12 | CP-35: Treatment recording — health/treatment.js with treatment type picker (grouped by category), product, structured dose (amount + unit from dose_units), withdrawal date, date, notes. Treatment button on animal table rows. 550 tests. | src/features/health/treatment.js, src/features/animals/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-36: Breeding & heat — health/breeding.js (method toggle AI/bull, sire pickers for AI bulls + herd bulls, semen ID, technician, expected calving, confirmed date) + health/heat.js (date, notes). Both wired to animal table rows. 550 tests. | src/features/health/breeding.js, src/features/health/heat.js, src/features/animals/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-37: Calving — health/calving.js. Creates calf animal (tag, sex, class, group assignment via membership), birth weight record (source='calving'), calving record (dam link, stillbirth, dried_off_date for dairy). A27 class reassignment prompt (heifer→cow). 550 tests. | src/features/health/calving.js, src/features/animals/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-38: Amendment reference tables — product categories CRUD, input products CRUD (NPK%, cost), spreaders CRUD (capacity in user units), product units list. All on settings screen. 550 tests. | src/features/amendments/reference-tables.js, src/features/settings/index.js, src/i18n/locales/en.json |
| 2026-04-13 | CP-39: Soil tests — per-paddock recording with 13-element nutrient panel (N-Cl), pH/buffer/CEC/base sat/OM, lab, extraction method. List with NPK+pH summary. #/soil-tests route. 550 tests. | src/features/amendments/soil-tests.js, src/main.js, src/i18n/locales/en.json |
| 2026-04-13 | CP-40: Amendment entry — date, source (product/manure), product picker, quantity, cost override, multi-select paddocks, NPK preview. Creates amendment + amendment_locations. #/amendments route. 550 tests. | src/features/amendments/entry.js |
| 2026-04-13 | CP-41: Manure system — batch CRUD (label, source location, 13-element composition, volume, capture date). Transaction ledger per batch (input/use/transfer/adjust). #/manure route. 550 tests. | src/features/amendments/manure.js |
| 2026-04-13 | CP-42: NPK price history — per-farm with effective_date. List sorted by date, current entry highlighted. #/npk-prices route. 550 tests. | src/features/amendments/npk-prices.js |
| 2026-04-13 | CP-43: Harvest — select location + harvest-active feed types, quantity/weight/DM%/cutting. Creates harvest_event + harvest_event_fields + auto-creates batch (source='harvest'). #/harvest route. 550 tests. | src/features/harvest/index.js |
| 2026-04-13 | CP-44: Feed quality / batch profiles — lab results entry (DM%, protein, ADF, NDF, TDN, RFV, NPK, minerals). Source tracking. #/feed-quality route. 550 tests. | src/features/feed/quality.js |
| 2026-04-13 | CP-45: Calc engine core — 9 formulas registered: NPK-1 (excretion), NPK-3 (paddock attribution), ANI-1 (group totals), ANI-2 (animal-days), ANI-3 (weaning target), TIM-1/2/3 (time calcs), UNT-1 (unit conversion). Reference console on reports screen. | src/calcs/core.js, src/features/reports/reference-console.js, src/features/reports/index.js, src/main.js |
| 2026-04-13 | CP-46: Calc engine feed & forage — 20 formulas: DMI-1 through DMI-7 + DMI-1a, FOR-1 through FOR-5, CST-1/2, FED-1 through FED-5. Strip-grazing-aware FOR-1. Lactation-aware DMI-2. | src/calcs/feed-forage.js |
| 2026-04-13 | CP-47: Calc engine advanced — 6 formulas: NPK-2 (fertilizer value with price history), NPK-4 (amendment NPK), CST-3, REC-1 (recovery window), SUR-1 (quality rating), UNT-2 (manure volume). All 35 formulas registered. 13 calc tests. 563 total tests. | src/calcs/advanced.js, tests/unit/calcs.test.js |
| 2026-04-13 | CP-48–51: Reports — 4 report tabs (Feed & DMI, NPK Fertility, Animal Performance, Season Summary) + Reference Console tab. Feed tab: total deliveries/DM/cost + group DMI breakdown. NPK tab: per-event NPK with price history. Animals tab: weight/BCS/breeding/treatment summaries. Season tab: AUDs by paddock + feed costs + survey trends. | src/features/reports/index.js, src/i18n/locales/en.json |
| 2026-04-13 | CP-52: Dashboard full metrics — group cards show real DMI (via DMI-2), feed cost (via CST-1), days on pasture. Replaces placeholder text. | src/features/dashboard/index.js, src/i18n/locales/en.json |
| 2026-04-13 | CP-53: E2E smoke test updated — added CP-53 test covering batch → deliver feed → survey → all report tabs render → dashboard metrics. Requires E2E credentials. **Phase 3.3 Assessment COMPLETE.** | tests/e2e/smoke.spec.js |

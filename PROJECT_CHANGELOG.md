# Project Changelog

| Date | Change | Files |
|------|--------|-------|
| 2026-04-18 | feat(event-paddock-window): add split-on-state-change architecture (OI-0095). Paddock-side analog of OI-0091's group-window split. Two new store helpers — `splitPaddockWindow(locationId, eventId, changeDate, changeTime, newState)` closes the current open PW with its prior state preserved as historical truth and opens a new PW carrying `newState` (accepts `areaPct`, `isStripGraze`, `stripGroupId`, `noPasture`; fields not overridden fall back to the closing row's values); `closePaddockWindow(locationId, eventId, closeDate, closeTime)` is the terminal-close variant. Both log warn and no-op when no open PW exists for the pair (Part A orphan prevention). New calc helper `getOpenPwForLocation(locationId, eventId, paddockWindows)` in `src/calcs/window-helpers.js` returns the single currently-open PW for the pair or null. Entry points wired: Advance Strip (`submove.js`) routes the close half through `closePaddockWindow` and keeps the existing `add()` for the open half because the UI exposes distinct close/open dates (strip grazing can have a gap between strips). Edit paddock window (`edit-paddock-window.js`): OPEN windows with `areaPct` or `isStripGraze` changed route through `splitPaddockWindow` with "Saving creates a new window from today forward. The prior area is preserved in the grazing history." caption; toggling strip-graze on mints a fresh `stripGroupId`, toggling off clears it; metadata-only edits (`dateOpened` / `timeOpened`) stay as direct `update()`; CLOSED windows keep direct `update()` as the historical-correction escape hatch; Reopen action adds a same-paddock overlap guard that blocks if any other PW on the same location is currently open on any event. Move-wizard close loop and `close.js` close-event loop converted to `closePaddockWindow`. New paddock-side classifier `classifyPwsForReopen(event)` in `reopen-event.js` — partitions PWs closed on `event.dateOut` into `toReopen` vs `toKeepClosed` with reasons `reused` (same paddock opened later in the event — strip-grazing reuse pattern) or `swap` (another event has an open window on the same paddock). Reopen summary dialog now renders combined group + paddock counts with separate "stays closed because..." lists. Hardcoded `areaPct: 100` literals replaced with `getOpenPwForLocation(...)?.areaPct ?? 100` (falls back to `primaryPw?.areaPct ?? 100` in the single-paddock dashboard case; multi-paddock case still uses 100 because a single areaPct can't represent per-paddock variation across a combined areaHa sum). Part B orphan cleanup shipped as app-side `src/data/one-time-fixes.js::closePaddockWindowOrphans()` guarded by a per-device `localStorage` flag — runs once, routes through `closePaddockWindow` so sync queues normally, logs every row touched; wired into `src/main.js` after store init + initial pull. V2_APP_ARCHITECTURE.md §4.4b added with paddock helper contract + 10-entry-point table + paddock grep contract; §4.4 change-log row added. 930 tests pass (20 new: `store-paddock-window-split.test.js` ×6, `orphan-cleanup.test.js` ×5, `classifyPwsForReopen` cases in `reopen-event.test.js` ×5, `getOpenPwForLocation` cases in `window-helpers.test.js` ×4). **No schema change, no migration, no CP-55/CP-56 impact, no `schema_version` bump.** **Closes OI-0095.** | src/data/store.js, src/data/one-time-fixes.js, src/calcs/window-helpers.js, src/features/events/submove.js, src/features/events/edit-paddock-window.js, src/features/events/move-wizard.js, src/features/events/close.js, src/features/events/reopen-event.js, src/features/dashboard/index.js, src/features/locations/index.js, src/main.js, V2_APP_ARCHITECTURE.md, OPEN_ITEMS.md, tests/unit/store-paddock-window-split.test.js, tests/unit/orphan-cleanup.test.js, tests/unit/reopen-event.test.js, tests/unit/window-helpers.test.js |
| 2026-04-18 | feat: empty group archive flow — migration 024 `archived_at` + empty-group prompt + management UI (OI-0090 / SP-11 Parts 2–4). Migration 024 (`024_groups_archived_at.sql`) adds `groups.archived_at TIMESTAMPTZ`, backfills from `archived = true` → `updated_at`, drops the old boolean, adds a partial `idx_groups_active` on `farm_id WHERE archived_at IS NULL`. Schema-version bump is guarded (`UPDATE operations SET schema_version = 24 WHERE schema_version < 24`) so Tim's live DB at 25 (from OI-0091's migration 025 running before 024 existed) doesn't downgrade; fresh DBs get the normal sequential 23→24→25 chain. Applied + verified via MCP — `information_schema` shows exactly one row (`archived_at`, `timestamp with time zone`). Entity update: `src/entities/group.js` `archived` field replaced with `archivedAt`; `create`/`validate`/`toSupabaseShape`/`fromSupabaseShape` handle null-or-ISO round-trip; new validate rule rejects non-ISO strings. Two new store actions — `archiveGroup(id)` / `reactivateGroup(id)` — stamp / clear `archivedAt` via the standard 6-param `update` flow (Rule 7). Backup-migrations v23→v24 chain entry now contains the real boolean→timestamp transform (falls back to `exported_at` then `now()` when `updated_at` is missing); v24→v25 stays no-op. New `src/features/animals/empty-group-prompt.js` exports `maybeShowEmptyGroupPrompt(groupId)` (triggers when group has zero open memberships and isn't already archived), plus pure helpers `groupIsEmpty` / `groupEventHistoryCount`. Prompt sheet: Archive (primary green), Keep active (secondary), Delete (danger, disabled + tooltip when `event_group_windows` count > 0). Wired after existing OI-0091 window commits in cull-sheet, animals/index.js Edit Group checkboxes / Edit Animal group change / Split Group, move-wizard (only when live headCount < 1 after close), and group-windows §7 Remove. Management UI: `renderGroupsList` extended with Show archived toggle (shows only when archived groups exist), archived section below active groups with Reactivate + Delete buttons (same gating as prompt). Picker filter sweep: every `!g.archived` read in src/features/ swapped to `!g.archivedAt` (animals/index.js ×5, dashboard/index.js ×4, health/calving.js, events/group-windows.js, events/index.js). v1-migration updated to emit `archived_at` instead of `archived` on seed. `src/features/animals/index.js:30 maybeSplitForGroup` + Phase 3 trigger sites left untouched — still OI-0094 territory. 910 tests pass (16 new: `group-archive.test.js` ×10 covering entity round-trip, store actions, chain entry; `empty-group-prompt.test.js` ×6 covering the two pure helpers). New e2e `empty-group-archive.spec.js` cull→prompt→archive→reactivate with Supabase verification. **Closes OI-0090.** SP-11 reconciliation (merge into V2_UX_FLOWS.md §3.4 + §15.2, V2_SCHEMA_DESIGN.md §3.3) deferred to sprint-end reconciliation session. | src/entities/group.js, src/data/store.js, src/data/backup-migrations.js, src/data/v1-migration.js, src/features/animals/empty-group-prompt.js, src/features/animals/index.js, src/features/animals/cull-sheet.js, src/features/events/group-windows.js, src/features/events/move-wizard.js, src/features/events/index.js, src/features/health/calving.js, src/features/dashboard/index.js, supabase/migrations/024_groups_archived_at.sql, OPEN_ITEMS.md, tests/unit/group-archive.test.js, tests/unit/empty-group-prompt.test.js, tests/e2e/empty-group-archive.spec.js |
| 2026-04-18 | feat: complete window-split coverage across all state-change entry points (OI-0094) + remove Animals bulk action bar (OI-0093). **OI-0094 (package 2 after OI-0091):** ten additional flows now route group state changes through OI-0091's `splitGroupWindow` / `closeGroupWindow` helpers, completing the architectural rule ("split on every state change"). (#1) Edit Group checkboxes aggregate affected groupIds into one `affectedGroupIds` pass, then split each. (#2) Split Group sheet splits source on commit; guards target for rare placed-existing case. (#3) Edit Animal group dropdown splits both source (previousGroupId) and target (newGroupId) groups. (#4) Group Weights bulk update splits the group's open window when any weight was written (head count unchanged, avg weight shifts). (#5) Calving new-calf-to-group flow splits dam's group window via new `maybeSplitForGroup` helper; no-op when group not placed. (#6) §7 Add group (group-windows.js openGroupAddSheet) now renders `headCount` + `avgWeightKg` as system-generated view-only labels computed from `getLiveWindowHeadCount` / `getLiveWindowAvgWeight` at save-time; caption: "System generated from live memberships. Use Cull, Move, or Reweigh to change." (#7) §7 Remove group routes through `closeGroupWindow` so live values are stamped on close (was bare `update()`). (#8) §7 per-row Edit dialog (edit-group-window.js) renders `headCount` / `avgWeightKg` as disabled view-only labels when `dateLeft === null` (open window — system-generated, v1 parity per locked sub-decision 2026-04-17); closed windows keep editable inputs as historical-correction escape hatch; date/time join/left fields remain editable in both states. (#9) Delete window button shows the spec'd confirm: "Delete this window? This removes the group's historical presence on this event. Use only to clean up mistakes." (#10) Event reopen flow rewritten with `classifyGwsForReopen` pure classifier — reopen iff group hasn't since moved (no other open window) AND has live memberships (>0); summary dialog shows "N group windows will be reopened. M stay closed because the group has since left." with explicit confirm. New i18n keys `event.systemGeneratedCaption` + `event.deleteWindowConfirm`. V2_APP_ARCHITECTURE.md §4.4 expanded with the authoritative 13-entry-point table + updated grep-contract exception list documenting the narrow allowed direct-mutation paths (new-window creation, explicit user correction in per-row Edit, selective reopen). **OI-0093:** removed the green `#animals-action-bar` DOM, `renderActionBar()`, `selectedAnimals` Set, per-row checkbox column, and the `openAnimalMoveSheet` function entirely (grep confirmed no callers outside the action bar). Edit Animal → Group field rewritten from raw `<select>` to v2 `loc-picker` tap-to-select rows (matches Move wizard + group-add patterns); `inputs.groupId.value` shim preserves saveAnimal() interface. OI-0094 entry #11 (Animals bulk Move) done-by-default — flow removed. New unit test file `tests/unit/reopen-event.test.js` (4 cases covering classifier partitioning). New e2e `tests/e2e/group-state-change-completeness.spec.js` — Edit Animal group change + §7 Add/Remove with Supabase round-trip verification. 894 tests pass. **Closes OI-0094 and OI-0093.** | src/features/animals/index.js, src/features/animals/cull-sheet.js (unchanged, cross-ref), src/features/health/calving.js, src/features/events/group-windows.js, src/features/events/edit-group-window.js, src/features/events/reopen-event.js, src/i18n/locales/en.json, V2_APP_ARCHITECTURE.md, OPEN_ITEMS.md, tests/unit/reopen-event.test.js, tests/e2e/group-state-change-completeness.spec.js |
| 2026-04-18 | fix: event window split on state change (OI-0091 + OI-0073 coordinated P0 package). **OI-0091 (architectural fix, 13 acceptance criteria):** treat `event_group_window` as a period of stable group state. Any mid-event state change (cull, move, event close) closes the current open window with live `head_count` + `avg_weight_kg` stamped at the change date and opens a new window with the new live state. New store helpers `splitGroupWindow` and `closeGroupWindow` in `src/data/store.js` (6-param `update` + 5-param `add` per CLAUDE.md Rule 7, close toast + logger.warn on no-op). New pure calc helpers `getLiveWindowHeadCount` / `getLiveWindowAvgWeight` in `src/calcs/window-helpers.js` — open windows recompute from memberships, closed windows return the stored snapshot; inclusive lower bound on `dateJoined`, exclusive upper on `dateLeft`. All calc and render read sites rerouted through helpers across `src/features/dashboard/index.js` (×16 sites), `src/features/events/detail.js` (×6), `src/features/events/retro-place.js`, `src/features/events/rotation-calendar/calendar-grid.js`, `src/features/reports/index.js` (×3), and FOR-5 (`src/calcs/feed-forage.js` — input schema extended with memberships/animals/animalWeightRecords, DMI-8 callers threaded through). State-change flows wired at the mutation site: `cull-sheet.confirmCull` calls `splitGroupWindow` per affected group after the membership close loop; `move-wizard` replaces the close loop with `closeGroupWindow` and the destination creation (both new-event and existing-event paths) now stamps live values at `dateIn` with a duplicate-open guard; `events/close` uses `closeGroupWindow`. OI-0092 placeholder comment added above `move-wizard.js:482` — `remainingQuantity:0` unchanged per explicit non-scope. `event-group-window.js` validator relaxed to allow `headCount = 0` on closed windows (terminal state after full cull). New V2_APP_ARCHITECTURE.md §4.4 "Window-Split on State Change". **OI-0073 (Parts A/B/C coordinated cleanup):** Part A — dashboard `renderGroupCard` selection prefers open-event windows with most-recent `dateJoined` tie-break. Part B — migration `025_close_orphan_group_windows.sql` closes orphan open windows keeping only the most-recent per group; applied and verified via MCP (0 groups with >1 open window post-apply); `CURRENT_SCHEMA_VERSION` bumped 23→25; `backup-migrations.js` adds 23→24 (reserved slot) and 24→25 no-op entries so the chain stays contiguous. Part C — NPK NaN fix at the property-name mismatch root cause: dashboard was reading `result.n/.p/.k` but NPK-1 returns `{nKg, pKg, kKg}`; rewrote to defensive `result.nKg ?? result.n ?? 0` pattern at 4 sites (331, 422, 679-681). New unit tests: `window-helpers.test.js` (7 cases) + `store-window-split.test.js` (5 cases). New e2e: `cull-dashboard-event-detail.spec.js` (verifies Supabase has exactly 2 rows per (group, event) after cull: closed snapshot + new open). 890 tests pass. **Closes OI-0091 and OI-0073.** | src/data/store.js, src/calcs/window-helpers.js, src/calcs/feed-forage.js, src/features/dashboard/index.js, src/features/events/detail.js, src/features/events/move-wizard.js, src/features/events/close.js, src/features/events/retro-place.js, src/features/events/rotation-calendar/calendar-grid.js, src/features/reports/index.js, src/features/animals/cull-sheet.js, src/entities/event-group-window.js, src/data/backup-migrations.js, src/data/backup-import.js, supabase/migrations/025_close_orphan_group_windows.sql, V2_APP_ARCHITECTURE.md, tests/unit/window-helpers.test.js, tests/unit/store-window-split.test.js, tests/unit/backup-import.test.js, tests/unit/backup-roundtrip.test.js, tests/e2e/cull-dashboard-event-detail.spec.js |
| 2026-04-17 | fix | backup-export.js, backup-import.js, tests/unit/backup-sync.test.js | Closed OI-0087 and OI-0088 from Cowork's local-only fields audit. Added event_observations to BACKUP_TABLES/FK_ORDER; bumped CURRENT_SCHEMA_VERSION 20→23; added 3 mechanical invariant tests so the Export/Import Spec Sync Rule is enforced by CI. |
| 2026-04-17 | docs | SCHEMA_DUMP_2026-04-17.md | Live Supabase schema captured for local-only fields audit. Ground-truth input for Cowork matrix. |
| 2026-04-17 | fix: Cull Sheet v1 parity (OI-0086 / GH-13). Removed broken `window.prompt()` cull stub at `src/features/animals/index.js:1241–1261` that wrote a non-existent `culled` field (silently dropped by `toSupabaseShape()`). New `src/features/animals/cull-sheet.js` exports `openCullSheet`, `buildCulledBanner`, and pure helpers (`confirmCull`, `reactivateAnimal`, `buildAnimalLabel`, `CULL_REASONS`). Cull dialog: date input (defaults to today, can be backdated), 9-option reason `<select>`, optional notes input, amber info banner, Confirm/Cancel buttons. On confirm: writes `active=false` + `cullDate` + `cullReason` + `cullNotes` to Supabase via the entity's existing snake_case mapping AND closes every open `animal_group_memberships` row with `dateLeft=cullDate, reason='cull'`. Toast on success. Red culled-state banner now shows reason + date + notes + Reactivate button (replacing the previous broken red placeholder). Reactivate clears all four cull fields after a confirm. Fixed three stale `a.culled` references (filter, isCulled badge, group picker) to use `a.active === false` so the "Show culled" checkbox works correctly. All store calls verified at 6 params per CLAUDE.md Rule 7. `logger.info('cull', ...)` on confirm + reactivate. New i18n keys (`animal.cull*`, `animal.reactivate*`, `animal.optional`). 12 new unit tests; new `tests/e2e/cull-sheet.spec.js` for sync verification (skips without `E2E_EMAIL`/`E2E_PASSWORD`/`VITE_SUPABASE_*`). 872 tests. | src/features/animals/cull-sheet.js, src/features/animals/index.js, src/i18n/locales/en.json, tests/unit/cull-sheet.test.js, tests/e2e/cull-sheet.spec.js, github/issues/GH-13_cull-sheet-v1-parity.md, OPEN_ITEMS.md |
| 2026-04-17 | fix: §8 Feed Entries v1 parity + bug fixes (OI-0085). (1) Fixed two field-name typos in `renderFeedEntries`: `batch?.feedName` → `batch?.name` and `fe.deliveryDate` → `fe.date`. (2) Rebuilt §8 to v1 inline pattern: `+ Add feed` button in section header, v1-style row layout (date + description on left; DMI/cost stacked + Edit/× buttons on right), inline form mounts below the list (no sheet). New `src/features/events/feed-entry-inline-form.js` holds module-level form state (survives subscriber-triggered re-renders), pure helpers (`validateFeedEntryForm`, `commitAdd`, `commitEdit`, `clampDateToEvent`), and renders into a container. Multi-batch add creates N rows sharing a date; edit updates one row + adjusts `batches.remaining` by the delta; batch is locked in edit mode. All 5 SP-10 §8 validation guards enforced. Delete button restores inventory. `Deliver feed` big footer button removed (replaced by header `+ Add feed`); `Move feed out` footer button kept. 19 new unit tests. 860 tests. | src/features/events/detail.js, src/features/events/feed-entry-inline-form.js, tests/unit/feed-entry-inline-form.test.js, OPEN_ITEMS.md |
| 2026-04-17 | feat: SP-10 §9 feed check edit + re-snap invariant (OI-0084). New `src/features/events/edit-feed-check.js` — per-item edit dialog (date/time/remaining/notes; batch + location read-only chips), range guards (date < event.dateIn, > event.dateOut on closed, > today, remaining < 0), invariant check via pure `buildLineSequence` / `computeIntervals` / `classifyInvariant` helpers covering Cases A (benign save), B (later-interval break → re-snap dialog deletes impossible later items + saves atomically + non-modal toast with "Feed check" shortcut), C (earlier-interval break → Cancel-only conflict dialog), D (back-fill insert routes to A/B/C). Restructured `renderFeedChecks` in `detail.js` to render one row per (check × item) feed line — Edit button now wires to the new dialog. Delete button in the dialog removes the item and orphan parent check. 20 unit tests. 841 tests. | src/features/events/edit-feed-check.js, src/features/events/detail.js, tests/unit/edit-feed-check.test.js, OPEN_ITEMS.md |
| 2026-04-17 | feat: SP-10 retro-place flow (OI-0083). New `src/features/events/retro-place.js` — sheet picker with full-containment filter, optional paddock sub-picker, conflict-check abort, confirm dialog, atomic two-write commit (pre-validate + sequential update/add + manual revert on add failure). Pure helpers (`computeCandidateEvents`, `computeCandidatePaddocks`, `findConflictingWindow`, `commitRetroPlace`) exported for testing. 14 unit tests. Note: gap detection in `edit-group-window.js` is still stubbed (line 95 comment), so retro-place is reachable only via direct invocation/tests until gap detection is wired separately. 835 tests. | src/features/events/retro-place.js, tests/unit/retro-place.test.js, OPEN_ITEMS.md |
| 2026-04-17 | fix: Field mode 3 fixes — (1) feed check returns to #/field when in field mode, (2) event cards expand with detail (date, groups, feed status, cost) + Move All relocated inside expanded section, (3) heat tile 2-step picker: animal selection with event/group filter pills + search, then recording form with toast, stays open for multi-record. Closes OI-0078. | src/features/feed/check.js, src/features/field-mode/index.js, OPEN_ITEMS.md |
| 2026-04-17 | fix: SP-10 deferred — entry_type filtering in DMI-1/CST-1 + inline date_in edit + re-close overlap warning + recovery date preview. Dashboard negates removal quantities. Event summary date_in is editable with reject-on-narrow. Close flow warns on group overlap with other open events. §6 post-graze shows live recovery dates. OI-0083/OI-0084 added for retro-place and feed check edit (design required). | src/features/dashboard/index.js, src/features/events/detail.js, src/features/events/close.js, OPEN_ITEMS.md |
| 2026-04-17 | feat: SP-10 Phase 5 — Event Reopen. Clears date_out + re-opens child windows + group conflict dialog. Reopen button on closed event detail sheet. | src/features/events/reopen-event.js, src/features/events/detail.js |
| 2026-04-17 | feat: SP-10 Phase 4 — §12 paddock window edit + OI-0064 reopen. Date/area/strip edit, range guards, same-paddock overlap reject, delete guards, reopen closed windows. | src/features/events/edit-paddock-window.js, src/features/events/detail.js |
| 2026-04-17 | feat: SP-10 Phase 3 — §7 group window edit. Date/headcount/weight edit, auto-save on blur, delete window, Edit button per group row. Move Feed Out button on §8. | src/features/events/edit-group-window.js, src/features/events/detail.js |
| 2026-04-17 | feat: SP-10 Phase 2 — §8a Move Feed Out. Migration 023 (entry_type, destination_type, destination_event_id on event_feed_entries + check constraints). Entity updated. Move Feed Out 4-step sheet (state → check → amount+dest → confirm). BACKUP_MIGRATIONS[22] entry. | supabase/migrations/023_feed_removal_columns.sql, src/entities/event-feed-entry.js, src/features/events/move-feed-out.js, src/data/backup-migrations.js |
| 2026-04-17 | feat: SP-10 Phase 1 — snapshot/rollback helper + gap/overlap resolver. | src/data/store.js, src/features/events/resolve-window-change.js |
| 2026-04-17 | fix: OI-0080 SP-9 bulk header + surveys tab + bulk-edit replace. Added missing Row 1 action buttons (Cancel, DRAFT pill, Expand/Collapse all, Save Draft, Finish & Save, ✕). Surveys sub-tab: draft resume banner + committed survey list with Edit. Bulk-edit now replaces prior observations. | src/features/locations/index.js, OPEN_ITEMS.md |
| 2026-04-17 | feat: SP-9 survey sheet v1 parity (GH-12). Migration 022 (bale_ring_residue_count + bale_ring_residue_diameter_ft). BRC-1 calc with 6 unit tests. Survey sheet: bulk/single modes, filter pills, collapsible paddock cards, rating sync, bale ring helper, recovery window, draft lifecycle with auto-save, Finish & Save confirm bar. Field mode pasture picker. Settings diameter field. | src/features/locations/index.js, src/calcs/survey-bale-ring.js, src/entities/event-observation.js, src/entities/farm-setting.js, src/data/backup-migrations.js, supabase/migrations/022_survey_bale_ring_columns.sql |
| 2026-04-14 | fix: OI-0056 P1 — REFERENCE_TABLES blocking import delete. Removed 5 per-operation seed tables (forage_types, animal_classes, treatment_categories, treatment_types, input_product_categories) from REFERENCE_TABLES. Only truly global tables (dose_units, input_product_units) remain. Per-operation seed data now follows normal delete-then-insert during import. Cleaned stale Supabase data. 779 tests. | src/data/backup-import.js, tests/unit/backup-import.test.js, OPEN_ITEMS.md |
| 2026-04-14 | fix: OI-0055 P1 — add operation_id to 4 child tables. Migration 019: added operation_id (uuid NOT NULL FK) to event_feed_check_items, harvest_event_fields, survey_draft_entries, todo_assignments with parent-backfill. RLS policies simplified from join-based to direct operation_id. Entity files updated (FIELDS/create/validate/toSupabaseShape/fromSupabaseShape). All add() call sites updated (8 total: close.js, move-wizard.js, check.js, harvest, surveys ×2, todos ×2). v1-migration.js updated for all 4 tables. Backup migration 18→19 backfills from parent tables. Executed against Supabase via MCP + verified. Schema version 19. 779 tests. | supabase/migrations/019_add_operation_id_to_child_tables.sql, src/entities/event-feed-check-item.js, src/entities/harvest-event-field.js, src/entities/survey-draft-entry.js, src/entities/todo-assignment.js, src/data/backup-import.js, src/data/backup-migrations.js, src/data/v1-migration.js, src/features/events/close.js, src/features/events/move-wizard.js, src/features/feed/check.js, src/features/harvest/index.js, src/features/surveys/index.js, src/features/todos/todo-sheet.js, OPEN_ITEMS.md |
| 2026-04-14 | fix: OI-0054 P0 — sync insert/update split + granular RLS. Part 1: sync adapter now uses .insert() for new records, .update() for existing, .upsert() only for recovery/retry. Operation type stored in queue. store.add() passes 'insert', store.update() passes 'update'. Part 2: migration 018 splits all 45 FOR ALL policies into INSERT (WITH CHECK true) / SELECT / UPDATE / DELETE. INSERT allows all authenticated users; membership checks on read/modify only. Part 3: all 12 source migration files (001–012) updated to match. Part 4: executed against Supabase via MCP, verified zero FOR ALL policies remain. Schema version 18. 779 tests. | supabase/migrations/018_granular_rls_policies.sql, src/data/custom-sync.js, src/data/sync-adapter.js, src/data/store.js, src/data/backup-import.js, src/data/backup-migrations.js, supabase/migrations/001–012 (all updated), tests/unit/store.test.js, tests/unit/backup-import.test.js, tests/unit/backup-roundtrip.test.js, OPEN_ITEMS.md |
| 2026-04-14 | fix: OI-0053 P0 — Supabase migrations + RLS bootstrap. Applied missing migrations 014–016 to Supabase (active_farm_id, schema_version, invite_token + RPCs). Fixed operation_members RLS infinite recursion: dropped FOR ALL policy, replaced with granular SELECT/INSERT/UPDATE/DELETE. INSERT uses `user_id = auth.uid()` for onboarding bootstrap (no self-referential subquery). Disabled RLS on dose_units/input_product_units (standalone lookups, no operation_id). Migration 017 written. Migration 001 updated for fresh DB. Schema version 17. 779 tests. | supabase/migrations/017_fix_operation_members_rls.sql, supabase/migrations/001_d1_operations_farms.sql, src/data/backup-import.js, src/data/backup-migrations.js, tests/unit/backup-import.test.js, tests/unit/backup-roundtrip.test.js, OPEN_ITEMS.md |
| 2026-04-14 | fix: OI-0048/0049/0051 — pre-migration data fixes. OI-0048: observation type inferred from raw v1 source string (event_close→close, else open). OI-0049: feed transfer source linking via transferPairId index — both sides get source_event_id, orphans logged to audit. OI-0051: "Copy error log" button on all 3 migration result screens (success, parity, error) — collects logger buffer, audit warnings, result summary, copies to clipboard. 7 new tests. 779 tests passing. | src/data/v1-migration.js, src/features/settings/v1-import.js, src/i18n/locales/en.json, tests/unit/v1-migration.test.js, OPEN_ITEMS.md |
| 2026-04-14 | fix: OI-0050 — onboarding & settings records never synced to Supabase. All 10 add() calls in onboarding and 5 update() calls in settings now include toSupabaseFn + table params. Full codebase audit confirmed 0 other missing sync params. Added pushAllToSupabase() recovery function (src/data/push-all.js) + "Resync to server" button in Settings → Sync & Data for existing users who onboarded before this fix. 772 tests. | src/features/onboarding/index.js, src/features/settings/index.js, src/data/push-all.js, src/i18n/locales/en.json, OPEN_ITEMS.md |
| 2026-04-14 | CP-66: Member management & invite link. Migration 016 (invite_token uuid UNIQUE on operation_members + claim_invite_by_token + claim_pending_invite RPCs). Entity updated (inviteToken field). Backup migration chain 14→15→16. Member management sheet (list with role badges, invite creation form, copy/regenerate/cancel link, role change, remove member). Invite acceptance flow (router #invite= detection, auth context banner, sessionStorage token persistence, claim RPC, email fallback). Settings members section (admin: manage button, team: read-only count). 37 i18n keys. 772 tests passing. | supabase/migrations/016_invite_token.sql, src/entities/operation-member.js, src/data/backup-import.js, src/data/backup-migrations.js, src/features/settings/member-management.js, src/features/settings/index.js, src/features/auth/invite-claim.js, src/main.js, src/styles/main.css, src/i18n/locales/en.json, tests/unit/entities/operation-member.test.js, tests/unit/invite.test.js |
| 2026-04-14 | CP-66 spec written — member management & invite link (shareable URL, no email service). Schema: invite_token uuid on operation_members. UX: §20.7 in V2_UX_FLOWS.md. Phase 3.5. | github/issues/GH-10_cp-66-member-management.md, V2_UX_FLOWS.md, V2_BUILD_INDEX.md, V2_SCHEMA_DESIGN.md, OPEN_ITEMS.md |
| 2026-04-14 | Audit findings fixes (OI-0040/41/43/45/46). Fix 1 (OI-0040/41): Pre-graze and post-graze observation fields on all close/open surfaces — new observation-fields.js helper, createObservation() extended with fields param, post-graze on close.js/move-wizard.js/submove.js, pre-graze on move-wizard.js/submove.js, farm_settings.recovery_required toggle in Settings, unit-aware height display, i18n keys. Fix 2 (OI-0043): Harvest tile → #/harvest. Fix 3 (OI-0045): Removed dead daysBetweenExact(). Fix 4 (OI-0046): "Get The Hay Out" app name in header. OI-0042 deferred to Phase 3.5. 762 tests passing. | src/features/events/observation-fields.js, src/features/events/close.js, src/features/events/move-wizard.js, src/features/events/submove.js, src/features/events/index.js, src/features/settings/index.js, src/features/field-mode/index.js, src/ui/header.js, src/utils/date-utils.js, src/styles/main.css, src/i18n/locales/en.json, tests/unit/date-utils.test.js, OPEN_ITEMS.md |
| 2026-04-14 | Full app audit (Tier 2, 7 audits). Audit 7 (i18n): 28 violations found, 28 fixed (validation messages, filter labels, dashboard display labels, form defaults, reference console title, calendar toolbar, mobile event summary, parity reports, currency display). Audit 8 (innerHTML): 0 violations. Audit 9 (Logger): 0 violations. Audit 10 (Sync): all 8 priority files pass — zero missing sync calls. Audit 11 (UX Flows): §7 Survey PASS; §1 Move wizard 3 deviations (OI-0040, OI-0041); §9 Event close 2 deviations (OI-0040); §14 Health 2 deviations (OI-0042); §16 Field mode 1 deviation (OI-0043); §17 Dashboard 1 partial. Audit 12 (Dead Code): 1 dead export (OI-0045), 0 orphan routes, 0 commented blocks. Audit 13 (Design Tokens): 1 hardcoded hex fixed, 92 inline px values noted as pattern. 765 tests passing. Pre-migration full app audit: 33 violations found, 29 fixed, 5 flagged in OPEN_ITEMS.md. | src/features/dashboard/index.js, src/features/events/index.js, src/features/events/close.js, src/features/events/move-wizard.js, src/features/events/group-windows.js, src/features/events/list-view/events-log.js, src/features/events/mobile-events-screen.js, src/features/events/rotation-calendar/toolbar.js, src/features/reports/reference-console.js, src/features/settings/index.js, src/features/settings/v1-import.js, src/features/amendments/entry.js, src/features/health/weight.js, src/features/health/bcs.js, src/features/feed/delivery.js, src/features/feed/index.js, src/features/todos/todo-sheet.js, src/calcs/feed-forage.js, src/i18n/locales/en.json, OPEN_ITEMS.md |
| 2026-04-14 | Pre-migration audit (Tier 1, 6 audits). Audit 1 (Entity↔Schema): 52 entities verified, 0 mismatches. Audit 2 (Round-Trip): all 52 entities have substantive round-trip tests. Audit 3 (Store↔Entity): 52 types aligned, all add/update actions validate. Audit 4 (Transform Completeness): all 25 sections correct, flagged §2.25 spec text vs schema mismatch (OI-0039). Audit 5 (OI-0012): 29 new calc tests — DMI-1 residual regression, DMI-2 lactation branching, DMI-5 interpolation, FED-1 residual pct, CST-1/2/3 cost math, REC-1 strip graze recovery (13→42 calc tests). Audit 6 (OI-0013): 37 registerCalc() descriptions audited, 1 fix (CST-2 "bidirectional"). 765 tests passing. | tests/unit/calcs.test.js, src/calcs/feed-forage.js, OPEN_ITEMS.md |
| 2026-04-14 | CP-58: v1 migration integration tests + CP-57 spec drift fixes. Audit: OI-0037 (schema_version was hardcoded — now imported from backup-import.js), OI-0038 (auto-backup not skipped for empty operations — added skipAutoBackup option to importOperationBackup). 17 integration tests (full v1 fixture → transform → validate envelope → verify counts, conversions, splits, no data loss). 7 Playwright e2e tests (v1 import button, format detection, preview with counts, two-step confirm, cancel, unparseable dose warning). v1 fixture at tests/fixtures/v1-export-sample.json. 747 tests passing. | src/data/v1-migration.js, src/data/backup-import.js, src/features/settings/v1-import.js, tests/unit/v1-migration.test.js, tests/e2e/v1-migration.spec.js, tests/fixtures/v1-export-sample.json, OPEN_ITEMS.md |
| 2026-04-14 | CP-57: v1 → v2 migration tool — v1-migration.js (25 transform sections per §2.1–§2.25: ID remapping, imperial→metric conversions, location type mapping, event/paddock-window/group-window restructuring, 5-way health event split, dose parsing with audit CSV, NPK parity data collection, settings decomposition into operations+farms+farm_settings+user_preferences, NPK price history seeding). v1-import.js (Settings → Import from v1 button, file detection, transform preview with counts/warnings, two-step confirm, CP-56 pipeline handoff, dose audit CSV auto-download, success/parity/error reporting). 50 new unit tests. 730 tests passing. GH-9 filed. | src/data/v1-migration.js, src/features/settings/v1-import.js, src/features/settings/index.js, src/i18n/locales/en.json, tests/unit/v1-migration.test.js, github/issues/GH-9_cp-57-v1-migration-tool.md |
| 2026-04-13 | CP-56: JSON backup import — backup-import.js (§5.7 steps 1-10: envelope validation, preview extraction, migration chain, FK-ordered wholesale replace per §5.3a, two-pass for animals/events, reference table upsert, auto-backup before destructive replace, parity check). Settings import button with file picker, preview sheet, two-step destructive confirm, progress phases, parity report. 27 unit tests (validation, FK ordering, two-pass, reference tables) + 5 E2E tests. 680 tests passing. | src/data/backup-import.js, src/features/settings/index.js, src/styles/main.css, src/i18n/locales/en.json, tests/unit/backup-import.test.js, tests/e2e/backup-import.spec.js |
| 2026-04-13 | CP-55: JSON backup export — migration 015 (operations.schema_version), operation entity update, backup-export.js (Supabase-read with pagination, §5.2 envelope, §5.5 serialization, offline/sync guard, progress UI), backup-migrations.js (empty registry at v14), Settings export button with confirm/progress sheets, download trigger. 49 tables included, 3 excluded. Round-trip test (26 cases) + fixture + E2E test. 653 tests passing. | supabase/migrations/015_schema_version_stamp.sql, src/entities/operation.js, src/data/backup-export.js, src/data/backup-migrations.js, src/features/settings/index.js, src/styles/main.css, src/i18n/locales/en.json, tests/unit/entities/operation.test.js, tests/unit/backup-roundtrip.test.js, tests/fixtures/backup-v14.json, tests/e2e/backup-export.spec.js |
| 2026-04-13 | CP-54: Rotation calendar — FOR-6 (Forecast Standing DM) + CAP-1 (Period Capacity Coverage) formulas registered with full metadata. 11 new unit tests for confidence bands, strip-grazing sums, coverage fractions, never-grazed, multi-group, surplus/shortfall. Formula count 35 → 37 across 11 domains. | src/calcs/feed-forage.js, src/calcs/capacity.js, src/main.js, tests/unit/calcs.test.js |
| 2026-04-13 | CP-54: Calendar state module — URL query schema (zoom, anchor, groups, period, showConfinement, view), defaults per §19.9, reducers, history.replaceState sync, readStateFromUrl for deep-link support. Router updated to strip query params before route matching. 24 unit tests. | src/features/events/calendar-state.js, src/ui/router.js, tests/unit/calendar-state.test.js |
| 2026-04-13 | CP-54: Rotation calendar UI — 10-file component subtree (header-strip, toolbar, legend, calendar-grid, paddock-column, timeline, sidebar, past-block, future-block, today-line). 3-column grid layout, past event blocks with multi-group labels/strip bands/linked outlines/NOW ring, future forecast blocks (estimated gradient + DM capacity split), toolbar lightboxes, confinement toggle, keyboard shortcuts. 15 component unit tests. | src/features/events/rotation-calendar/*.js, tests/unit/rotation-calendar.test.js |
| 2026-04-13 | CP-54: List view + mobile fallback — GRZ-10 events log (parent + sub-move threads, filter, summary metrics) for desktop List toggle and mobile. GRZ-11 active-rotation banner (paddock chips, date, feedings, groups) for mobile. Mobile events screen rendered at <900px, no calendar. | src/features/events/list-view/events-log.js, src/features/events/mobile-events-screen.js |
| 2026-04-13 | CP-54: Events screen orchestrator — width check (≥900px: calendar + view toggle, <900px: mobile fallback). Calendar state read from URL on mount. Sheet wrappers preserved for event management actions. | src/features/events/index.js |
| 2026-04-13 | CP-54: Reports tabs updated — added Pasture Surveys + Weaning placeholder tabs. Reference tab kept. Tab order: Feed & DMI, NPK Fertility, Animal Performance, Season Summary, Pasture Surveys, Weaning, Reference (7 total). No stale rotation-calendar links found. | src/features/reports/index.js, src/i18n/locales/en.json |
| 2026-04-13 | CP-54: Calendar CSS — header strip, toolbar (lightboxes, pills, chips, confinement toggle), legend (swatches), calendar grid (3-col layout), paddock column, timeline (axes, today line), past/future blocks, sidebar, list view, mobile banner. | src/styles/main.css |
| 2026-04-13 | CP-54: E2E test — 7 Playwright tests: default first-load, view toggle, confinement toggle, deep-link, mobile fallback, resize behavior, Reports tab count. | tests/e2e/rotation-calendar.spec.js |
| 2026-04-13 | All-farms mode filtering (GH-5) — dashboard, events, locations, animals screens use getVisibleEvents/Locations/Groups(). Farm chips on event cards in all-farms mode. 575 tests. | src/features/events/index.js, src/features/locations/index.js, src/features/animals/index.js, src/features/dashboard/index.js |
| 2026-04-13 | Move wizard farm chip + cross-farm event markers (GH-5) — farm chip on Step 2 location picker scopes to dest farm without mutating active_farm_id. Cross-farm moves set sourceEventId. Event cards show directional markers (← from / → to) with farm names. CSS for cross-farm-marker, wizard-farm-chip, farm-chip. 575 tests. | src/features/events/move-wizard.js, src/features/events/index.js, src/styles/main.css |
| 2026-04-13 | Header redesign + farm picker + user menu (GH-5) — left cluster: operation name (18px/700) + farm picker button (multi-farm: chevron, single: plain text). Right cluster: sync dot + build stamp + Field Mode + user menu (28px circle with initials). Farm picker dropdown with All Farms + alphabetical farm list. User menu popover with email + Log Out (confirm only if unsynced writes). 575 tests. | src/ui/header.js, src/styles/main.css, src/i18n/locales/en.json |
| 2026-04-13 | Store API for multi-farm context (GH-5) — getActiveFarmId(), setActiveFarm(farmId), getVisibleLocations/Groups/Events(). Deleted-farm fallback. 5 new store tests. 575 tests. | src/data/store.js, tests/unit/store.test.js |
| 2026-04-13 | Entity updates for multi-farm context (GH-5) — user-preference.js +activeFarmId, event.js +sourceEventId. FIELDS, create, validate, toSupabaseShape, fromSupabaseShape all updated. 2 new round-trip tests. 570 tests. | src/entities/user-preference.js, src/entities/event.js, tests/unit/entities/user-preference.test.js, tests/unit/entities/event.test.js |
| 2026-04-13 | Schema migration 014: multi-farm context (GH-5) — user_preferences.active_farm_id (uuid NULL FK farms ON DELETE SET NULL) + events.source_event_id (uuid NULL FK events ON DELETE SET NULL) | supabase/migrations/014_multi_farm_context.sql |
| 2026-04-13 | Product add dialogs expose unit selection (closes OI-0017) — added unitId selector from inputProductUnits to amendment input product create/edit sheet. Feed type and treatment recording sheets already had unit fields. 568 tests. | src/features/amendments/reference-tables.js, OPEN_ITEMS.md |
| 2026-04-13 | Sync status indicator in app header (closes OI-0018) — compact dot-based indicator (idle/syncing/offline/error) in header-right. Tap navigates to settings sync panel. Reuses existing getSyncAdapter().getStatus(). 568 tests. | src/ui/header.js, src/styles/main.css, OPEN_ITEMS.md |
| 2026-04-13 | Dose units CRUD (closes OI-0016) — add/edit sheet, archive action in settings health reference tables. Follows existing category/type pattern. 568 tests. | src/features/health/reference-tables.js, src/i18n/locales/en.json, OPEN_ITEMS.md |
| 2026-04-13 | Verify manure volumeKg placeholder (closes OI-0014) — confirmed placeholder is architecturally correct (reports compute via NPK-1 at display time, not stored column). Updated code comment in close.js. | src/features/events/close.js, OPEN_ITEMS.md |
| 2026-04-13 | Wire feed screen metrics to live data (closes OI-0011) — DM on hand (batch.remaining × dm_pct), daily run rate (30-day avg DM delivered), days on hand (DM ÷ rate). Three stat cells in goal banner. Progress bar threshold coloring active. 568 tests. | src/features/feed/index.js, OPEN_ITEMS.md |
| 2026-04-13 | docs: add OI-0017 (product unit selection), OI-0018 (header sync status), OI-0019 (header logout) to OPEN_ITEMS.md | OPEN_ITEMS.md |
| 2026-04-13 | Strip grazing UI (A45) — dual area/pct input in move wizard with auto-derivation and unit awareness, strip progress bar visualization per §3.15 on event cards and dashboard (active/completed/upcoming states, proportional widths), per-window area_pct labels. Standalone commit before CP-54. 568 tests. | src/features/events/move-wizard.js, src/features/events/index.js, src/features/dashboard/index.js, src/styles/main.css, src/i18n/locales/en.json |
| 2026-04-13 | Unit system migration (A44) — move unit_system from localStorage to operations table. SQL migration 013, entity update (FIELDS/validate/shape), store.setUnitSystem() action, preferences.js reads from store, settings toggle rewired, onboarding adds unit selector, boot-time localStorage migration. 568 tests. | supabase/migrations/013, src/entities/operation.js, src/data/store.js, src/utils/preferences.js, src/features/settings/index.js, src/features/onboarding/index.js, src/main.js, tests/unit/entities/operation.test.js |
| 2026-04-13 | docs: integrate A44 unit system + A45 strip grazing across specs; close OI-0001, OI-0002 — schema design, infrastructure, migration plan, build index, open items updated; new implementation spec for unit system migration | GTHO_V2_SCHEMA_DESIGN.md, V2_INFRASTRUCTURE.md, V2_MIGRATION_PLAN.md, V2_BUILD_INDEX.md, OPEN_ITEMS.md, github/issues/unit-system-operations-migration.md |
| 2026-04-13 | OI-0009: Fix desktop layout overlap — add grid-column: 2 to .app-content in desktop media query | src/styles/main.css |
| 2026-04-13 | OI-0010: Complete dashboard rebuild per §17 — stats row (5 desktop / 3 mobile), period pills, view toggle, group cards (composition, location bar, DMI, NPK, actions), location cards, open tasks section, survey draft card, weaning nudge, mobile bottom nav, header with farm name | src/features/dashboard/index.js, src/ui/header.js, src/styles/main.css, src/i18n/locales/en.json |
| 2026-04-13 | Todos feature UI — list screen with 3-axis filters, create/edit sheet, card component, #/todos route, nav entries with badge | src/features/todos/index.js, src/features/todos/todo-card.js, src/features/todos/todo-sheet.js, src/main.js, src/ui/header.js |
| 2026-04-11 | Initial scaffold — repo setup with doc structure and issue tracking | CLAUDE.md, PROJECT_CHANGELOG.md, OPEN_ITEMS.md, IMPROVEMENTS.md, github/issues/ |
| 2026-04-12 | CP-1: Vite project init — package.json, vite.config.js, index.html, src/main.js, .gitignore, .env.example, eslint.config.js, vitest harness | package.json, vite.config.js, index.html, src/main.js, .gitignore, .env.example, eslint.config.js, tests/unit/setup.test.js |
| 2026-04-12 | CP-2: Core utilities — logger, date-utils, validators, units (6 types), calc-registry with 89 passing tests | src/utils/logger.js, date-utils.js, validators.js, units.js, calc-registry.js + 4 test files |
| 2026-04-12 | CP-3: D1 entities — operation, farm, farm-setting, operation-member, user-preference + migration SQL + 5 test files (134 total tests) | src/entities/5 files, supabase/migrations/001, tests/unit/entities/5 files |
| 2026-04-12 | CP-4: D2-D4 entities — location, forage-type, animal-class, animal, group, animal-group-membership, feed-type, batch, batch-adjustment + 3 migrations + 9 test files (179 total tests) | src/entities/9 files, supabase/migrations/002-004, tests/unit/entities/9 files |
| 2026-04-12 | CP-5: D5-D7 entities — 11 entities (events, surveys, harvest) + 3 migrations + 11 test files (226 total tests) | src/entities/11 files, supabase/migrations/005-007, tests/unit/entities/11 files |
| 2026-04-12 | CP-6: D8-D11 entities — 26 entities (nutrients, health, feed quality, infrastructure) + 4 migrations + 26 test files (473 total tests). All 51 entities complete. | src/entities/26 files, supabase/migrations/008-011, tests/unit/entities/26 files |
| 2026-04-12 | CP-7: i18n setup — t() with nested keys, interpolation, missing-key fallback + en.json skeleton + tests | src/i18n/i18n.js, src/i18n/locales/en.json, tests/unit/i18n.test.js |
| 2026-04-12 | CP-8: Data layer — store (51 entity types, getters return copies, validate→mutate→persist→notify), sync adapter interface, custom sync (offline queue, exponential backoff, dead letters), localStorage wrapper, Supabase client + 3 test files (516 total tests) | src/data/store.js, local-storage.js, sync-adapter.js, custom-sync.js, supabase-client.js + 3 test files |
| 2026-04-12 | CP-9: UI framework — DOM builder (el/text/clear, no innerHTML), Sheet class (.open toggle), hash router (7 routes), header, main.css (design tokens, responsive layout) + 2 test files | src/ui/dom.js, sheet.js, router.js, header.js, src/styles/main.css + 2 test files |
| 2026-04-12 | CP-10: App shell — boot sequence wired (store→locale→router→header), 7 placeholder screens, integration test (create→read→shape round-trip→subscriber). Phase 3.1 Scaffold COMPLETE. 538 tests passing. | src/main.js, index.html, 7 feature/index.js placeholders, tests/unit/integration.test.js |
| 2026-04-12 | GitHub Actions CI/deploy — lint→test→build→deploy to Pages on push to main. Vite base path set to /GTHO-v2/. Pages switched to Actions workflow. | .github/workflows/deploy.yml, vite.config.js, CLAUDE.md |
| 2026-04-12 | CP-11: Auth flow — login/signup/logout via Supabase Auth, session persistence, auth overlay (z-500), boot sequence gates app behind auth | src/features/auth/session.js, auth/index.js, main.js, en.json, main.css |
| 2026-04-12 | CP-12: Onboarding wizard — 4-step wizard (operation→farm→species→review), seeds animal classes with NRCS defaults, treatment/input categories, forage types, dose/product units | src/features/onboarding/index.js, seed-data.js, main.js, en.json, main.css |
| 2026-04-12 | CP-13: Settings screen — farm settings (12 fields), unit system toggle, user prefs (view/home mode), account/logout, sync status indicator. OI-0002 logged for missing unit_system column. | src/features/settings/index.js, src/utils/preferences.js, en.json, main.css, OPEN_ITEMS.md |
| 2026-04-12 | All 11 Supabase migrations applied — 51 tables created with RLS enabled via MCP | supabase/migrations/001–011 (applied to remote) |
| 2026-04-12 | CP-14: Locations screen — location CRUD with cards, filter pills (All/Pasture/Mixed-Use/Crop/Confinement), type-conditional create/edit sheet, area in user units, forage type CRUD section, data-testid on all interactive elements. 538 tests passing. | src/features/locations/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-15: Animals screen — Groups & Classes tabs. Group CRUD with cards (color dot, head count from memberships). Class CRUD with species grouping, filter pills, edit sheet for all rates (DMI, excretion, weight in user units, weaning age). 538 tests passing. | src/features/animals/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-16: Individual animals — Animals tab with sortable table (tag/class/group columns), search by tag/name/EID, animal CRUD sheet with sex toggle, class picker, group assignment (creates/closes memberships via ledger), birth date, notes. 538 tests passing. | src/features/animals/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-17: Events — Create & List. Event list (active first, day count, status badges). Event card shows paddock windows (primary label, open/closed) and group windows (head count, avg weight snapshots). Create event sheet with location picker (Ready/In Use/Confinement sections) and group picker (auto-fills head count + avg weight from memberships). Creates event + paddock_window + group_window records. 538 tests passing. | src/features/events/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-18: Paddock & Group windows. Sub-move open/close sheets (z-index 210). Primary paddock Close button disabled with tooltip. Group add sheet (group picker, head count, avg weight). Group remove sheet (date/time left). Event cards update live via subscribers. Advance Strip button wired (strip observation fields deferred to Phase 3.3). 538 tests passing. | src/features/events/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-19: Move wizard — 3-step wizard (destination type → location/event picker → close & move). Closes source event (all windows + date_out), creates destination event with paddock + group windows. Join existing event support. Strip graze flags (is_strip_graze, strip_group_id, area_pct). Feed transfer placeholder. 9-action save sequence. 538 tests passing. | src/features/events/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-20: Event close (no move) — Close Event button on active event cards. Sheet with date/time out. Closes all open paddock + group windows, sets event date_out. Feed check placeholder. Observation creation deferred to Phase 3.3. 538 tests passing. | src/features/events/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-21: Dashboard — Group cards with color bar, head count, location status (from active events), days on pasture. Mobile collapse/expand. Desktop 2-col grid. Move button navigates to events and triggers move wizard. FAB with creation shortcuts (new event, location, group). Reactive updates via store subscribers. Metrics placeholder. 538 tests passing. | src/features/dashboard/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-22: Supabase sync wiring — CustomSync adapter wired in boot sequence. Online writes → Supabase upsert. Offline → localStorage queue. Flush on reconnect via window 'online' event. Dead letters after 5 retries. Sync status indicator on settings screen reads from adapter. 538 tests passing. | src/main.js |
| 2026-04-12 | CP-23: Integration smoke test — Playwright e2e covering signup → onboard → locations → animals → events → sub-move → move wizard → close event → verify persistence. Playwright config added. 538 unit tests passing. **Phase 3.2 Core Loop COMPLETE.** | playwright.config.js, tests/e2e/smoke.spec.js |
| 2026-04-12 | OI-0004 fix: Pull/merge from Supabase — sync registry (50 entity types → table + fromSupabaseShape), mergeRemote() with remote-wins-on-updated_at logic (5 tests), pullAllRemote() orchestrator, wired into boot + reconnect. 543 tests. | src/data/sync-registry.js, src/data/pull-remote.js, src/data/store.js, src/main.js, tests/unit/store.test.js |
| 2026-04-12 | OI-0005 fix: E2E test selectors — fixed 3 onboarding mismatches, changed from signup to login flow, added env var guard. All 35 selectors verified against source. | tests/e2e/smoke.spec.js |
| 2026-04-12 | OI-0007 fix: Paddock observations — createObservation() helper wired into all 5 event action points (event create, sub-move open, sub-move close, move wizard, event close). Records shell with type/source/sourceId/observedAt. Forage fields Phase 3.3. 543 tests. | src/features/events/index.js, OPEN_ITEMS.md |
| 2026-04-12 | OI-0006 fix: Advance Strip button — renders on event cards with open strip graze windows. Two-phase sheet (close current + open next). End strip early option. Strip progress label. Creates close/open observations. 543 tests. | src/features/events/index.js, OPEN_ITEMS.md |
| 2026-04-12 | Schema amendment: animal_notes table (OI-0003). Migration 012, entity file, store (52 types), sync registry, 7 round-trip tests. 550 tests. | supabase/migrations/012, src/entities/animal-note.js, src/data/store.js, src/data/sync-registry.js, tests/unit/entities/animal-note.test.js |
| 2026-04-12 | CP-24: Feed types CRUD — Feed screen with feed type list (category, unit, DM%, weight, harvest badge). Create/edit sheet with all fields: name, category, unit, DM%, NPK%, default weight, cutting #, forage type, harvest_active toggle. 550 tests. | src/features/feed/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-25: Batch inventory — Batch CRUD with cards (remaining/total progress bar, color-coded). Batch adjustment sheet (reconcile/waste/sold/other) creates batch_adjustment record + updates batch remaining. Feed metrics placeholder for daily run rate/DM on hand. 550 tests. | src/features/feed/index.js, src/i18n/locales/en.json, src/styles/main.css |
| 2026-04-12 | CP-26: Feed day goal — Banner at top of feed screen showing goal from farm_settings.feed_day_goal. Progress bar with threshold coloring (green ≥ goal, amber 33–99%, red < 33%). Days on hand placeholder until CP-27 provides consumption data. 550 tests. | src/features/feed/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-27: Feed delivery — Deliver Feed button on active event cards. Delivery sheet with batch selector, paddock picker (from active windows), quantity with ±0.5 adjusters, date/time. Creates event_feed_entry + decrements batch.remaining. 550 tests. | src/features/events/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-28: Feed check — Feed Check button on active event cards. Sheet shows one row per batch×paddock with total delivered and remaining input. Creates event_feed_check + event_feed_check_items. 550 tests. | src/features/events/index.js, src/i18n/locales/en.json |
| 2026-04-12 | Refactor: Split events/index.js into 7 domain-scoped files per V2_APP_ARCHITECTURE.md §3 (events/index 725, move-wizard 419, submove 303, group-windows 208, close 106, feed/delivery 197, feed/check 156). | src/features/events/*, src/features/feed/* |
| 2026-04-12 | CP-29: Feed transfer in move wizard — Step 3 shows remaining feed per batch×paddock with "Move?" toggles. Creates close-reading feed_check on source (is_close_reading=true) + feed_entry on destination with source_event_id. Replaces placeholder. 550 tests. | src/features/events/move-wizard.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-30: Event close full version — optional feed check (shows batch×paddock remaining inputs when feed exists, creates close-reading check). Confinement NPK routing creates manure_batch_transaction (type='input') for locations with capture_percent > 0. Volume populated by calc engine. 550 tests. | src/features/events/close.js |
| 2026-04-12 | CP-31: Survey workflow — create survey (bulk/single), draft entry form with all fields (forage height, cover, quality, condition, bale ring residue, recovery days, notes). Bulk auto-creates entries for all land locations. "Finish Survey" commits drafts to paddock_observations. Route #/surveys registered. 550 tests. | src/features/surveys/index.js, src/main.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-32: Health reference tables — AI sires CRUD, treatment categories CRUD, treatment types CRUD (with category FK), dose units list. All on settings screen. Sheets in health/reference-tables.js. 550 tests. | src/features/health/reference-tables.js, src/features/settings/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-33: Weight & BCS recording — health/weight.js and health/bcs.js reusable sheets per §14. Weight + BCS buttons on animal table rows. Species-dependent BCS scale (cattle 1–9, sheep/goat 1–5). Latest weight + BCS displayed in animal table. 550 tests. | src/features/health/weight.js, src/features/health/bcs.js, src/features/animals/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-34: Field mode — field-mode/index.js home screen with 4 action tiles, active events (feed status dots), tasks. Toggle in header. URL param (?field=*) + localStorage persistence. Full-screen sheets on mobile via CSS. Field mode header with exit/home nav. #/field route registered. 550 tests. | src/features/field-mode/index.js, src/ui/header.js, src/main.js, src/utils/preferences.js, src/styles/main.css, src/i18n/locales/en.json |
| 2026-04-12 | CP-35: Treatment recording — health/treatment.js with treatment type picker (grouped by category), product, structured dose (amount + unit from dose_units), withdrawal date, date, notes. Treatment button on animal table rows. 550 tests. | src/features/health/treatment.js, src/features/animals/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-36: Breeding & heat — health/breeding.js (method toggle AI/bull, sire pickers for AI bulls + herd bulls, semen ID, technician, expected calving, confirmed date) + health/heat.js (date, notes). Both wired to animal table rows. 550 tests. | src/features/health/breeding.js, src/features/health/heat.js, src/features/animals/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-37: Calving — health/calving.js. Creates calf animal (tag, sex, class, group assignment via membership), birth weight record (source='calving'), calving record (dam link, stillbirth, dried_off_date for dairy). A27 class reassignment prompt (heifer→cow). 550 tests. | src/features/health/calving.js, src/features/animals/index.js, src/i18n/locales/en.json |
| 2026-04-12 | CP-38: Amendment reference tables — product categories CRUD, input products CRUD (NPK%, cost), spreaders CRUD (capacity in user units), product units list. All on settings screen. 550 tests. | src/features/amendments/reference-tables.js, src/features/settings/index.js, src/i18n/locales/en.json |
| 2026-04-13 | CP-39: Soil tests — per-paddock recording with 13-element nutrient panel (N-Cl), pH/buffer/CEC/base sat/OM, lab, extraction method. List with NPK+pH summary. #/soil-tests route. 550 tests. | src/features/amendments/soil-tests.js, src/main.js, src/i18n/locales/en.json |
| 2026-04-13 | CP-40: Amendment entry — date, source (product/manure), product picker, quantity, cost override, multi-select paddocks, NPK preview. Creates amendment + amendment_locations. #/amendments route. 550 tests. | src/features/amendments/entry.js |
| 2026-04-13 | CP-41: Manure system — batch CRUD (label, source location, 13-element composition, volume, capture date). Transaction ledger per batch (input/use/transfer/adjust). #/manure route. 550 tests. | src/features/amendments/manure.js |
| 2026-04-13 | CP-42: NPK price history — per-farm with effective_date. List sorted by date, current entry highlighted. #/npk-prices route. 550 tests. | src/features/amendments/npk-prices.js |
| 2026-04-13 | CP-43: Harvest — select location + harvest-active feed types, quantity/weight/DM%/cutting. Creates harvest_event + harvest_event_fields + auto-creates batch (source='harvest'). #/harvest route. 550 tests. | src/features/harvest/index.js |
| 2026-04-13 | CP-44: Feed quality / batch profiles — lab results entry (DM%, protein, ADF, NDF, TDN, RFV, NPK, minerals). Source tracking. #/feed-quality route. 550 tests. | src/features/feed/quality.js |
| 2026-04-13 | CP-45: Calc engine core — 9 formulas registered: NPK-1 (excretion), NPK-3 (paddock attribution), ANI-1 (group totals), ANI-2 (animal-days), ANI-3 (weaning target), TIM-1/2/3 (time calcs), UNT-1 (unit conversion). Reference console on reports screen. | src/calcs/core.js, src/features/reports/reference-console.js, src/features/reports/index.js, src/main.js |
| 2026-04-13 | CP-46: Calc engine feed & forage — 20 formulas: DMI-1 through DMI-7 + DMI-1a, FOR-1 through FOR-5, CST-1/2, FED-1 through FED-5. Strip-grazing-aware FOR-1. Lactation-aware DMI-2. | src/calcs/feed-forage.js |
| 2026-04-13 | CP-47: Calc engine advanced — 6 formulas: NPK-2 (fertilizer value with price history), NPK-4 (amendment NPK), CST-3, REC-1 (recovery window), SUR-1 (quality rating), UNT-2 (manure volume). All 35 formulas registered. 13 calc tests. 563 total tests. | src/calcs/advanced.js, tests/unit/calcs.test.js |
| 2026-04-13 | CP-48–51: Reports — 4 report tabs (Feed & DMI, NPK Fertility, Animal Performance, Season Summary) + Reference Console tab. Feed tab: total deliveries/DM/cost + group DMI breakdown. NPK tab: per-event NPK with price history. Animals tab: weight/BCS/breeding/treatment summaries. Season tab: AUDs by paddock + feed costs + survey trends. | src/features/reports/index.js, src/i18n/locales/en.json |
| 2026-04-13 | CP-52: Dashboard full metrics — group cards show real DMI (via DMI-2), feed cost (via CST-1), days on pasture. Replaces placeholder text. | src/features/dashboard/index.js, src/i18n/locales/en.json |
| 2026-04-13 | CP-53: E2E smoke test updated — added CP-53 test covering batch → deliver feed → survey → all report tabs render → dashboard metrics. Requires E2E credentials. **Phase 3.3 Assessment COMPLETE.** | tests/e2e/smoke.spec.js |
